% State the fact: name(pack_uhigh) — a binding test fixture pack, not a real pack.
name(pack_uhigh).
% State the fact: version('1.0.0').
version('1.0.0').
% State the fact: title — a layer-to-stratum binding fixture.
title('Binding fixture: layer 3 bound to stratum high_stratum').
% State the fact: layer(3) — the pack's declared layer.
layer(3).
% State the fact: stratum(high_stratum) — the stratum this pack represents (the binding declaration).
stratum(high_stratum).
