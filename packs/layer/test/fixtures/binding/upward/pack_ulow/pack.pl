% State the fact: name(pack_ulow) — a binding test fixture pack, not a real pack.
name(pack_ulow).
% State the fact: version('1.0.0').
version('1.0.0').
% State the fact: title — a layer-to-stratum binding fixture.
title('Binding fixture: layer 1 bound to stratum low_stratum').
% State the fact: layer(1) — the pack's declared layer.
layer(1).
% State the fact: stratum(low_stratum) — the stratum this pack represents (the binding declaration).
stratum(low_stratum).
