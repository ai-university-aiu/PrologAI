% State the fact: name(pack_nostratum) — a binding test fixture pack, not a real pack.
name(pack_nostratum).
% State the fact: version('1.0.0').
version('1.0.0').
% State the fact: title — a layer-to-stratum binding fixture.
title('Binding fixture: layer 2 bound to stratum ').
% State the fact: layer(2) — the pack's declared layer.
layer(2).
