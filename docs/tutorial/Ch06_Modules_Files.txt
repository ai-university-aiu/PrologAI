PROLOGAI TUTORIAL

Chapter 6: Modules and Loading Files

Author: D. R. Dison, Founder of AIU (Artificial Intelligence University). Creator of PrologAI and Mentova. ORCID: 0009-0001-9246-5758. linkedin.com/in/d-r-dison/


WHY MODULES AND FILES?

When you type facts and rules at the ?- prompt, they disappear when you exit.

Real programs are written in files and organized into modules.

A file is a document saved on your computer that contains Prolog code.

A module is a named group of predicates. Modules let you organize code and control which predicates are available to other parts of the system.

PrologAI is organized into modules called packs. Each pack implements one or more work packages (PRs). When you type use_module(library(assessment)), you are loading the assessment pack's module into your session.


THE LAYER SYSTEM: WHY PACKS COME IN ORDER

Think of PrologAI as a tall building.

Each floor is a layer.

Each pack lives on exactly one floor.

You build from the bottom up.

A pack on floor 50 is allowed to call predicates from any pack on floors 1 through 49.

A pack on floor 50 is NOT allowed to call predicates from a pack on floor 50 or higher.

This is the layering rule, and it applies to every single pack in PrologAI without exception.


Why does this matter to you as a learner?

It explains why this tutorial introduces packs in a specific sequence.

When you read Chapter 100, every predicate called inside that chapter's pack already exists and is tested, because it lives on a lower floor.

You can rely on it.

If someone tried to place a pack on floor 100 that depended on a pack on floor 150, the pack on floor 150 would not exist yet.

The code would break.

This is why you cannot drop a pack in anywhere.

Its floor number must be higher than the floor number of everything it calls.


The two tiers of the layer system:

Tier 1 is the Cognitive Stack (layers 1 through 59).

These are the core platform layers: the memory engine, the shared knowledge store called the Lattice, the actor framework, the reasoning engine, the episodic memory, and so on.

These layers are the foundation of the building itself.

Tier 2 is the Data Layer (layers 60 and above).

These are the perception, analysis, and transformation packs: color, shape, symmetry, fill, compare, grid operations, and everything that processes structured data.

As of this writing, the highest Data Layer pack is gridpath on layer 205.

New packs always receive a layer number higher than all existing packs they depend on.


When you load a pack in your code, Prolog resolves dependencies in order.

If you load a pack at layer 120, all packs it depends on at layers 60 through 119 must already be available.

The layer system makes this predictable and verifiable.


WRITING YOUR FIRST .PL FILE

Create a new text file on your computer. Name it my_knowledge.pl

(The .pl extension tells your computer and SWI-Prolog that this is a Prolog file.)

In that file, type:

    :- module(my_knowledge, [adult/1, greet/1]).

    age(alice, 30).
    age(bob, 17).
    age(carol, 25).

    adult(Name) :-
        age(Name, A),
        A >= 18.

    greet(Name) :-
        format("Hello, ~w!~n", [Name]).

Save the file.

Now look at what is in this file.

LINE 1: :- module(my_knowledge, [adult/1, greet/1]).

This declares a module named my_knowledge. It exports (makes available to other files) the predicates adult/1 and greet/1. The :- at the start means "execute this as a directive when loading."

LINES 3-5: Three facts. Just like the facts you typed at the prompt, but saved permanently.

LINES 7-9: A rule. The rule body is indented for readability. Indentation does not matter to Prolog, but it makes code easier to read.

LINES 11-13: Another rule. format/2 is a built-in predicate that prints formatted output. ~w prints a value. ~n prints a newline (moves to the next line).


LOADING A FILE

In your session, load the file with use_module/1 or consult/1:

At the ?- prompt:

    ?- consult('/path/to/my_knowledge.pl').

Replace /path/to/ with the actual path to your file.

Or, if you are in the same directory as the file:

    ?- consult('my_knowledge.pl').

After loading, you can use all the predicates in the file:

    ?- adult(alice).
    true.

    ?- adult(bob).
    false.

    ?- greet(alice).
    Hello, alice!
    true.


LOADING A MODULE WITH USE_MODULE

If the file declares a module, you should load it with use_module/1 instead of consult/1:

    ?- use_module('my_knowledge.pl').

The difference: use_module/1 loads the module and imports its exported predicates. consult/1 loads the file and makes everything in it available.

For PrologAI packs (like library(assessment)), always use use_module.


DIRECTIVE SYNTAX

Lines starting with :- (colon-dash) in a .pl file are directives. They are executed when the file loads.

Common directives:

    :- module(name, [exported/1, predicates/2]).   % Declare module.
    :- use_module(library(lists)).                  % Load a library.
    :- use_module('my_other_file.pl').              % Load another file.
    :- assertz(some_fact).                          % Add a fact on load.

PrologAI files also use:

    :- initialization(some_goal, now).   % Run this goal immediately on load.
    :- initialization(start_predicate, main).   % Run this when program starts.


COMMENTS

Comments in Prolog files start with a percent sign %. Everything after % on the same line is ignored.

    % This is a comment.
    bird(tweety).   % So is this.

For multi-line comments, use /* and */:

    /* This is a
       multi-line comment. */

IMPORTANT FOR PROLOGAI: Every line of code in PrologAI .pl files must have an ERC (English Readable Code) comment immediately above it. This is the platform's documented convention. Each comment explains what the following line does in plain English.

Here is an example from the PrologAI style:

    % Check if the entity is a bird.
    bird(tweety).
    % Check if the entity can fly.
    can_fly(tweety).
    % Define the rule: a flying bird is something that is a bird and can fly.
    flying_bird(X) :- bird(X), can_fly(X).


THE PROLOGAI PACKS

PrologAI organizes its capabilities into packs. Each pack is a directory in the packs/ folder of the PrologAI repository. Each pack contains a prolog/ subdirectory with the actual code.

To use a pack's predicates:

1. Register its library path:

    ?- assertz(user:file_search_path(library, '/path/to/PrologAI/packs/assessment/prolog')).

2. Load its module:

    ?- use_module(library(assessment), [assess_piaget/3, assess_all/2]).

The Mentova console REPL (bin/mentova) does both steps automatically for the most commonly used packs.


KEY PROLOGAI PACKS (PR 1 THROUGH PR 51)

Here is a summary of the major packs, in dependency order.

PR 1 - kernel: The PrologAI kernel defines the foundational types, the constitutional gate, and the base reasoning patterns.

PR 2 - types: The type system for PrologAI terms.

PR 3 - lattice: The Lattice (the knowledge store). Chapter 7 covers the Lattice in depth.

PR 4 - node_facts: The predicates for storing and retrieving knowledge in the Lattice (anchor_node/4, live_node_facts/2, traverse_nexus/4).

PR 5 - scopes: The nine-zone compartmentalization system that controls what the mind can see and modify.

PR 6 - actors: The basic actor (individual reasoning unit).

PR 7 - pubsub: Publish-subscribe messaging between actors.

PR 8 - perception: The perception system (how the mind receives sensory input).

PR 9 - sentinels: Automatic watchers that fire when specific conditions occur in the Lattice.

PR 10 - kernel continuations: Advanced kernel features.

PR 11 - sona: The episodic memory (Sparse Online Nexus Archive).

PR 12 - assessment: The developmental assessment framework (Piagetian, Bayley, CHC, consciousness indicators).

PR 13 - refinement: The testing and improvement harness.

PR 14 - mcp_gateway: The Model Context Protocol gateway (for connecting to AI tool ecosystems).

PR 15 through PR 22 - reasoning modules: Defeasible, probabilistic, Bayesian, causal, analogical, and other reasoning types.

PR 23 - curiosity: The curiosity and self-directed learning system.

PR 24 - somatic markers: The emotion-like influence system.

PR 25 - daydream: Steered imagination and simulation.

PR 26 - appraisal: Emotional appraisal and coping.

PR 27 - motivation: The motivational drive system.

PR 28 - embedding: Vector embedding integration.

PR 29 - vector_backend: The vector search backend.

PR 30 through PR 42 - higher reasoning: Abduction, induction, frames, analogical reasoning, reflection, synthesis, workspace, attention economy, attention schema, and more.

PR 43 - a2a: The Agent-to-Agent protocol (A2A).

PR 44 - tooling: The tool-use faculty.

PR 45 - language: The language understanding layer.

PR 46 - ros_bridge: The robot body (Robot Operating System version 2 bridge).

PR 47 - acp: The Agent Communication Protocol (ACP) gateway.

PR 48 - anp: The Agent Network Protocol (ANP) gateway with decentralized identity.

PR 49 - lattice_crypto: The cryptographic privacy layer (covered in Chapter 13).

PR 50 - imagination: Imaginative memory - mindscapes (mental canvases), tableaux of bound elements (vantage, figure, motif), and rendered reveries for mental simulation.

PR 51 - awareness: Situational awareness - the ladder of regards (ambient, selfward, otherward, avowed, disavowed), theory-of-mind perspective taking, and self-reconciliation.

PR 53 - ephemera: Ephemeral code synthesis and execution. Write short-lived programs in Prolog, Python, or Bash, execute them with a timeout, capture output, and log execution traces. Covered in Chapter 14.

PR 54 - agency: The Agentic Execution Loop. Formal, observable, bounded goal-pursuit with step budget accounting, goal stack management, loop detection, and escalation to human oversight. Covered in Chapter 15.

PR 55 - refinery: The Evaluator-Optimizer and Metacognitive Quality Layer. Critique outputs against criteria, score them, drive improvement cycles, explore multiple reasoning paths, and learn from past failures. Covered in Chapter 16.

PR 56 - grid: ARC-AGI Grid Perception and Manipulation. Native grid operations for the ARC-AGI benchmark: dimensions, cell access, color analysis, 4-connected object extraction, seven spatial transformations, translation, cropping, overlay, diff, symmetry detection, flood fill, and cell mutation. Covered in Chapter 17.

PR 57 - analogy: ARC-AGI Structural Analogy and Transformation Inference. Infers the transformation rule that explains all training (input, output) pairs and applies it to test inputs. Supports the full D4 dihedral group (8 spatial isometries) and color substitution maps. Covered in Chapter 18.

PR 58 - scene: ARC-AGI Scene Model and Object-Centric Reasoning. Builds a structured object inventory from a grid: background color identification, all foreground objects as obj(Color, Cells) terms, object properties (size, shape, bounding box, centroid), filtering, sorting, counting, and spatial relations (above, below, left-of, right-of, touching, contained-in). Covered in Chapter 19.

PR 59 - quant: Quantitative Reasoning over Object Sets. Counting, grouping by color/size/shape, histogram, frequency analysis, uniformity tests, multiset matching. Covered in Chapter 20.

PR 60 through PR 275 - ARC-AGI-2 Data Layer packs (Layers 60 through 169): More than 200 specialized work packages for grid perception, region analysis, and scene manipulation. These include color analysis, grid math, number sequences, edge detection, topology, projection, gradient, extrema, neighborhood aggregation, median, ranking, cell-set algebra, Boolean/mask operations, geometry, object operations, shape and symmetry operations, spatial arrangement, and more. Each is a self-contained module with a two-letter prefix and 12-14 exported predicates. See the Pack Reference (docs/Pack_Reference.txt) for a complete listing. See Chapters 60 through 141 of this tutorial for individual coverage.

PR 276 - objsym: Object Symmetry Analysis for obj(Color,Cells) Terms (os_*, Layer 170).

PR 277 - objbound: Object Shape Classification and Bounding Box Analysis (ob_*, Layer 171).

PR 280 - objrel: Object Pair Relation Analysis for obj(Color,Cells) Terms (or_*, Layer 172).

PR 281 - objfilter: Object List Filtering and Selection for obj(Color,Cells) Terms (of_*, Layer 173).

PR 283 - canvas: Grid Canvas and Object Rendering for obj(Color,Cells) Terms (cv_*, Layer 174).

PR 284 - objseq: Object Sequence and Progression Analysis for obj(Color,Cells) Terms (oq_*, Layer 175).

PR 285 - objdelta: Object-Pair Change Analysis and Rule Application for obj(Color,Cells) Terms (dp_*, Layer 176).

PR 286 - objcopy: Object Tiling and Multi-Copy Layout for obj(Color,Cells) Terms (tc_*, Layer 177).

PR 287 - objmatch: Object-List Correspondence and Matching (mx_*, Layer 178).

PR 288 - objmerge: Object Merging, Set Operations, and Component Splitting (mg_*, Layer 179).

PR 289 - objattr: Object-List Aggregate Attribute Analysis (oa_*, Layer 180).

PR 290 - objgroup: Object-List Grouping by Shared Attribute (og_*, Layer 181).

PR 291 - scenecmp: Scene-Level Comparison of Two Object Lists (sm_*, Layer 182).

PR 292 - objlocate: Object-List Spatial and Attribute Query Against a Reference Object (lq_*, Layer 183).

PR 293 - scenexf: Scene-Level Uniform Transformation of All Objects (sx_*, Layer 184).

PR 294 - ruleinfer: Scene-Level Transformation Rule Inference from Object-List Pairs (ri_*, Layer 185).

PR 295 - sceneapply: Scene-Level Rule Term Evaluation Engine (sa_*, Layer 186).

PR 296 - condxf: Conditional and Selective Scene Transformation (xc_*, Layer 187).

PR 297 - scenepair: Holistic Before-After Scene Pair Analysis (ps_*, Layer 188).

PR 298 - scenerank: Rule Hypothesis Ranking for Scene Lists (rk_*, Layer 189).

PR 299 - colortable: Color Substitution Table Learning and Application (ct_*, Layer 190).

PR 300 - gridsolve: End-to-End Scene Puzzle Solver (gs_*, Layer 191).

PR 301 - transformgen: Systematic Scene Rule Candidate Generation (tg_*, Layer 192).

PR 302 - multicolor: Multi-Color Scene Analysis (mc_*, Layer 193).

PR 303 - sceneinv: Scene Invariant Detection across Training Pairs (si_*, Layer 194).

PR 304 - seqinfer: Sequential Rule Inference: Multi-Step Scene Transformation Search (sq_*, Layer 195).

PR 305 - gridquery: Grid Query and Manipulation on Raw Grids (gq_*, Layer 196).

PR 306 - gridparse: Grid Parse and Object Extraction (gp_*, Layer 197).

PR 307 - gridtask: Grid Task End-to-End Raw Grid Task Solver (gt_*, Layer 198).

PR 308 - gridnbr: Grid Neighbor Analysis (gn_*, Layer 199): 4- and 8-connected neighbor values, neighbor color counts, border and isolated cell detection, morphological dilation and erosion, and Conway-style cellular automaton steps on raw grids.

PR 309 - griddist: Grid Distance Transform (gd_*, Layer 200): Manhattan and Chebyshev distances, per-cell distance maps to nearest Color cell, BFS flood with obstacles, zone marking, ring extraction, Voronoi assignment, N-step morphological expand and shrink, distance-based recoloring, equidistance detection, and farthest-cell lookup.

PR 310 - gridperiod: Grid Periodic Pattern Detection and Extension (gper_*, Layer 201): row and column period detection, tile extraction, grid tiling and extension to target dimensions, autocorrelation scoring, and wrap-around row and column shifts.

PR 311 - gridscale: Grid Block-Pixel Scaling (gsc_*, Layer 202): integer upsampling, uniform-block downsampling, majority-vote downscaling, scale-factor detection, tile inference, nearest-neighbor resize, stride subsampling, border padding, and factor-aligned cropping.

PR 312 - gridrun: Grid Run-Length Encoding and Stripe Analysis (grl_*, Layer 203): run-length encoding of rows and columns, decode, run counting, row/column uniformity, horizontal/vertical stripe detection, stripe colors, longest-run finding, and alternating-pattern detection.

PR 313 - gridflood: Grid Flood-Fill, Region Analysis, Hole Filling, and Connected Components (gf_*, Layer 204): 4-connected and 8-connected flood-fill, global recolor, region isolation, region cells and size, bounding box, enclosed hole detection and filling, connected-component enumeration, largest-region extraction, connectivity testing, and boundary-stop flood-fill.

PR 314 - gridpath: Grid Pathfinding: BFS Shortest Path, Distance Maps, Flood-N, Wavefront, Line-of-Sight, and Region Path (gpa_*, Layer 205): BFS shortest path between cells, step-count, reachability, all-reachable cells, distance maps, nearest-color search, flood-N expansion, wavefront-at-N, horizontal and vertical segment generation, line-of-sight, interior-cell extraction, and shortest path between any two colored regions.

Each of these is covered further in the Advanced Topics chapters (Chapters 9 through 18).


EXERCISE

1. Create a file called family.pl with facts for at least three people (name, age, city) and one rule (same_city/2 that is true when two people live in the same city).

2. Load the file: ?- consult('family.pl').

3. Query it: find two people who live in the same city.

4. Use findall/3 to collect all people who are adults.

END OF CHAPTER 6
