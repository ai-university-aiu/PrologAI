PROLOGAI TUTORIAL

Chapter 11: Advanced Topics - Actors, SONA, Workspace, and Protocols

Author: D. R. Dison, Founder of AIU (Artificial Intelligence University). Creator and Owner of PrologAI and Mentova. ORCID: 0009-0001-9246-5758. linkedin.com/in/d-r-dison/


INTRODUCTION

This chapter covers the more advanced parts of PrologAI. You do not need to master these topics before using Mentova for everyday reasoning. But if you want to build your own minds, extend Mentova, or integrate PrologAI with other AI systems, this chapter gives you the foundation.


ACTORS (PR 6 THROUGH PR 9)

An actor is an individual reasoning unit that runs concurrently with other actors.

Think of actors as workers in a company. The company (the mind) has many workers, each with a specific job. They communicate through messages and shared resources (the Lattice).

PrologAI actors are implemented in the actors pack (PR 6 through PR 9).

The main predicates:

    create_actor(+Name, +Module, +Init) - create a new actor.
    send_message(+Actor, +Message) - send a message to an actor.
    subscribe(+Channel, +Handler) - subscribe to a broadcast channel.
    publish(+Channel, +Content) - broadcast content on a channel.

Sentinels (PR 9) are a special kind of actor: they watch the Lattice for specific node_fact relation types and fire automatically when the pattern appears. You do not need to call them manually - they trigger on their own.

Example: when anchor_node(percept_signal, ...) is called, any sentinel registered to watch percept_signal will fire automatically. This is how the Level 1 Piagetian milestone (reflex coordination) works: the sentinel represents the automatic reflex arc.


SONA - EPISODIC MEMORY (PR 11)

SONA (Sparse Online Nexus Archive) is the mind's episodic memory.

While the Lattice holds structured knowledge (facts, assessments, plans), SONA holds experiences - sequences of events the mind has witnessed.

Think of SONA as the mind's ability to remember: "I saw X happen, and then Y happened, and then Z."

Key predicates:

    sona_absorb(+Trajectory) - store an experience in episodic memory.
    sona_retrieve(+Pattern, +Limit, -Results) - retrieve matching experiences.

A trajectory is any term representing a sequence of events:

    traj(actions([push_block(left), pull_block(right), stack]))

SONA uses sparse encoding (it does not store every detail, only the significant ones) and online learning (it updates continuously as the mind has experiences).

Example:

    ?- use_module(library(sona), [sona_absorb/1, sona_retrieve/3]).
    ?- sona_absorb(traj(events([saw(tweety, 10_30am), heard(robin, 10_35am)]))).
    ?- sona_retrieve(traj(_), 5, Results).


THE GLOBAL WORKSPACE (PR 18 AND PR 32)

The Global Workspace Theory (GWT) is a model of consciousness from cognitive science. It proposes that a "global workspace" broadcasts information to many specialized processors simultaneously.

PrologAI implements this theory as the workspace cycle (PR 18) and the attention economy (PR 32).

The workspace cycle:
1. Collects content from the Lattice and the actor network.
2. Selects the most salient (important) content.
3. Broadcasts it to all subscribers.
4. Subscribers process the broadcast and update the Lattice.
5. SONA absorbs the broadcast as an episodic memory.

Salience is computed as: novelty (0.4 weight) + goal_relevance (0.3) + affect (0.2) - habituation.

Content that is novel, goal-relevant, and emotionally significant rises to the top.

The attention economy (PR 32) adds a budget constraint: only a certain amount of content can be broadcast per cycle, so content competes for workspace access.

Running the workspace:

    ?- mentova_query(workspace, run_cycle(5), R).

This runs 5 workspace cycles. Each cycle broadcasts the highest-salience content and updates the Lattice.


THE ATTENTION SCHEMA (PR 42)

The attention schema is a model that Mentova maintains about its own attention.

Research on consciousness (particularly the work of Michael Graziano) proposes that consciousness is the brain's model of its own attention process. The attention schema theory suggests that what we call "awareness" is the brain's simplified description of how it is allocating attention.

PrologAI implements an attention schema (PR 42) that:
• Tracks what content is in the workspace.
• Predicts what will be attended to next.
• Maintains a record of habituated (over-repeated) content.
• Reports on attentional dynamics.

    ?- mentova_query(attention_schema, report, R).

Returns the current attention schema state.


THE AGENT COMMUNICATION PROTOCOLS

PrologAI supports four major agent communication protocols that allow Mentova to communicate with other AI systems.

MODEL CONTEXT PROTOCOL (MCP) - PR 14

MCP is a protocol for AI tool use, developed by Anthropic. It allows a language model (or any AI client) to call tools provided by a server.

PrologAI implements an MCP gateway that exposes 14 Mentova predicates as MCP tools. An external client can call these tools using standard MCP HTTP requests.

The MCP gateway runs on port 7474. Authenticated with an API key.

Available MCP tools include: lattice_inscribe, lattice_query, actor_list, assess_all, and more.

To start the MCP gateway:

    ?- use_module(library(mcp_gateway), [pai_mcp_start/1]).
    ?- pai_mcp_start(7474).


AGENT-TO-AGENT PROTOCOL (A2A) - PR 43

A2A is a protocol for agents to delegate tasks to each other.

It uses a task lifecycle: submitted -> working -> completed (or failed).

Each agent has an agent card that describes its capabilities and endpoint.

Mentova implements the full A2A protocol including durable peer mail (messages that persist until the recipient is available).

To start the A2A server:

    ?- use_module(library(a2a), [pai_a2a_start/1]).
    ?- pai_a2a_start(7475).


AGENT COMMUNICATION PROTOCOL (ACP) - PR 47

ACP is IBM's BeeAI REST protocol for agent runs.

POST /runs creates a new run. GET /runs/{id} checks status. DELETE /runs/{id} cancels.

The agent description is served at GET /.well-known/agent.json.

ACP runs are asynchronous: you submit a task and poll for the result.

    ?- use_module(library(acp), [pai_acp_start/1]).
    ?- pai_acp_start(7476).


AGENT NETWORK PROTOCOL (ANP) - PR 48

ANP provides decentralized agent identity using W3C Decentralized Identifiers (DIDs).

Each mind gets a DID (a globally unique identifier that does not depend on any central registry). Mentova's DID has the form did:web:hostname.

All ANP messages are signed with HMAC-SHA256. Incoming messages are verified before any content is processed.

ANP supports meta-protocol negotiation: when two agents meet, they announce which protocols they support, so they can choose the best one.

    ?- use_module(library(anp), [pai_anp_did/1, pai_anp_negotiate/2]).
    ?- pai_anp_did(DID).       % Get Mentova's DID.
    ?- pai_anp_negotiate(peer_endpoint, Protocols).  % Negotiate protocols with a peer.


THE ROBOT BODY (PR 46 - ROS BRIDGE)

PrologAI can inhabit a robot body through the Robot Operating System version 2 (ROS 2) bridge.

The bridge connects the Lattice to ROS 2 topics, services, and actions.

Perception from the robot (camera, LIDAR, touch) flows into the Lattice as percept_signal node_facts.

Actions decided by the mind are sent to ROS 2 action servers, which execute them on the physical or simulated robot.

The constitutional gate screens every physical action before it is sent. High-risk, irreversible actions require human authorization.

The robot body is one of several bodies Mentova can inhabit:
• text_io: the text console (the ?- prompt).
• game bodies: ARC-AGI, Raven's Progressive Matrices, Baba Is You, Pokemon.
• robot_body: the ROS 2 bridge.

Bodies are enrolled at boot time. Additional bodies can be enrolled during a session.


SUMMARY OF ALL 48 WORK PACKAGES

PR 1: kernel - foundational types and patterns.
PR 2: types - the type system.
PR 3: lattice - the knowledge store.
PR 4: node_facts - anchor_node/4, live_node_facts/2, traverse_nexus/4.
PR 5: scopes - the nine-zone compartmentalization.
PR 6: actors - cyclic actor framework.
PR 7: pubsub - publish-subscribe messaging.
PR 8: perception - sensory input processing.
PR 9: sentinels - automatic Lattice watchers.
PR 10: kernel extensions.
PR 11: sona - episodic memory.
PR 12: assessment - Piagetian, Bayley, CHC, consciousness indicators.
PR 13: refinement - testing harness.
PR 14: mcp_gateway - Model Context Protocol server.
PR 15: defeasible - default reasoning with exceptions.
PR 16: probabilistic - probabilistic reasoning.
PR 17: continual refinement - self-improvement harness.
PR 18: workspace - Global Workspace Theory cycle.
PR 19: prediction / active inference.
PR 20: bayesian - Bayesian updating.
PR 21: evidential reasoning and budgets.
PR 22: model composition and planning.
PR 23: curiosity and self-directed learning.
PR 24: somatic markers (emotion-like influence).
PR 25: daydream - steered imagination.
PR 26: appraisal - emotional appraisal and coping.
PR 27: motivation - motivational drive system.
PR 28: embedding - vector embedding integration.
PR 29: vector_backend - vector search.
PR 30: frames - frame-based knowledge.
PR 31: vsa - Vector Symbolic Architecture.
PR 32: attention economy - salience and competition.
PR 33: induction - rule learning.
PR 34: chainer - meta-reasoning chain.
PR 35: abduction - best-explanation reasoning.
PR 36: hyperon bridge (OpenCog Hyperon integration).
PR 37: clause synthesis and specialization.
PR 38: reflection and synthesis.
PR 39: reference frames (spatial and conceptual).
PR 40: spinoff - sub-agent creation.
PR 41: distributional semantics and probabilistic logic.
PR 42: attention schema.
PR 43: a2a - Agent-to-Agent protocol.
PR 44: tooling - tool-use faculty.
PR 45: language understanding.
PR 46: ros_bridge - robot body (ROS 2).
PR 47: acp - Agent Communication Protocol.
PR 48: anp - Agent Network Protocol (decentralized identity).
PR 49: lattice_crypto - Cryptographic Privacy Layer (RSA, ECDH, and PQC hybrid encryption for sensitive Lattice data).
PR 50: imagination - Imaginative memory: mindscapes, tableaux, and rendered reveries.
PR 51: awareness - Situational awareness: the ladder of regards, theory of mind, and self-reconciliation.


IMAGINATIVE MEMORY (PR 50)

Imaginative memory is the mind's inner stage. A mindscape is a compartmentalized canvas held in a non-observed reality, so nothing imagined can ever be mistaken for something perceived. On a mindscape you place a tableau: an arrangement of elements, each a vantage (a camera angle), a figure (an object, possibly grounded to a percept), or a motif (an animation resource). Rendering a mindscape produces a reverie: a deterministic, frame-by-frame animation in which each figure moves by its own velocity, letting the mind rehearse how a scene would unfold before acting.

    ?- use_module(library(imagination)).
    ?- pai_mindscape_new(imagined, M).
    ?- pai_tableau_add(M, [element(vantage, eye_level, []),
                           element(figure, ball, [pos(0,0), vel(2,1)])], T).
    ?- pai_reverie_render(M, 3, R), pai_reverie_frames(R, Frames).

The ball starts at (0,0) and is found at (4,2) by the third frame. Asking for a mindscape in the observed reality simply fails: imagination is sandboxed away from perceived fact.


SITUATIONAL AWARENESS (PR 51)

Awareness is engineered as a ladder of regards (viewpoints): ambient_regard (general awareness, the default), selfward_regard (self-interest), otherward_regard(Agent) (the view of another mind, for theory of mind), avowed_regard (what the mind owns), and disavowed_regard (what the mind disowns). A proposition is held under a regard, so the mind can believe something from one standpoint and deny it from another.

    ?- use_module(library(awareness)).
    ?- pai_regard_hold(selfward_regard, location(ball, box)).
    ?- pai_tom_attribute(alice, not(location(ball, box))).
    ?- pai_tom_divergence(alice, Divs).

The divergence list reports that alice holds a false belief relative to the mind's own view - the computational core of theory of mind. pai_regard_reconcile/1 then integrates the owned and disowned regards into a single coherent self-account.


DREAMING ENGINE (PR 52)

Humans have dreamed for as long as there have been humans. Now dreaming is becoming an activity for machines.

The dreaming pack gives PrologAI an idle-period dream cycle with three phases, each modeled on a stage of biological sleep.

Phase 1 - Slow-Wave (NREM analog). During this phase, the mind replays its most important SONA trajectories with a small stochastic perturbation called replay_noise. This is the symbolic equivalent of generative replay, in which an AI system re-simulates past experiences during offline periods to prevent catastrophic forgetting. After replay, the attention-economy banker cycle runs, promoting important STI (Short-Term Importance) nodes to LTI (Long-Term Importance) storage.

Phase 2 - REM (Rapid Eye Movement analog). During this phase, the mind queries all Lattice nodes with a positive LTI value and randomly pairs them to generate hypothetical new relations, tagged imagined. Nothing from this phase is asserted into the observed Lattice without a separate explicit validation step. This mirrors the way DreamerV3's RSSM imagines novel trajectories inside a learned world model.

Phase 3 - Hypnagogic and Hypnopompic transitions. Every dream event is recorded in the dream journal with a monotonic DreamId and a phase label.

Example:

    ?- use_module(library(dreaming)).
    ?- dreaming_cycle(my_mind, Report).
    ?- dreaming_journal(my_mind, Journal).
    ?- dreaming_counterfactual(my_mind, some_node_id, 3, Alternatives).

The dreaming pack has soft dependencies on SONA, attention, and lattice. If any of these are not loaded, the corresponding phase degrades gracefully.


RUVECTOR HTTP BACKEND (vector_backend PR 2 update)

The vector_backend pack now supports the RuVector backend alongside the built-in pure-Prolog backend.

RuVector (https://github.com/ruvnet/ruvector) is a Rust-language vector database using HNSW (Hierarchical Navigable Small World) indexing with SIMD (Single Instruction, Multiple Data) acceleration and a self-learning GNN (Graph Neural Network) reranking layer.

Start the server (default port 6333):
    bash packs/vector_backend/scripts/ruvector_server.sh

Switch the backend and run the bakeoff:
    ?- vb_set_backend(ruvector).
    ?- run_bakeoff([prolog, ruvector], [100, 1000]).

If the server is not running, all six backend predicates degrade gracefully: insert/delete return silently, search returns an empty list, and create still returns a valid Ref term.

Confirmed bakeoff results (2026-06-24):

    ruvector: 0.2655  (100,000 vectors)
    prolog:   0.0650  (capped at 200 for speed)

RuVector is approximately 4 times faster than the pure-Prolog backend.

The score changes by less than 2% from 100 to 100,000 vectors, confirming that the HNSW index scales sub-linearly.


SHADOW STORE AND REBUILD PREDICATE (vector_backend, shadow store update)

The ruvector-server stores all vector collections in memory only.

If the server process stops for any reason, all vectors in the HNSW index are lost.

The Lattice facts (the actual knowledge) are stored in SWI-Prolog and are not affected.

Only the similarity index is at risk.

To mitigate this, the backend_ruvector module maintains an in-process shadow store.

Every call to vbr_insert/4 records a vbr_shadow/5 dynamic fact:

    vbr_shadow(CollectionName, BaseUrl, IdAtom, FloatVec, MetaAtom)

The shadow facts live in the SWI-Prolog process and survive a RuVector server restart as long as the Prolog session stays alive.

When the server is restarted, call the rebuild predicate:

    ?- vb_rebuild(Ref).

or, if using the backend_ruvector module directly:

    ?- vbr_rebuild(Ref).

The rebuild predicate executes four steps automatically.

Step 1 - It drops the collection from the server (the server may have already forgotten it, so errors are ignored).

Step 2 - It reads the vector dimension from the first shadow entry.

Step 3 - It recreates the collection on the server with the original name and dimension.

Step 4 - It re-inserts every shadow vector by calling the raw HTTP insert predicate directly, without modifying the shadow store.

When rebuild finishes, the HNSW index is fully restored and search results are accurate again.

To check how many vectors are in the shadow store:

    ?- vbr_shadow_count(Ref, Count).

A count of zero means either no vectors have been inserted or vbr_close/1 was called.

After a rebuild, the count remains unchanged because rebuild reads the shadow store but does not add or remove any facts.

Note on vbr_delete/2: The ruvector-server does not expose a per-point DELETE endpoint in the current version.

Calling vbr_delete(Ref, Id) removes the shadow entry for that Id so the vector will not be re-inserted on the next rebuild.

The ghost result (the deleted vector appearing in search results) will persist until the next rebuild cycle.

Note on session restarts: If the SWI-Prolog session itself restarts, the shadow facts are also cleared.

In that case, PrologAI's normal startup procedures (loading the Lattice, generating embeddings, inserting vectors) will rebuild both the HNSW index and the shadow store from scratch.

The vb_rebuild/1 interface is backend-agnostic.

For the pure-Prolog backend, vb_dispatch_rebuild(prolog, _Ref) is a no-op: the pure-Prolog backend stores vectors as dynamic facts that are always in-process and intact.


WHAT COMES NEXT

Chapter 12 covers writing your own PrologAI programs: creating .pl files, loading them into Mentova, and building new reasoning capabilities.

Chapter 13 covers the Cryptographic Privacy Layer: how to encrypt passwords, identification numbers, and other sensitive values stored in the Lattice, using RSA, ECDH, and Post-Quantum Cryptography.

Chapter 14 covers the Ephemera Pack: how PrologAI can compose and execute short-lived programs in Prolog, Python, and Bash to solve sub-problems that are most naturally expressed as code.

Chapter 17 covers the Grid Pack: native ARC-AGI grid perception and manipulation, including object extraction, spatial transformations, symmetry detection, and flood fill.

Chapter 18 covers the Analogy Pack: inferring ARC-AGI transformation rules from training examples using the full D4 dihedral group and color substitution maps.

Chapter 19 covers the Scene Pack: building a structured object inventory from a grid, with object properties and spatial relations.

Chapter 20 covers the Quant Pack: counting, grouping, frequency analysis, and uniformity tests over object sets.

Chapter 21 covers the Pattern Pack: period detection in lists, rows, and columns; tile-based grid construction; integer scaling; horizontal and vertical repetition; mirror expansion; and constructed patterns including checkerboard and stripes.

Chapter 22 covers the Compose Pack: sequential rule pipelines (cp_pipe), conditional branching (cp_branch), repetition combinators (cp_repeat, cp_until, cp_fixed_point), row and column mapping (cp_map_rows, cp_map_cols), pairwise grid combination (cp_zip), and left-fold over lists of grids (cp_fold).

Chapter 23 covers the Motion Pack: gravity predicates (motion_gravity_down, motion_gravity_up, motion_gravity_left, motion_gravity_right), grid translation (motion_shift_grid), directional sliding (motion_slide_col, motion_slide_row), scene-level object translation (motion_obj_translate, motion_scene_translate), scene-to-grid rendering (motion_scene_to_grid), scene gravity via grid round-trip (motion_scene_gravity), and proximity computation (motion_distance, motion_closest_cell).

Chapter 24 covers the Frame Pack: rectangular border detection (frame_border_color, frame_has_border), interior extraction (frame_inner, frame_interior_uniform, frame_interior_color), frame generation (frame_make_framed, frame_add_border), sub-region border testing (frame_region_has_border, frame_region_border_color), bounding box search (frame_bounding_box), and concentric nesting analysis (frame_is_nested, frame_ring_count).

Chapter 25 covers the Path Pack: 4-connected neighbor enumeration (path_neighbors), flood fill (path_flood_fill), connectivity testing (path_connected, path_is_connected), connected component analysis (path_components, path_component_count, path_component_size, path_largest_component), BFS shortest path (path_shortest_path, path_path_length, path_path_exists), reachability (path_reachable), and region bounding box (path_fill_bbox).

Chapter 26 covers the Symmetry Pack: individual D4 symmetry tests (sy_is_hsymmetric, sy_is_vsymmetric, sy_is_rot180, sy_is_rot90, sy_is_diagonal, sy_is_antidiagonal), symmetry group computation (sy_group), orbit generation (sy_rotations, sy_orbit), canonical form (sy_canonical), orbit equivalence (sy_equivalent), and symmetry order (sy_order).

Chapter 27 covers the Color Pack: palette extraction (cl_palette, cl_color_count, cl_is_mono, cl_has_color), color counting and histograms (cl_count, cl_histogram), palette comparison (cl_same_palette), color replacement and remapping (cl_replace, cl_remap, cl_swap), dominant and rarest color detection (cl_dominant, cl_rarest), and color filtering (cl_isolate, cl_remove).

Chapter 28 covers the Shape Pack: normalized shape creation from cell lists (shape_from_cells) and grids (shape_from_grid), shape properties (shape_area, shape_bounding_size, shape_contains_cell, shape_equal), spatial transformations (shape_translate, shape_rotate90, shape_reflect_h, shape_reflect_v), D4 orbit and canonical form (shape_orbit, shape_canonical, shape_equivalent), and grid placement (shape_to_grid).

Chapter 29 covers the Relation Pack: positional ordering (relation_above, relation_below, relation_left_of, relation_right_of), 4-connected adjacency (relation_adjacent), minimum Manhattan distance (relation_distance), bounding box containment (relation_contained_bbox), set relations (relation_overlap, relation_disjoint), row and column alignment (relation_same_row, relation_same_col), integer centroid (relation_centroid), centroid offset (relation_offset), and cardinal direction (relation_direction).

Chapter 30 covers the Sequence Pack: integer range generation (sq_range), first differences (sq_delta), arithmetic sequence detection and extension (sq_is_arithmetic, sq_common_diff, sq_extend_arith), list chunking (sq_chunk), zipping and unzipping pairs (sq_zip, sq_unzip), cumulative prefix sums (sq_cumsum), 0-indexed slicing (sq_slice), one-level flatten (sq_flatten1), matrix transposition (sq_transpose), and period detection (sq_period, sq_is_periodic).

Chapter 31 covers the Crop Pack: bounding box detection (crop_bbox), rectangular subgrid extraction (crop_crop_bbox), content-aware cropping (crop_crop_content), uniform padding (crop_pad), border removal (crop_strip_border), horizontal and vertical splitting (crop_split_h, crop_split_v), row and column band extraction (crop_rows, crop_cols), horizontal and vertical stitching (crop_stitch_h, crop_stitch_v), subgrid embedding (crop_embed), center extraction (crop_center), and quadrant splitting (crop_quadrants).

Chapter 32 covers the Overlay Pack: transparent overlay (overlay_over, overlay_blend), bitwise grid operations (overlay_or, overlay_and, overlay_xor), difference and intersection (overlay_diff, overlay_intersect), masking and inverse masking (overlay_mask, overlay_mask_inv), priority merge across a list of grids (overlay_priority), color replacement (overlay_replace, overlay_fill_bg), and pointwise extrema (overlay_max, overlay_min).

Chapter 33 covers the Measure Pack: area (measure_area), bounding box (measure_bbox, measure_bbox_size), perimeter (measure_perimeter), diameter (measure_diameter), extent fraction (measure_extent), aspect ratio (measure_aspect), row and column spans (measure_row_span, measure_col_span), integer centroid (measure_centroid), Chebyshev radius (measure_radius), interior and border cell counts (measure_interior_count, measure_border_count), and distinct color count (measure_color_count).

Chapter 34 covers the Transform Pack: spatial scaling (tr_scale_up, tr_scale_down), horizontal and vertical tiling (tr_tile_h, tr_tile_v, tr_tile), transposition (tr_transpose), axis reflections (tr_flip_h, tr_flip_v), rotations (tr_rot90, tr_rot180), content shifting with fill (tr_shift), color-map application (tr_apply_map), single-color replacement (tr_replace_color), and mask-based cell selection (tr_mask_grid).

Chapter 35 covers the Select Pack: finding the largest and smallest regions (select_largest, select_smallest), sorting regions by cell count (select_sort_by_area), filtering by exact, minimum, or maximum area (select_filter_area, select_filter_area_min, select_filter_area_max), border-membership testing (select_touches_border), border and interior filtering (select_filter_border, select_filter_interior), directional filtering by row and column bounds (select_above_row, select_below_row, select_left_of_col, select_right_of_col), and unique-area selection (select_unique_area).

Chapter 36 covers the Count Pack: cell count by color (count_color_count), value count in a flat list (count_by_value), sorted color histogram (count_histogram), most and least frequent colors (count_max_color, count_min_color), rows and columns containing a color (count_color_rows, count_color_cols), distinct value counts per row and column (count_row_distinct, count_col_distinct), total cell count (count_grid_total), equal and differing cell counts between two grids (count_equal_cells, count_diff_cells), region color lookup (count_region_color), and per-color region tally (count_regions_per_color).

Chapter 37 covers the Fill Pack: filling a region list with a color (fill_fill_region), filling a bounding box rectangle (fill_fill_bbox), filling entire rows or columns (fill_fill_row, fill_fill_col), filling an explicit cell list (fill_fill_cells), filling the outermost grid ring (fill_fill_border), filling only the boundary cells of a region (fill_outline_region), filling only the interior cells (fill_fill_interior), creating a new uniform-color grid (fill_solid_rect), creating a new checkerboard grid (fill_checkerboard), drawing horizontal and vertical line segments (fill_draw_hline, fill_draw_vline), filling the main diagonal (fill_fill_main_diag), and overlaying a subgrid with transparency (fill_stamp).

Chapter 38 covers the Pattern Pack: detecting the tiling period of a row (pattern_row_period) or column (pattern_col_period), finding the horizontal and vertical tiling period of an entire grid (pattern_grid_period_h, pattern_grid_period_v), extracting the minimal horizontal tile (pattern_tile_unit_h), vertical tile (pattern_tile_unit_v), and 2D tile (pattern_tile_unit), testing whether a grid is an exact tiling of a given tile (pattern_is_tiling), finding all top-left positions where a tile matches (pattern_find_tile), counting those positions (pattern_count_tile), extracting an H x W subgrid at a specified position (pattern_extract_tile), counting distinct rows and columns (pattern_unique_rows, pattern_unique_cols), and testing whether all rows are identical (pattern_has_uniform_rows).

Chapter 39 covers the Compare Pack: finding cells where two grids differ (cp_diff_cells), cells where they agree (cp_same_cells), cells where a color was gained (cp_added_color) or lost (cp_removed_color), cells that changed to (cp_changed_to) or from (cp_changed_from) a specific color, a 0/1 difference map (cp_diff_map), an integer-exact similarity score (cp_similarity), set difference of two cell regions (cp_region_diff), set intersection (cp_region_intersect), set union (cp_region_union), order-independent region equality (cp_region_equal), structural grid equality (cp_grids_equal), and a list of Old-New color pairs for all changed cells (cp_color_shift).

Chapter 40 covers the Spatial Pack: cardinal direction between two cells (sp_direction), Manhattan distance (sp_distance_manhattan), Chebyshev distance (sp_distance_chebyshev), 4-connected in-bounds neighbor enumeration (sp_neighbors4), 8-connected in-bounds neighbor enumeration (sp_neighbors8), 4-connected adjacency test (sp_adjacent4), 8-connected adjacency test (sp_adjacent8), bounding box containment (sp_bbox_contains), cell membership in a region (sp_in_region), row-band region filtering (sp_row_between), column-band region filtering (sp_col_between), nearest region cell by Manhattan distance (sp_closest), farthest region cell (sp_farthest), and integer centroid of a region (sp_centroid).

Chapter 41 covers the Induction Pack: grid dimensions (induction_grid_dims), sorted distinct colors in input and output grids (induction_input_colors, induction_output_colors), colors gained or lost between grid pairs (induction_new_colors, induction_lost_colors), per-cell change and no-change lists (induction_changed_cells, induction_unchanged_cells), inferred Old-New color substitution map (induction_color_map), consistent color-substitution test (induction_is_recolor), uniform single-color output detection (induction_uniform_output, induction_output_color), dimension ratio of an output relative to its input (induction_size_ratio), integer uniform-scale test (induction_is_scale), and integer scale factor extraction (induction_scale_factor).

Chapter 42 covers the Gravity Pack: column value extraction (gv_col_values) and replacement (gv_set_col), compacting all non-background cells to the bottom of each column (gv_compact_col, gravity_fall_down), to the top (gravity_fall_up), to the left of each row (gv_compact_row, gravity_fall_left), to the right (gravity_fall_right), settling a specific color to the bottom of each column past background while other non-background cells act as obstacles (gv_settle_color, gv_stack_down), floating a specific color to the top (gv_float_color, gv_stack_up), and applying a custom column or row transform (gv_apply_col, gv_apply_row).

Chapter 43 covers the Noise Pack: binary mask application with a fill color (noise_mask_apply), mask inversion (noise_mask_invert), pointwise AND and OR of two masks (noise_mask_and, noise_mask_or), building a binary mask from a color criterion (noise_mask_from_color), converting a mask to a region list (noise_mask_to_region) and back (noise_region_to_mask), finding cells that differ from the dominant color (noise_noise_cells), denoising a grid by replacing outliers with the majority color (noise_denoise), identifying the most frequent color (noise_majority_color), counting occurrences of a color (noise_color_count), finding cells belonging to rare colors (noise_sparse_cells), finding cells belonging to frequent colors (noise_dense_cells), and isolating one color by zeroing out all others (noise_isolate_color).

Chapter 44 covers the Generate Pack: uniform color fill (ge_uniform), horizontal and vertical gradients (ge_gradient_h, ge_gradient_v), checkerboard alternating two colors (ge_checkerboard), horizontal and vertical color stripes (ge_stripes_h, ge_stripes_v), bordered rectangle and frame patterns (ge_border_rect, ge_frame), main diagonal and anti-diagonal patterns (ge_diagonal, ge_antidiagonal), identity matrix pattern (ge_identity_grid), cross through the center (ge_cross), building a grid from a list of cell-color pairs (ge_from_map), and tiling a smaller pattern to fill a larger grid (ge_repeat_pattern).

Chapter 45 covers the Lookup Pack: association list key lookup (lk_get), adding or replacing a key-value pair (lk_put), extracting all keys (lk_keys) or values (lk_values), key membership test (lk_has_key), removing a key-value pair (lk_delete), transforming all values with a predicate (lk_map_values), building a map from pairs (lk_from_pairs), grid row access (lk_grid_row), grid column access (lk_grid_col), grid cell access (lk_grid_cell), building a Color-to-positions index map (lk_color_positions), building a position-to-Color index map (lk_position_color), and inverting an association list (lk_invert).

Chapter 46 covers the Connect Pack: 4-connected flood fill from a seed cell (connect_flood4), 8-connected flood fill (connect_flood8), all 4-connected components of a color (connect_components4), all 8-connected components (connect_components8), component count in 4-connected (connect_count4) and 8-connected (connect_count8) mode, sorted component size lists (connect_sizes4, connect_sizes8), largest component by size (connect_largest4, connect_largest8), smallest component by size (connect_smallest4), cells on the boundary of a region (connect_border_cells), cells in the interior of a region (connect_interior_cells), and background cells enclosed inside a closed shape (connect_enclosed).

Chapter 47 covers the Morph Pack: 4-connected and 8-connected dilation (mo_dilate4, mo_dilate8), 4-connected and 8-connected erosion (mo_erode4, mo_erode8), repeated dilation and erosion (mo_dilate4_n, mo_erode4_n), morphological open to remove small protrusions (mo_open4), morphological close to fill small gaps (mo_close4), inner boundary cells (mo_boundary4, mo_boundary8), outer one-cell ring of background cells (mo_ring4), filling enclosed background holes with color (mo_fill_holes4), adding a one-cell border around the grid (mo_pad), and removing the outermost ring (mo_unpad).


Chapter 48 covers the Rewrite Pack: applying a color substitution map to all grid cells (rewrite_map_color), replacing one color with another (rewrite_replace_color), swapping two colors simultaneously (rewrite_swap_colors), painting a region of r(R,C) cells to a target color (rewrite_set_region), applying a binary mask to select which cells receive a fill color (rewrite_mask_apply), compositing two grids by letting non-background cells of Grid2 overwrite Grid1 (rewrite_overlay), stamping a smaller patch into a larger grid at an offset (rewrite_stamp), applying a list of r(R,C)-Color cell edits (rewrite_diff_apply), renumbering distinct non-background colors to 1,2,... in row-major first-appearance order (rewrite_normalize), complementing each color value with respect to a maximum (rewrite_invert_colors), replacing one background color with another (rewrite_remap_bg), painting the outermost ring of cells to a color (rewrite_set_border), filling an axis-aligned rectangle with a color (rewrite_fill_rect), and conditionally recoloring each cell based on a 3-argument goal predicate (rewrite_conditional).


Chapter 49 covers the Run Pack: run-length encoding a flat list to Value-Count pairs (rn_encode), decoding Value-Count pairs back to a flat list (rn_decode), encoding a single grid row (rn_row_encode) or column (rn_col_encode), encoding all rows (rn_grid_rows) or all columns (rn_grid_cols) of a grid, computing the total element count of a run list (rn_length), looking up the value at a 0-indexed position (rn_at), finding the longest run of a given value (rn_max_run), counting distinct runs of a given value (rn_count_runs), testing whether all runs share the same value (rn_uniform), removing leading and trailing occurrences of a background value (rn_trim), repeating an encoded sequence N times with boundary run merging (rn_repeat), and enumerating the 0-indexed positions of all cells with a given value (rn_positions).


Chapter 50 covers the Arith Pack: cell-wise addition of two grids (arith_cell_add), cell-wise subtraction (arith_cell_sub), cell-wise multiplication (arith_cell_mul), cell-wise modulo by a scalar (arith_cell_mod), adding a scalar to every cell (arith_scalar_add), multiplying every cell by a scalar (arith_scalar_mul), sum of all cells in one row (arith_row_sum), sum of all cells in one column (arith_col_sum), list of row sums (arith_row_sums), list of column sums (arith_col_sums), grid-wide maximum cell value (arith_cell_max), grid-wide minimum cell value (arith_cell_min), clamping every cell to a range (arith_cell_clamp), and cell-wise absolute difference of two grids (arith_cell_abs_diff).


Chapter 51 covers the Obj Pack: constructing an object term from a color and cell list (obj_from_cells), extracting the color (obj_color), extracting the sorted cell list (obj_cells), measuring the number of cells (obj_size), computing the axis-aligned bounding box (obj_bbox), computing the integer centroid (obj_center), computing the translation-independent normalized shape (obj_shape), extracting all 4-connected objects of one color (obj_inventory), extracting all objects of all non-zero colors (obj_all), counting objects of a color (obj_count), finding the largest object (obj_largest), finding the smallest object (obj_smallest), looking up which object contains a given cell (obj_at_cell), and sorting objects by size (obj_sort_size).


Chapter 52 covers the Pipeline Pack: registering a named step handler (pipeline_register), querying a registered handler (pipeline_registered), removing a handler (pipeline_unregister), applying one step with an optional local registry (pipeline_step), threading an input through a sequence of named steps (pipeline_run), mapping a 2-argument goal over a list (pipeline_map), filtering a list to elements that satisfy a 1-argument goal (pipeline_filter), folding a 3-argument goal over a list with an accumulator (pipeline_fold), zipping two lists into a list of pairs (pipeline_zip), unzipping a list of pairs into two lists (pipeline_unzip), taking the first N elements (pipeline_take), dropping the first N elements (pipeline_drop), and partitioning a list into satisfied and rejected elements (pipeline_partition).


Chapter 53 covers the Context Pack: adding or replacing a key-value binding (context_put), retrieving a value by key (context_get), testing key presence (context_has), removing a key (context_delete), extracting all keys (context_keys) or values (context_values), building a map from a pair list (context_from_pairs), converting to a pair list (context_to_pairs), merging two maps with Map2 overriding Map1 (context_merge), dispatching a goal by key with a default fallback (context_dispatch), selecting the value for the first present key in a priority list (context_select), transforming all values via a goal (context_map_values), filtering entries by a key predicate (context_filter_keys), and counting entries (context_size).


Chapter 54 covers the Score Pack: testing structural equality of two grids (sc_exact), counting cells where two grids agree (sc_cell_match), counting the total number of cells in a grid (sc_cell_total), computing pixel accuracy as a float in [0.0, 1.0] (sc_accuracy), computing per-color recall (sc_color_recall), per-color precision (sc_color_precision), and per-color F1 score (sc_color_f1), applying a rule to a single training pair and measuring accuracy (sc_pair_score), computing mean accuracy over a list of training pairs (sc_pairs_score), testing whether a rule solves one training pair exactly (sc_perfect), testing whether a rule solves all training pairs exactly (sc_pairs_perfect), ranking candidate grids by accuracy descending (sc_rank), picking the best candidate (sc_best), and filtering candidates by a minimum accuracy threshold (sc_threshold).


Chapter 55 covers the Induct Pack: computing the color-delta between two grids as a list of changed-cell triples (induct_delta), testing identity (induct_constant), inferring a color substitution map from one training pair (induct_color_map), intersecting color maps across all training pairs for consistency (induct_color_map_pairs), computing the row and column size change for one pair (induct_size_change), verifying consistent size change across all pairs (induct_size_change_pairs), building the union color palette for one pair (induct_color_palette), separating input and output color sets across all pairs (induct_palette_pairs), listing invariant cells (induct_invariant_cells), listing changed cells (induct_changed_cells), verifying a consistent delta across all pairs (induct_consistent_delta), finding the background color by frequency (induct_bg_color), verifying a consistent background across all pairs (induct_bg_color_pairs), and intersecting two color maps (induct_common_keys).


Chapter 56 covers the Hyp Pack: applying a color substitution map to every cell with identity fallback for unmapped colors (hyp_color_sub), the identity no-op hypothesis (hyp_identity), a partial-application alias for color substitution (hyp_from_map), testing a hypothesis on one training pair and returning pixel accuracy (hyp_test), testing on all pairs and returning mean accuracy (hyp_test_all), exact-match test for one pair (hyp_verify), exact-match test for all pairs (hyp_verify_all), selecting the best hypothesis from a list (hyp_select), ranking hypotheses by mean accuracy descending (hyp_rank), an alias for color substitution (hyp_apply_map), sequential two-map color substitution (hyp_compose), inverting a color substitution map (hyp_invert_map), color lookup with identity fallback (hyp_map_lookup), and describing a hypothesis as a human-readable atom (hyp_describe).


Chapter 57 covers the Sym Pack: reflecting a grid left-right (sy_reflect_h), top-bottom (sy_reflect_v), and across the main diagonal (sy_transpose), rotating 90 degrees clockwise (sy_rotate90), 180 degrees (sy_rotate180), and 270 degrees clockwise (sy_rotate270), testing for horizontal symmetry (sy_has_h_symm), vertical symmetry (sy_has_v_symm), 2-fold rotational symmetry (sy_has_rot2_symm), and 4-fold rotational symmetry (sy_has_rot4_symm), listing all symmetry names present in a grid (sy_symmetries), making a grid horizontally symmetric by mirroring the left half (sy_make_h_symm), making it vertically symmetric by mirroring the top half (sy_make_v_symm), and computing the full D4 orbit of all distinct spatial transforms (sy_d4_orbit).


Chapter 58 covers the Seek Pack: finding all (row,col) positions of a value in a grid (seek_positions), finding row indices containing a value (seek_rows_with), finding column indices containing a value (seek_cols_with), listing all border cell positions (seek_border_cells), listing all interior cell positions (seek_interior_cells), testing whether a sub-grid matches exactly at a position (seek_fits), nondeterministically enumerating positions where a sub-grid fits (seek_find_sub), collecting all fitting positions as a list (seek_all_subs), counting the number of fitting positions (seek_count_sub), counting how many cells match when a sub-grid is placed at a position (seek_match_count), finding the position that maximises the match count (seek_best_fit), discovering the D4 transform name that maps one grid to another (seek_find_d4), upscaling a grid so each cell becomes a Factor-by-Factor block (seek_upscale), and finding the integer scale factor relating two grids (seek_find_scale).

Chapter 59 covers the Remap Pack: replacing every occurrence of one value with another (remap_replace), swapping two values throughout a grid (remap_swap), applying a color substitution map to all cells with identity fallback for unmapped values (remap_apply_map), applying a map only to cells matching a specific value (remap_apply_map_to), inverting a map by swapping keys and values (remap_invert_map), composing two maps by chaining their lookups (remap_compose_maps), normalizing all distinct values to consecutive integers starting from 1 (remap_normalize), shifting all cell values by an integer offset (remap_shift), clamping all cell values to a range (remap_clamp), recoloring cells that satisfy a predicate goal (remap_conditional), binarizing a grid to foreground/background (remap_binarize), remapping the background color to a new value (remap_remap_bg), extracting the sorted palette of distinct values (remap_palette), and reindexing a grid using a supplied palette (remap_reindex).

Chapter 60 covers the Logic Pack: boolean intersection of two grids keeping values only where both are non-background (lg_and), boolean union returning first grid's value on ties (lg_or), exclusive-or returning a value only where exactly one grid is non-background (lg_xor), inverting foreground and background (lg_not), set-difference keeping only cells present in the first grid but absent in the second (lg_diff), overlaying one grid onto another with background treated as transparent (lg_overlay), applying a mask to keep grid values only where the mask is non-background (lg_mask_apply), creating a binary presence mask from a grid (lg_mask_from), computing per-row presence flags indicating whether any cell is non-background (lg_any_row), computing per-column presence flags (lg_any_col), computing per-row fullness flags indicating whether all cells are non-background (lg_all_row), per-column fullness flags (lg_all_col), cell-wise equality producing 1 where grids agree and 0 where they differ (lg_eq), and cell-wise inequality (lg_neq).

Chapter 61 covers the Window Pack: listing R2-C2-Val triples for all in-bounds 4-connected neighbors of a cell (window_neighbors4), listing 8-connected neighbor triples (window_neighbors8), counting 4-connected neighbors equal to a target value (window_count4), counting 8-connected neighbors equal to a value (window_count8), extracting an H x W sub-grid starting at a given position (window_extract), enumerating all valid H x W sliding windows as R0-C0-Sub triples (window_slide), padding all sides of a grid with N layers of a padding value (window_pad), testing whether a cell is a local maximum among its 4-connected neighbors (window_local_max4), testing whether a cell is a local minimum (window_local_min4), finding cells adjacent to a target value but not equal to it (window_halo4), computing integer convolution of a grid with a kernel (window_convolve), finding the floor-center coordinates of a grid (window_center), computing the Manhattan distance between two grid positions (window_manhattan), and listing in-bounds cells at exactly Manhattan distance D from a given cell (window_cells_at_dist).

Chapter 62 covers the Sort Pack: computing the integer sum of each row (sort_row_sums), computing the integer sum of each column (sort_col_sums), counting a specific value per row (sort_row_count), counting a specific value per column (sort_col_count), sorting rows ascending by count of a value (sort_sort_rows_asc), sorting rows descending by count of a value (sort_sort_rows_desc), sorting columns ascending by count of a value (sort_sort_cols_asc), sorting columns descending by count of a value (sort_sort_cols_desc), finding the row index with the highest count of a value (sort_max_row), finding the row index with the lowest count (sort_min_row), finding the column index with the highest count (sort_max_col), finding the column index with the lowest count (sort_min_col), listing all cell values sorted ascending with duplicates preserved (sort_sorted_vals), and finding the 1-based rank of a cell's value among distinct grid values (sort_cell_rank).

Chapter 63 covers the Tile Pack: repeating a tile N times horizontally (tile_tile_h), repeating a tile N times vertically (tile_tile_v), tiling a motif into an NR-by-NC arrangement of copies (tile_tile), splitting a grid into horizontal bands of TH rows each (tile_split_rows), splitting a grid into vertical stripes of TW columns each (tile_split_cols), splitting a grid into a list-of-tile-rows of TH x TW tiles (tile_split), reassembling split tiles back into one grid (tile_flatten_tiles), overlaying a motif at position (R, C) in a base grid (tile_stamp), stamping a motif at every position in a list (tile_stamp_all), extracting the tile at tile-position (TR, TC) (tile_extract_tile), checking if a grid is an exact tiling of one motif (tile_is_tiling), finding the smallest horizontal period in columns (tile_find_period_h), finding the smallest vertical period in rows (tile_find_period_v), and generating an H x W checkerboard with two values (tile_checkerboard).

Chapter 64 covers the Trace Pack: finding maximal contiguous non-background runs in a row (tr_runs_row), per-row run lists for a grid (tr_spans_h), per-column run lists (tr_spans_v), casting a horizontal ray to find the first non-background cell in a direction (tr_ray_h), casting a vertical ray (tr_ray_v), listing all R-C pairs in a horizontal line inclusive of endpoints (tr_line_h), listing all R-C pairs in a vertical line (tr_line_v), extracting grid values along a list of positions (tr_path_vals), painting a value at each position in a list (tr_draw_path), listing the border cells of a bounding rectangle (tr_bbox_border), non-background cells touching background or on the grid boundary (tr_perimeter), background cells touching non-background cells (tr_outline), all cells on the grid boundary (tr_edge_cells), and computing the floor midpoint of two positions (tr_midpoint).

Chapter 65 covers the Label Pack: assigning unique integer labels to each maximal 4-connected component (label_label), returning component cell lists (label_components), counting components (label_count), returning the cell count of a specific label (label_size_of), returning all Label-Size pairs (label_sizes_all), listing cells of a specific label (label_cells_of), returning the bounding box corners of a label (label_bbox_of), listing foreground labels adjacent to a given label (label_neighbors_of), replacing all cells of a label with a value (label_fill_label), keeping only the largest component (label_keep_largest), removing components below a size threshold (label_remove_small), coloring each label from a cycling palette (label_color_labels), merging two labels into one (label_merge_two), and extracting one component from the original grid (label_select_label).

Chapter 66 covers the Morph Pack: expanding all non-background regions by one 4-connected step copying the neighbor's color (morph_dilate), shrinking all non-background regions by one step (morph_erode), applying dilation N times (morph_dilate_n), applying erosion N times (morph_erode_n), morphological opening as erode then dilate (morph_open), morphological closing as dilate then erode (morph_close), morphological smoothing as open then close (morph_smooth), extracting only perimeter non-background cells (morph_boundary), extracting only interior non-background cells (morph_interior), dilating with a fixed fill value instead of the neighbor's color (morph_dilate_val), BFS flooding from seed cells into adjacent background territory (morph_grow_from), computing the L1 Manhattan distance from each non-background cell to the nearest background cell (morph_dist_to_bg), extracting the cells added at exactly the Nth dilation step as a ring (morph_ring), and filling all enclosed background regions not reachable from the grid border (morph_fill_holes).

Chapter 67 covers the Walk Pack: returning all R-C positions in row-major order (walk_row_scan), column-major order (walk_col_scan), zigzag boustrophedon order alternating left-right on even rows and right-left on odd rows (walk_zigzag_scan), grouped by main diagonal D = C minus R (walk_diag_scan), grouped by anti-diagonal D = R + C (walk_antidiag_scan), in clockwise inward spiral order peeling rings from outside to center (walk_spiral_in), and in clockwise border order for the outermost ring only (walk_border_walk); extracting values on a specific main diagonal (walk_diag_extract) or anti-diagonal (walk_antidiag_extract); computing the main diagonal index D = C minus R for a cell (walk_diag_of) or the anti-diagonal index D = R + C (walk_antidiag_of); extracting grid values at a list of R-C positions (walk_cells_to_vals); painting values at a list of R-C positions (walk_vals_to_cells); and returning all non-border R-C positions in row-major order (walk_inner_cells).

Chapter 68 covers the Step Pack: taking one unbounded step in a direction (st_step); taking one bounded step that fails if out of grid (st_step_in); collecting all in-bounds cells in a direction excluding the start (st_ray); collecting cells in a direction stopping before a given value (st_ray_to); collecting all in-bounds cells including the start (st_walk); returning the four cardinal directions (st_dirs4); returning all eight principal directions (st_dirs8); rotating a direction 90 degrees clockwise (st_rotate_cw); rotating 90 degrees counter-clockwise (st_rotate_ccw); reversing a direction (st_opposite); computing the unit step direction between two cells (st_normalize); following a list of direction steps from a start cell (st_path); finding the first cell in a direction with a given value (st_first); and counting the number of steps until the grid boundary (st_to_edge).

Chapter 69 covers the Pivot Pack: computing the integer floor centroid of a cell list (pivot_centroid); converting an absolute cell to a pivot-relative offset (pivot_to_rel); converting a pivot-relative offset back to an absolute cell (pivot_from_rel); rotating one cell 90 degrees clockwise around a pivot (pivot_rotate_cell_cw); rotating a list of cells 90 degrees clockwise (pivot_rotate_cells_cw); rotating 180 degrees (pivot_rotate_cells_180); rotating 90 degrees counter-clockwise (pivot_rotate_cells_ccw); reflecting a list of cells horizontally around the pivot column (pivot_reflect_cells_h); reflecting vertically around the pivot row (pivot_reflect_cells_v); reflecting across the main diagonal through the pivot (pivot_reflect_cells_diag); reflecting across the anti-diagonal through the pivot (pivot_reflect_cells_antidiag); computing the sorted D4 orbit of a single cell around the pivot (pivot_orbit); computing the sorted D4 symmetry closure of a cell list (pivot_sym_closure); and stamping a list of DR-DC offset cells at a pivot position in a grid (pivot_stamp_at).


Chapter 70 covers the Project Pack: propagating each non-BG cell's value downward through BG cells in the same column until hitting another non-BG cell or the boundary (pj_shadow_down); propagating upward (pj_shadow_up); propagating leftward (pj_shadow_left); propagating rightward (pj_shadow_right); dispatching shadow casting by direction atom down, up, left, or right (pj_shadow_dir); returning sorted row indices that contain at least one non-BG cell (pj_nonbg_rows); returning sorted column indices that contain at least one non-BG cell (pj_nonbg_cols); returning a list of non-BG cell counts per row (pj_row_counts); returning a list of non-BG cell counts per column (pj_col_counts); collapsing all rows to one row by taking the first non-BG value per column (pj_collapse_rows); collapsing all columns to one column by taking the first non-BG value per row (pj_collapse_cols); finding the row index of the topmost non-BG cell in a column (pj_col_first); finding the row index of the bottommost non-BG cell in a column (pj_col_last); and finding the column index of the leftmost non-BG cell in a row (pj_row_first).


Chapter 72 covers the Order Pack: computing the integer floor centroid of an obj(Color, Cells) term (od_centroid); sorting objects by centroid row ascending (od_sort_row); sorting by centroid column ascending (od_sort_col); sorting by row then column in reading order (od_reading_order); sorting by color value ascending (od_sort_color); returning the object with the smallest centroid row (od_topmost); returning the object with the largest centroid row (od_bottommost); returning the object with the smallest centroid column (od_leftmost); returning the object with the largest centroid column (od_rightmost); retrieving the Nth object in row-ascending order (od_nth_row); retrieving the Nth object in column-ascending order (od_nth_col); finding the object whose centroid has minimum Manhattan distance to a reference position (od_nearest); finding the object whose centroid has maximum Manhattan distance to a reference position (od_farthest); and finding the 1-based rank of an object in row-ascending order (od_rank_row).


Chapter 71 covers the Diff Pack: finding all changed cells between two grids as diff(R,C,OldValue,NewValue) terms (diff_cell_diff); finding cells that transitioned from background to non-background (diff_added); finding cells that transitioned from non-background to background (diff_removed); finding cells that stayed non-background but changed color (diff_recolored); finding cells whose value did not change (diff_stable); reporting which colors were added to or lost from the palette (diff_palette_change); finding cells that changed in every In-Out pair in a list (diff_common_diffs); finding cells that were stable in every pair (diff_common_stable); finding cells that were added in every pair (diff_always_added); finding cells that were removed in every pair (diff_always_removed); counting the total number of changed cells (diff_total_changes); applying a list of diff terms to a grid (diff_apply_diffs); inverting a list of diffs to reverse the direction of each change (diff_invert_diffs); and filtering a list of diffs by a goal predicate (diff_filter_diffs).


Chapter 73 covers the Assemble Pack: horizontally concatenating a list of same-height grids into one wide grid (assemble_hcat); vertically stacking a list of same-width grids into one tall grid (assemble_vcat); assembling a 2D matrix of grids into one combined grid by joining each row then stacking the strips (assemble_grid_of); reducing each K x K non-overlapping block to a single cell by majority vote (assemble_downscale); surrounding a grid with a W-cell wide frame of a chosen color (assemble_border); embedding a grid at the integer floor center of a larger canvas filled with a background color (assemble_center_in); extracting one of four named quadrants tl, tr, bl, or br using floor division at the midpoint (assemble_quarter); concatenating a grid with its left-right mirror side by side (assemble_flip_h_cat); stacking a grid with its top-bottom mirror (assemble_flip_v_cat); interleaving columns from two same-size grids so columns alternate between them (assemble_zip_h); interleaving rows from two same-size grids (assemble_zip_v); unconditionally pasting a sub-grid into a base grid at a given offset, overwriting regardless of cell value (assemble_paste); replacing cells with a fill value wherever the corresponding mask cell is non-zero (assemble_mask_fill); and cropping or padding a grid to exactly a target number of rows and columns using a background fill (assemble_crop_to).


Chapter 74 covers the Region Pack: checking whether a row or column is entirely equal to a separator value (region_is_sep_row, region_is_sep_col); collecting all separator row or column indices into a sorted list (region_sep_rows, region_sep_cols); computing inclusive R0-R1 or C0-C1 span pairs for the non-separator sections along each axis (region_spans_h, region_spans_v); splitting a grid at its separator rows into a list of sub-grids with separators excluded (region_cut_h); splitting a grid at its separator columns into a list of sub-grids (region_cut_v); assembling a 2D matrix of all sections by cutting in both directions (region_sections); retrieving the N-th horizontal or vertical section by 1-indexed number (region_section_h, region_section_v); counting how many horizontal or vertical sections a grid has (region_count_h, region_count_v); and extracting the sub-grid of the section that contains a given cell coordinate, failing if the coordinate is itself on a separator (region_region).


Chapter 75 covers the Neighbor Pack: returning a list of nb(Row,Col,Val) terms for each valid 4-connected neighbor of a cell, with out-of-bounds directions omitted (neighbor_4neighbors); the same for 8-connected neighbors including diagonals (neighbor_8neighbors); testing whether a non-Bg cell has at least one Bg or out-of-bounds 4-neighbor, making it a boundary cell (neighbor_is_boundary); testing whether a non-Bg cell has all 4-neighbors in-bounds and non-Bg, making it an interior cell (neighbor_is_interior); collecting all boundary cells as sorted R-C pairs (neighbor_boundary_cells); collecting all interior cells as sorted R-C pairs (neighbor_interior_cells); counting 4-neighbors with the same value as the cell (neighbor_count_same); counting 4-neighbors with a different value (neighbor_count_diff); returning the sorted set of distinct values found among 4-neighbors (neighbor_adjacent_colors); finding all cells of a specific Color that are adjacent to a cell of a different color or the grid boundary, forming the outline of the Color region (neighbor_contour); testing whether any cell of ColorA is 4-adjacent to any cell of ColorB anywhere in the grid (neighbor_color_touches); enumerating all such 4-adjacency pairs as (R1-C1)-(R2-C2) terms (neighbor_touching_pairs); replacing the 4-connected region from a seed cell with a fill value to produce a modified grid (neighbor_flood_fill); and expanding a Color region by one cell in all 4-connected directions by setting adjacent Bg cells to Color (neighbor_dilate).


Chapter 76 covers the Permute Pack: reordering all rows of a grid according to an explicit index list so that result row i comes from grid row Perm[i] (permute_permute_rows); reordering all columns by an index list (permute_permute_cols); exchanging exactly two rows (permute_swap_rows); exchanging exactly two columns (permute_swap_cols); cyclically shifting rows so the last N rows move to the front (permute_cycle_rows); cyclically shifting columns so the last N columns move to the left (permute_cycle_cols); discovering the index permutation that maps the rows of one grid to the rows of another (permute_find_row_perm); discovering the index permutation that maps the columns of one grid to the columns of another (permute_find_col_perm); sorting all rows in ascending lexicographic order while preserving duplicates (permute_sort_rows); sorting all columns in ascending lexicographic order while preserving duplicates (permute_sort_cols); inserting a complete row before a given position (permute_insert_row); removing the row at a given position (permute_delete_row); inserting a column of values before a given position in every row (permute_insert_col); and removing the column at a given position from every row (permute_delete_col).


Chapter 77 covers the Gravity Pack: compacting all non-Bg values to the left end of a row while padding right with Bg (gravity_pack_row_left); compacting to the right end (gravity_pack_row_right); compacting to the top of a column list (gravity_pack_col_up); compacting to the bottom of a column list (gravity_pack_col_down); sliding all non-Bg cells left within their row (gravity_fall_left); sliding all non-Bg cells right within their row (gravity_fall_right); making all non-Bg cells fall downward within their column (gravity_fall_down); making all non-Bg cells rise upward within their column (gravity_fall_up); dispatching gravity by direction atom down, up, left, or right (gravity_fall_dir); sliding only a specific Color left while other non-Bg cells act as immovable walls bounding each gravity segment (gravity_fall_color_left); sliding only Color right within each segment (gravity_fall_color_right); dropping only Color cells downward within each column segment (gravity_fall_color_down); raising only Color cells upward within each column segment (gravity_fall_color_up); and dispatching color-specific gravity by direction atom (gravity_fall_color_dir).


Chapter 78 covers the Count Pack (Layer 96): counting occurrences of a value in a flat list (count_by_value); counting a value across all cells of a 2D grid (count_color_count); building a frequency histogram of all cell values as parallel sorted Colors and Counts lists (count_histogram); returning the most frequently occurring value (count_max_color); returning the least frequently occurring value (count_min_color); counting the number of rows containing a given value (count_color_rows); counting the number of columns containing a given value (count_color_cols); counting distinct values in a single row (count_row_distinct); counting distinct values in a single column (count_col_distinct); returning the total number of cells as rows times columns (count_grid_total); counting cells where two grids of identical dimensions agree (count_equal_cells); counting cells where two grids differ (count_diff_cells); looking up the grid value at the first cell of a region expressed as a list of r(R,C) terms (count_region_color); and building a Color-Count tally for a list of regions (count_regions_per_color).


Chapter 79 covers the Stripe Pack (Layer 97): testing whether row R has all cells equal to Color (stripe_uniform_row); testing whether column C has all cells equal to Color (stripe_uniform_col); returning sorted row indices uniformly equal to a specified Color (stripe_uniform_rows); returning sorted column indices uniformly equal to a specified Color (stripe_uniform_cols); returning R-Color pairs for all uniform rows in row-index order (stripe_all_stripe_rows); returning C-Color pairs for all uniform columns in column-index order (stripe_all_stripe_cols); returning sorted row indices that are NOT uniform (stripe_mixed_rows); returning sorted column indices that are NOT uniform (stripe_mixed_cols); filling row R with Color to produce a new grid (stripe_fill_row); filling column C with Color to produce a new grid (stripe_fill_col); filling all row indices in a list with Color (stripe_fill_rows); filling all column indices in a list with Color (stripe_fill_cols); returning r(R,C) terms for the Cartesian product of two index lists (stripe_cross_cells); and filling all cells at a set of row-column intersections with Color (stripe_cross_fill).


Chapter 80 covers the Diagonal Pack (Layer 98): extracting the values on the main diagonal where C equals R (diagonal_main_diag); extracting values on the main anti-diagonal where R plus C equals NC minus 1 through the top-right corner (diagonal_anti_diag); extracting values on the N-th diagonal where C minus R equals N (diagonal_nth_diag); extracting values on the N-th anti-diagonal where R plus C equals N (diagonal_nth_anti_diag); returning all diagonals as a list of value lists sorted by offset from -(NR-1) to NC-1 (diagonal_all_diags); returning all anti-diagonals as a list sorted by sum from 0 to NR+NC-2 (diagonal_all_anti_diags); filling the main diagonal with Color (diagonal_fill_main); filling the main anti-diagonal with Color (diagonal_fill_anti); filling the N-th diagonal with Color (diagonal_fill_nth_diag); filling the N-th anti-diagonal with Color (diagonal_fill_nth_anti_diag); computing the diagonal index N equals C minus R for cell (R,C) (diagonal_cell_diag); computing the anti-diagonal index N equals R plus C for cell (R,C) (diagonal_cell_anti_diag); testing whether the N-th diagonal has all cells equal to Color (diagonal_uniform_diag); and testing whether the N-th anti-diagonal has all cells equal to Color (diagonal_uniform_anti_diag).


Chapter 81 covers the Recur Pack (Layer 99): generating an N-term arithmetic sequence from a start value and step (recur_arith); extracting the common difference from an existing arithmetic sequence (recur_arith_step); testing whether a sequence is arithmetic and returning its step (recur_is_arith); generating a sequence by repeating a unit N times end-to-end (recur_repeat); finding the minimal repeating unit of a sequence whose length must evenly divide the sequence length (recur_period); testing whether a sequence is an exact integer-multiple repetition of some unit (recur_is_periodic); computing the next N terms of an arithmetic sequence by continuing from its last element (recur_next_arith); computing the next N terms of a periodic sequence by cycling from the current offset (recur_next_repeat); extending an arithmetic sequence by N terms and returning the extended list and detected step (recur_extend_arith); extending a periodic sequence by N terms and returning the extended list and minimal period unit (recur_extend_repeat); returning the N-th element one-based of the infinite cyclic repetition of a unit using modular arithmetic (recur_cycle_nth); applying a three-argument goal pair-wise over two same-length lists to produce a result list (recur_zip_with); computing consecutive element-wise differences of a numeric list where the result has one fewer element (recur_diff_list); and testing whether all consecutive differences of a numeric list are equal and returning that common value (recur_const_diffs).


Chapter 84 covers the Cluster Pack (Layer 102): computing the Chebyshev L-infinity distance max of absolute row and column differences (cl_chebyshev); computing the Manhattan L-1 distance sum of absolute differences (cl_manhattan); computing the squared Euclidean distance sum of squared differences for integer comparison without square roots (cl_euclidean_sq); filtering a cell list to those within a Chebyshev radius of a query point (cl_within); finding the cell in a set with the minimum Chebyshev distance to a point with tie-breaking by first occurrence (cl_nearest); finding the cell with the maximum Chebyshev distance (cl_farthest); computing the integer centroid of a cell set by rounding the mean row and column indices (cl_center); computing the maximum pairwise Chebyshev distance within a set (cl_diameter); computing the bounding box row and column spans (cl_spread); grouping cells by their grid color into parallel sorted Colors and Groups lists (cl_group_by_color); sorting cells by ascending Chebyshev distance to a point using stable keysort (cl_sort_by_dist); finding the pair of cells with minimum pairwise Chebyshev distance (cl_nearest_pair); finding the pair with maximum pairwise distance (cl_farthest_pair); and filtering cells whose row and column indices both fall within given inclusive bounds (cl_cells_in_band).


Chapter 83 covers the Rect Pack (Layer 101): enumerating all cells of a solid axis-aligned rectangle in row-major order (rect_cells); listing only the frame cells of a rectangle in sorted row-major order (rect_border); listing interior cells strictly inside the frame or returning empty for thin rectangles (rect_interior); filling the entire solid rectangle with a color (rect_draw); drawing only the frame with a color (rect_draw_border); filling only the interior with a color (rect_draw_interior); computing the total number of cells as row count times column count (rect_area); testing whether a cell lies within the rectangle on all four inclusive sides (rect_contains); returning the four corner cells in top-left top-right bottom-left bottom-right order (rect_corners); computing the bounding box of any non-empty list of R-C cells as minimum and maximum row and column values (rect_bbox); testing whether every cell in the rectangle has the same color and returning that color (rect_is_solid); testing whether every border cell has the same color (rect_is_frame); testing whether two axis-aligned rectangles share at least one cell using row and column interval arithmetic (rect_overlap); and scaling a rectangle by an integer factor from its top-left corner by multiplying the row and column spans (rect_scale).


Chapter 82 covers the Line Pack (Layer 100): listing all R-C cells on a horizontal segment at a given row between two column bounds in ascending column order (line_hline); listing all R-C cells on a vertical segment at a given column between two row bounds in ascending row order (line_vline); listing cells on a 45-degree diagonal segment where the row span must equal the column span in walk order from start to end (line_diag_seg); drawing a horizontal line onto a grid by setting every cell along the segment to a color (line_draw_h); drawing a vertical line onto a grid (line_draw_v); drawing a 45-degree diagonal line onto a grid (line_draw_diag); testing whether two cells share a row where R1 equals R2 (line_same_row); testing whether two cells share a column where C1 equals C2 (line_same_col); testing whether two cells lie on the same diagonal where C minus R is constant (line_same_diag); testing whether two cells lie on the same anti-diagonal where R plus C is constant (line_same_anti); testing whether three cells are collinear meaning they all share a row, column, diagonal, or anti-diagonal (line_collinear); extracting the first and last R-C pairs from a non-empty cell list (line_endpoints); returning the cells strictly between two aligned points on the same row, column, diagonal, or anti-diagonal excluding both endpoints (line_gap); and classifying a segment as point, horizontal, vertical, diagonal, anti_diagonal, or other (line_line_type).


Chapter 85 covers the Ray Pack (Layer 103): testing whether a cell coordinate pair is within the 0-based grid bounds (ray_in_bounds); enumerating all in-bounds cells starting one step beyond an origin in a direction (DR,DC) until leaving the grid (ray_cells_in_dir); finding the first cell in a direction whose value differs from a background color (ray_first_hit); collecting the maximal background-colored prefix of a ray before the first obstacle (ray_cast_clear); computing the step count from an origin to the first non-background cell (ray_distance_to_hit); computing the step count from an origin to the grid boundary in a direction (ray_distance_to_edge); testing that every cell in a direction is the background color (ray_is_clear); computing the coordinates exactly N steps from an origin in a direction and failing if out of bounds (ray_project); mapping named direction atoms up down left right ul ur dl dr to unit row-column step pairs (ray_dir_to_delta); reflecting a direction across a horizontal axis by negating the row component (ray_reflect_h); reflecting a direction across a vertical axis by negating the column component (ray_reflect_v); testing that all interior cells on a same-row same-column or same-diagonal segment between two cells are background (ray_los); enumerating the interior cells strictly between two aligned cells on the same row column main diagonal or anti-diagonal (ray_los_cells); and casting rays in all 4 compass directions simultaneously returning a list of Dir-Result pairs where each result is the first obstacle cell or the atom none if the direction is clear to the boundary (ray_cast_all_4).


Chapter 86 covers the Vec2 Pack (Layer 104): computing the displacement vector from one cell to another as DR=R2-R1 and DC=C2-C1 (vec2_offset); applying a displacement vector to translate a point (vec2_add); scaling a vector by an integer factor with negative factors reversing direction (vec2_scale); negating a vector to point in the opposite direction (vec2_neg); computing the squared Euclidean length without floating-point arithmetic (vec2_len_sq); computing the L1 Manhattan magnitude as the sum of absolute components (vec2_manhattan); computing the L-infinity Chebyshev magnitude as the maximum absolute component (vec2_chebyshev); rotating a vector 90 degrees clockwise in row-column coordinates where RDR=DC and RDC=-DR (vec2_rot90_cw); rotating 90 degrees counter-clockwise where RDR=-DC and RDC=DR (vec2_rot90_ccw); rotating 180 degrees equivalent to negation (vec2_rot180); computing the dot product DR1*DR2+DC1*DC2 which is positive for same-direction vectors and zero for orthogonal ones (vec2_dot); computing the z-component of the 2D cross product DR1*DC2-DC1*DR2 which is positive when V2 is counter-clockwise from V1 (vec2_cross); testing whether two vectors are parallel by asserting that their cross product is zero (vec2_parallel); and translating a list of R-C cell coordinates by a displacement vector preserving order (vec2_translate_region).


Chapter 87 covers the Spread Pack (Layer 105): computing a BFS distance map from a single source through background cells returning Cell-D pairs for every reachable cell (spread_dist_map); computing a multi-source BFS distance map returning minimum distances from any seed cell (spread_dist_multi); listing all background cells reachable from a source in sorted order (spread_reachable); finding the BFS shortest path distance between two specific cells failing when no path exists (spread_path_dist); testing reachability between two cells deterministically (spread_path_exists); collecting all cells at exactly BFS distance D in a sorted list (spread_ring); collecting all cells within BFS distance MaxD in a sorted list (spread_zone); finding the minimum BFS distance from a source to any cell in a target set (spread_dist_to_set); identifying the farthest reachable cell by BFS distance (spread_farthest); performing one BFS level expansion returning the sorted deduplicated set of unvisited Bg neighbors of a frontier (spread_expand_once); spreading a color outward through background cells up to MaxD BFS steps from all seed cells of that color (spread_spread_color); computing the maximum BFS distance from a source to any reachable cell (spread_max_dist); overlaying BFS distances onto the grid leaving non-Bg and unreachable cells unchanged (spread_fill_dist); and computing a Voronoi assignment of Bg cells to the nearest seed by index with ties broken by lower seed index (spread_voronoi).


Chapter 88 covers the Enclosure Pack (Layer 106): collecting all cells on the four grid edges in sorted order (enclosure_border_cells); finding all background cells reachable from the grid border by 4-connected background steps returning the exterior region (enclosure_outer_cells); finding all background cells not reachable from the border returning the enclosed interior region (enclosure_inner_cells); testing if a specific cell is an enclosed background cell (enclosure_is_inner); testing if a specific cell is an exterior background cell (enclosure_is_outer); painting all enclosed background cells with a given color (enclosure_fill_inner); counting the number of enclosed background cells (enclosure_inner_count); testing whether at least one enclosed background cell exists (enclosure_has_inner); partitioning enclosed background cells into their 4-connected components (enclosure_inner_components); partitioning exterior background cells into their 4-connected components (enclosure_outer_components); filling the single enclosed background component containing a given cell with a color failing if the cell is not enclosed (enclosure_fill_hole); finding all cells of a given color that lie on the boundary of the color region defined as cells on the grid edge or with at least one non-color neighbor (enclosure_boundary_cells); finding all cells of a given color strictly interior to the color region defined as cells with exactly four in-bounds neighbors all of the same color (enclosure_interior_cells); and testing whether all four orthogonal neighbors of a cell are either out of bounds or equal to a given wall color (enclosure_is_surrounded).


Chapter 89 covers the Hull Pack (Layer 107): computing the convex hull of a cell set as a list of vertices in clockwise screen order using Andrew's monotone chain algorithm with collinear points excluded (hull_convex_hull); testing whether a discrete cell set is convex meaning it equals the complete set of grid cells inside its convex hull within the bounding box (hull_is_convex); counting the number of hull vertices excluding collinear intermediate points (hull_hull_size); computing twice the hull polygon area as an integer using the shoelace formula with zero returned for degenerate hulls of fewer than 3 vertices (hull_hull_area2); testing whether a given row-column point lies inside or on the boundary of a convex hull with separate handling for empty, single-vertex, and two-vertex degenerate cases (hull_in_hull); collecting all grid cells that lie inside or on the convex hull as a sorted list (hull_cells_in_hull); painting all cells inside the convex hull with a given color (hull_fill_hull); finding all background cells inside the convex hull but absent from the input shape, representing the concavities or dents in the shape (hull_concavities); finding the pair of cells with the greatest squared Euclidean distance and returning that squared distance (hull_diameter); computing the row-span and column-span of the hull bounding box (hull_aspect); testing whether the convex hull has exactly four vertices and all four interior angles are exactly 90 degrees working for both axis-aligned and tilted rectangles (hull_is_rect); computing the sum of squared edge lengths around the closed hull as an integer proxy for perimeter avoiding floating-point arithmetic (hull_hull_perim2); computing the integer floor of the average row and column of the hull vertices as a centroid estimate (hull_centroid); and testing whether every cell in a given set lies inside or on the convex hull of another set (hull_hull_contains).


Chapter 90 covers the Patch Pack (Layer 108): extracting an H x W rectangular sub-patch from a grid starting at a given row and column position (patch_extract); getting the height and width dimensions of a patch (patch_dims); non-deterministically enumerating all positions where a small pattern occurs exactly in a larger grid by Prolog structural unification of the extracted sub-grid (patch_match); collecting all matching positions as a list (patch_match_all); counting matching positions (patch_match_count); matching with a background wildcard where background-colored cells in the pattern match any grid cell and non-background cells must match exactly (patch_match_bg); enumerating all overlapping H x W sub-patches in row-major order with their top-left coordinates (patch_slides); finding the sorted list of all distinct patches of a given size (patch_unique_patches); finding the patch that occurs most frequently and its count using msort-then-grouping (patch_most_common_patch); returning the sorted list of R-C cell positions where two same-size patches differ (patch_diff); testing whether all cells in a patch have the same value and returning that value (patch_is_uniform); reversing each row independently for a horizontal left-right flip (patch_flip_h); reversing the sequence of rows for a vertical top-bottom flip (patch_flip_v); and rotating a patch 90 degrees clockwise using the formula Rotated(r-prime, c-prime) equals Patch(H-1-c-prime, r-prime) to produce a W x H output from an H x W input (patch_rot90).


Chapter 91 covers the Signal Pack (Layer 109): finding the sorted list of 0-based indices of strict interior local maxima in a list where index I is a peak if List[I] exceeds both immediate neighbors and I is neither the first nor the last element (signal_peaks); finding strict interior local minima by the symmetric condition (signal_valleys); finding the 0-based index of the maximum value with first-occurrence tie-breaking using max_list then nth0 plus cut (signal_argmax); finding the index of the minimum value (signal_argmin); computing consecutive element-wise differences where the result has one fewer element than the input (signal_diffs); mapping each consecutive difference to its sign as 1 for positive, -1 for negative, or 0 for zero (signal_slope_signs); testing that each consecutive pair satisfies strict less-than vacuously true for lists of length 0 or 1 (signal_is_increasing); testing strict greater-than (signal_is_decreasing); testing non-strict less-than-or-equal (signal_is_nondecreasing); testing non-strict greater-than-or-equal (signal_is_nonincreasing); partitioning 0-based indices into those with values strictly above a threshold and those at or below it (signal_threshold); extracting a complete row of a grid by 0-based index (signal_row_profile); extracting a complete column by capturing the column index in a YALL lambda inside maplist (signal_col_profile); and extracting both the row and column profiles through a single cross-shaped query at a given grid position (signal_cross_profile).


Chapter 92 covers the Histogram Pack (Layer 110): counting occurrences of a specific value in a list using include/3 with ==(V) and counting the result (histogram_count); computing sorted V-Count pairs for every distinct value in ascending value order (histogram_freq); finding the most frequent value and its count with ties broken by smallest value (histogram_mode); finding the least frequent value with ties broken by largest value (histogram_rare); returning the sorted list of distinct values via sort/2 (histogram_unique_vals); counting distinct values (histogram_unique_count); sorting all V-Count pairs by count descending with ties broken by value ascending using negated-count keysort for stability (histogram_sorted_by_freq); extracting the N most frequent V-Count pairs as a prefix of the sorted list using length/2 and append/3 (histogram_top_n); testing that a value appears at least once with memberchk for determinism (histogram_has_value); testing that every element of a list equals the first element using forall and =:= (histogram_all_equal); flattening a 2D grid with append/2 and computing its frequency table (histogram_grid_freq); finding the most frequent cell color in a grid (histogram_grid_mode); finding the least frequent cell color in a grid (histogram_grid_rare); and finding the 1-based rank of a value by frequency where rank 1 is the most common using nth1/3 with a cut for determinism (histogram_rank).


Chapter 93 covers the Zoom Pack (Layer 111): upscaling a grid by integer factor F where each cell becomes an FxF block of the same color using a recursive per-row structure to preserve correct row ordering (zoom_scale_up); repeating each element F times in a row using findall + between + append (zoom_scale_up_row); downsampling a grid by sampling every Fth row and column starting at position 0 (zoom_scale_down); sampling every Fth element from a single row using I mod F =:= 0 (zoom_scale_down_row); inferring the integer scale factor from the ratio of column widths of two grids (zoom_scale_factor); testing that a grid is exactly the F-upscaling of a smaller grid (zoom_is_scaled_up); computing the upscaled dimensions BH = H*F and BW = W*F (zoom_dims_up); computing the downsampled dimensions SH = H//F and SW = W//F (zoom_dims_down); extracting the FxF block at tile position (BR, BC) using row and column arithmetic with a zoom_take_cols_ helper (zoom_extract_block); splitting a grid into all FxF blocks as a flat row-major list (zoom_split_blocks); computing the number of complete tile blocks along each grid dimension (zoom_blocks_dims); returning the single color of a uniform FxF block and failing for non-uniform blocks (zoom_block_color); downsampling using the mode (most common value) of each FxF block with largest-value tie-breaking via msort + last on N-V count pairs (zoom_scale_down_mode); and tiling a small grid NR times vertically and NC times horizontally by first extending each row NC times then repeating the full block NR times (zoom_tile_grid).


Chapter 94 covers the Offset Pack (Layer 112): shifting a row rightward by N cells where the left N positions become background and the rightmost N elements are dropped using length/2 + append/3 for prefix extraction and findall + between for background cell generation (od_shift_row); shifting a row leftward (od_shift_row_left); circular rightward row shift where the last N mod W elements wrap to the front by splitting at W-N and reassembling (od_roll_row); shifting all grid rows rightward with maplist and a YALL lambda (od_shift_right); shifting leftward (od_shift_left); shifting content downward by inserting N background rows at the top and dropping N bottom rows (od_shift_down); shifting upward (od_shift_up); dispatching shift by direction atom using if-then-else for determinism (od_shift_dir); circular rightward shift of every row (od_roll_right); circular leftward shift of every row (od_roll_left); circular downward roll where the last N rows wrap to the top by splitting at H-N (od_roll_down); circular upward roll where the first N rows wrap to the bottom (od_roll_up); shifting a specific color by (DR, DC) where each output cell checks its source position for Color with an in-bounds test and if-then-else logic without an explicit mutable structure (od_shift_color); and finding the integer (DR, DC) displacement between two grids by brute-force search over all displacements up to grid dimensions with a cut after the first match (od_infer_shift).


Chapter 95 covers the Interleave Pack (Layer 113): merging two same-height grids by alternating rows so that row 0 comes from GridA, row 1 from GridB, row 2 from GridA, and so on (interleave_rows); merging by alternating columns within each row so that elements interleave A[0], B[0], A[1], B[1] (interleave_cols); splitting a grid into even-indexed rows and odd-indexed rows using a two-clause index-accumulating helper with a cut (interleave_derows); splitting each row into even-indexed and odd-indexed elements (interleave_decols); inserting a background row of the same width between every pair of adjacent rows to create a woven grid of 2*H-1 rows (interleave_weave_rows); inserting a background value between adjacent column values in each row to create rows of width 2*W-1 (interleave_weave_cols); removing odd-indexed rows to recover the even-indexed core by delegating to interleave_derows (interleave_unweave_rows); removing odd-indexed columns (interleave_unweave_cols); extracting every Step-th row starting at a 0-based index using findall + between + modular arithmetic (interleave_stride_rows); extracting every Step-th column using findall for column indices and maplist to project each row (interleave_stride_cols); extracting rows at a given 0-based index list using maplist over nth0 (interleave_select_rows); extracting columns at a given index list using maplist over per-row index projection (interleave_select_cols); round-robin interleaving of rows from a list of equal-height grids so that row I of each grid appears in turn before row I+1 of any grid, using findall with between and member (interleave_rows_n); and partitioning rows into consecutive groups of N using recursive length + append with an if-then-else for partial last groups (interleave_block_rows).


Chapter 96 covers the Panel Pack (Layer 114): testing whether a row is entirely background using forall/2 over all row elements (panel_is_divider_row); testing whether a column is entirely background using forall over nth0-accessed column values (panel_is_divider_col); listing all all-background row indices (panel_divider_rows); listing all all-background column indices (panel_divider_cols); splitting at given row divider indices where divider rows are excluded and bands between dividers become separate parts using a two-clause recursive helper with cut (panel_split_rows); splitting at given column divider indices using panel_slice_row_ via maplist (panel_split_cols); detecting and splitting at all-background rows automatically (panel_auto_split_rows); detecting and splitting at all-background columns automatically (panel_auto_split_cols); removing all all-background rows using findall and not-memberchk (panel_strip_rows); removing all all-background columns from each row (panel_strip_cols); splitting at floor midpoints to produce four quadrant sub-grids (panel_quadrants); partitioning into N equal-width column strips using findall with arithmetic for strip boundaries (panel_n_col_parts); partitioning into custom-width column slices given a list of widths using cumulative sums and a recursive slicer (panel_col_slices); and testing for any all-background row or column using if-then-else to short-circuit on the first found divider (panel_has_dividers).


Chapter 97 covers the Scan Pack (Layer 115): enumerating all R-C-V triples in row-major order with row as the outer loop (scan_row_major); enumerating in column-major order with column as the outer loop (scan_col_major); listing all R-C positions of a specific value using findall over both dimensions (scan_cells_of); returning a new grid with a single cell replaced using nested findall with if-then-else on the row and column indices (scan_set_cell); applying a user-supplied 4-argument goal Goal(R,C,OldV,NewV) to every cell via nested findall with call/5 (scan_map_cells); collecting R-C-V triples satisfying a 3-argument predicate via findall with call/3 (scan_filter_cells); applying a list of R-C-V batch updates using memberchk in an if-then-else lookup (scan_update_cells); traversing in boustrophedon order where even rows go left-to-right and odd rows go right-to-left by computing the column as W1-Offset for odd rows (scan_zigzag); traversing in clockwise spiral order from the outermost ring inward using a recursive scan_spiral_/6 helper with nested if-then-else termination and reverse/2 for bottom and left sides (scan_spiral_in); visiting the outer border cells clockwise using if-then-else guards on H > 1 and W > 1 and reverse/2 for right-to-left sides (scan_border_traversal); visiting NE-going diagonals where D = R+C is enumerated from 0 to H+W-2 with within-diagonal row iteration (scan_diag_traversal_ne); visiting SE-going diagonals where D = C-R is enumerated from -(H-1) to W-1 with within-diagonal row iteration (scan_diag_traversal_se); building an H-by-W grid from a sparse R-C-V cell list using memberchk in an if-then-else inside nested findall (scan_grid_from_cells); and finding the first occurrence of a value in row-major order using between/3 enumeration followed by a cut (scan_index_of).


Chapter 98 covers the Bound Pack (Layer 116): computing the tight inclusive bounding box of all cells of a given color using findall + min_list + max_list and failing when the color is absent (bound_bbox); computing bounding box height as R1-R0+1 (bound_bbox_h); computing bounding box width as C1-C0+1 (bound_bbox_w); cropping a grid to an inclusive row and column rectangle using findall over the row range and bound_slice_row_ with length/2 + append/3 for inclusive column slices (bound_crop_bbox); cropping to the tight bounding box of a color as a two-step pipeline (bound_crop_color); removing all all-background border rows and columns by finding the bounding box of all non-background cells with if-then-else for the all-background case (bound_trim); adding N background cells on all four sides using findall for background rows and maplist with bound_pad_row_ for row-level extension (bound_pad); placing a sub-grid into a canvas at a given top-left position with nested findall and if-then-else on patch row/column bounds (bound_place); computing the integer center of a color's bounding box using floor division (bound_center); testing whether a cell lies within an inclusive bounding box with four arithmetic comparisons (bound_bbox_contains); testing whether two inclusive bounding boxes overlap by the four-condition overlap formula (bound_bbox_overlap); computing the smallest bounding box enclosing two boxes using min and max (bound_bbox_union); expanding a bounding box outward by N cells on all sides with arithmetic (bound_expand); and filling every cell within an inclusive bounding box with a color using nested findall with if-then-else on the row and column bounds (bound_fill_bbox).


Chapter 99 covers the Cellset Pack (Layer 117): converting a grid to a sorted list of R-C positions for a given color using findall over all row-column pairs and sort/2 for canonical order (cellset_from_grid); painting a sorted cell set onto an H by W canvas by testing membership with memberchk inside nested findall (cellset_to_grid); computing the sorted set union of two cell sets by appending and sorting (cellset_union); computing the sorted set intersection by collecting members of A that appear in B (cellset_intersect); computing the set difference of A minus B by delegating to subtract/3 from library(lists) (cellset_subtract); shifting all cells by a row-column offset using maplist with the private cellset_add_ helper preserving sorted order because translation is monotone (cellset_translate); computing the bounding box of a non-empty cell set using findall for row and column index lists and min_list plus max_list for extremes (cellset_bbox); translating a cell set so its minimum row and column become zero and returning the negative offsets used with an empty-list base case (cellset_normalize); counting cells using length/2 (cellset_size); testing deterministic membership with memberchk (cellset_contains); finding all 4-connected background neighbors not in the set using four fixed direction offsets in a findall and sorting to remove shared-neighbor duplicates (cellset_adjacent_bg); testing shape equality after normalizing both sets to the origin and comparing with structural equality (cellset_same_shape); rotating cell positions 90 degrees clockwise within a grid of H rows using the formula new_R = old_C and new_C = H-1-old_R with maplist and a final sort (cellset_rotate_90); and mirroring cell positions horizontally within a grid of W columns using the formula new_C = W-1-old_C with maplist and sort (cellset_mirror_h).


Chapter 100 covers the Locate Pack (Layer 118): testing whether a sub-grid appears at a specific top-left position in a larger grid using forall over all sub-grid positions (locate_subgrid_at); finding the first row-major position where a template matches using between enumeration plus locate_subgrid_at with a cut (locate_find_sub); collecting all matching positions with findall (locate_all_sub); counting occurrences by measuring the locate_all_sub result (locate_subgrid_count); finding the first row index where the grid row equals a pattern via nth0 unification with a cut (locate_row_pattern); collecting all matching row indices (locate_all_row_pattern); finding the first column index whose extracted value list equals a pattern via an inner findall with a cut (locate_col_pattern); collecting all such column indices using an outer findall whose inner findall unifies with the pattern (locate_all_col_pattern); decomposing a grid row as a prefix followed by a rest using append/3 (locate_row_prefix); testing whether a grid row ends with a given suffix using append and a cut for a deterministic test (locate_row_suffix); finding the unique cell position where a value appears exactly once by collecting all positions with findall and matching a single-element list pattern (locate_anchor); counting occurrences of a value per row using maplist with a findall-plus-member helper (locate_row_count); finding sorted rows that contain a value using findall with member and sort (locate_row_contains); and finding sorted columns that contain a value using findall with nth0 and sort (locate_col_contains).


Chapter 101 covers the Extended Cell Selection Pack (Layer 119, xs_* prefix): selecting cells with value strictly less than a threshold (xsel_cells_lt); selecting cells with value strictly greater than a threshold (xsel_cells_gt); selecting cells with value less than or equal to a threshold (xsel_cells_le); selecting cells with value greater than or equal to a threshold (xsel_cells_ge); selecting cells in a closed value interval (xsel_cells_between); selecting cells not equal to a given value (xsel_cells_ne); locating all cells that contain the maximum value using a two-pass collect-then-filter approach (xsel_max_cells); locating all cells that contain the minimum value (xsel_min_cells); applying a binary threshold to produce a new grid where each cell becomes one of two values depending on whether it meets the threshold (xsel_threshold); replacing cells with value below a threshold with a new value (xsel_replace_lt); replacing cells with value above a threshold with a new value (xsel_replace_gt); computing the sorted list of unique values as an ordinal rank ladder (xsel_rank_vals); finding the 0-based ordinal rank of a specific value (xsel_val_rank); and replacing every cell with its ordinal rank to produce a rank grid (xsel_rank_grid).


Chapter 106 covers the Numerical Sequence Operations Pack (Layer 124, nr_* prefix): computing consecutive pairwise differences where each element is the difference of the next minus the current using two-clause recursion (numseq_diff); computing prefix cumulative sums where each output element is the sum of all inputs up to and including that position using a recursive accumulator helper (numseq_cumsum); computing the running maximum where each output element is the maximum of all inputs seen so far using a recursive helper updating Max1 is max(H, Max0) (numseq_running_max); computing the running minimum symmetrically using Min1 is min(H, Min0) (numseq_running_min); element-wise addition of two equal-length lists using maplist/3 with a named numseq_add_ helper (numseq_zip_add); element-wise subtraction (numseq_zip_sub); element-wise multiplication (numseq_zip_mul); element-wise maximum (numseq_zip_max); element-wise minimum (numseq_zip_min); multiplying every element by a scalar K using maplist/3 with a numseq_scale_ helper that captures K (numseq_scale); adding a constant D to every element using a numseq_offset_ helper that captures D (numseq_offset); testing that a list is non-decreasing using three-clause recursion with A =< B guards (numseq_is_sorted); testing that a list is an arithmetic sequence by computing the common difference from the first pair and verifying it throughout using a recursive helper (numseq_is_arith); and finding the shortest repeating period P within 1..MaxP by trying periods in ascending order with between/3, checking that Len mod P = 0, and verifying all elements equal their counterpart P positions later using forall + nth0 (numseq_period).

Chapter 107 covers the Cell-Wise Arithmetic on 2D Grids Pack (Layer 125, gm_* prefix): computing cell-wise sum of two grids using nested maplist/4 with a named row helper gm_add_row_ and cell helper gm_add_ where Z is X+Y (gm_add); cell-wise difference (gm_sub); cell-wise product (gm_mul); cell-wise maximum using max/2 built-in (gm_max); cell-wise minimum (gm_min); multiplying every cell by a scalar K using nested maplist/3 with row helper gm_scale_row_(K) capturing K and cell helper gm_scale_(K) (gm_scale); adding a constant D to every cell (gm_offset); replacing every cell with its absolute value using abs/1 (gm_abs); producing a 1/0 mask where 1 marks positions where two grids agree using a two-clause cut helper gm_eq_(X,X,1):-!. gm_eq_(_,_,0) (gm_eq_mask); producing the complement mask where 1 marks positions where the grids differ (gm_diff_mask); clamping each cell to an inclusive range [Lo,Hi] using max(Lo,min(Hi,V)) (gm_clamp); applying modulo M to every cell using V mod M (gm_modulo); summing all cell values by flattening with findall+member and accumulating with a recursive helper (gm_sum); and computing the product of all cell values similarly (gm_product).

Chapter 108 covers the Color Lookup Table and Palette Substitution Pack (Layer 126, cm_* prefix): substituting each cell value using a list of Old-New pairs where unmatched cells are left unchanged via memberchk lookup in an if-then-else (colormap_apply); same substitution but replacing unmatched cells with a default background value (colormap_apply_default); applying the inverse of a map by swapping all Old-New pairs to New-Old before applying (colormap_invert); composing two maps so that MapAB[k] = MapB[MapA[k]] using findall with memberchk for the intermediate lookup (colormap_compose); deriving a map from the cell-by-cell correspondence of two same-sized grids using nth0 to align positions and sort/2 to deduplicate (colormap_from_grids); building the identity map V-V for every value in a palette (colormap_identity); building a map from two parallel equal-length lists using nth0 to pair corresponding elements (colormap_remap); applying a map to a one-dimensional list with unmatched elements left unchanged (colormap_remap_list); returning the sorted list of all source (key) values in a map using findall and sort/2 (colormap_palette); returning the sorted list of all distinct values used anywhere in a grid using findall with two member/2 calls and sort/2 (colormap_used); testing whether a key appears in a map using memberchk/2 for determinism (colormap_has_key); looking up the value for a key in a map failing if absent (colormap_lookup); filtering a map to keep only entries whose key appears in a given key set (colormap_restrict); and extending a map with K-Bg entries for every palette value that has no entry in the map using findall + negation-as-failure + append/3 (colormap_expand).

Chapter 109 covers the Rectangular Sub-Grid Block Decomposition Pack (Layer 127, bk_* prefix): extracting a rectangular sub-grid by row and column range using nested findall with between/3 and nth0/3 (block_extract); computing block row and column counts by integer division of grid dimensions by block dimensions (block_dims); splitting a grid into horizontal strips of a fixed height using findall over strip indices (block_split_h); splitting into vertical strips of a fixed width using findall with inner row-slicing (block_split_v); tiling a grid N times vertically by replicating rows using findall over between(1,N,_) and member (block_tile_h); tiling horizontally by replicating cell values in each row using maplist with a capturing helper (block_tile_v); counting complete horizontal strips via integer division (block_count_h); counting complete vertical strips (block_count_v); applying a 2-argument predicate to each block in a list using maplist (block_map); returning the value at a single cell position using nth0 twice (block_uniform); finding the most frequent value in a 2x2 block using include/3 + max_list/2 + sort for tie-breaking (block_mode_color); finding the most frequent in-bounds border value in a 3x3 neighborhood excluding the center using findall + negation-as-failure (block_border_color); testing whether every cell equals a target color using forall + member twice (block_is_solid); and testing whether a cell lies on the outer grid border using a disjunctive if-then-else guard (block_is_border).

END OF CHAPTER 11

Chapter 102 covers the Cellular Automaton Neighborhood Aggregation Pack (Layer 120, at_* prefix): producing a count grid where each cell holds the number of in-bounds 4-connected neighbors equal to a target value using triple-nested findall (autom_count_nbrs_4); doing the same for 8-connected neighborhoods (autom_count_nbrs_8); counting 4-connected neighbors that share the same value as the cell itself (autom_count_same_4); counting same-valued 8-connected neighbors (autom_count_same_8); producing a boolean 1/0 grid indicating whether any in-bounds 4-neighbor equals a target value using a cut-on-first-match helper (autom_any_nbr_4); producing a boolean grid indicating whether every in-bounds 4-neighbor equals a target value using forall/2 (autom_all_nbrs_4); doing the any-test for 8-neighbors (autom_any_nbr_8); doing the all-test for 8-neighbors (autom_all_nbrs_8); finding isolated target-valued cells that have no same-valued 4-neighbor using negation-as-failure (autom_isolated_4); finding 8-connected isolated cells (autom_isolated_8); applying a birth rule where dead cells with exactly N live 4-neighbors become live (autom_birth_4); doing the same for 8-neighbors (autom_birth_8); computing the majority value among a cell and its in-bounds 4-neighbors with tie-breaking to a background value (autom_majority_4); and applying one step of Conway's Game of Life with the B3/S23 rule using a three-clause autom_gol_next_ helper (autom_step_gol).

Chapter 103 covers the 1D Cross-Section Extraction Pack (Layer 121, cx_* prefix): extracting a single row by index using nth0/3 (cross_row); extracting a single column as a list by collecting the C-th element from every row using member+nth0 inside findall (cross_col); extracting the main diagonal where each element is Grid[R][R] (cross_diag); extracting the anti-diagonal where each element is Grid[R][W-1-R] with a guard C>=0 for non-square grids (cross_antidiag); computing the sum of a row using sum_list/2 (cross_row_sum); computing the sum of a column (cross_col_sum); counting occurrences of a value in a row using findall+member+length (cross_row_count); counting occurrences in a column (cross_col_count); returning sorted row indices that contain a given value using between+nth0+member inside findall then sort/2 (cross_rows_with); returning sorted column indices that contain a given value (cross_cols_with); returning the sorted unique value set of a row using sort/2 (cross_row_uniq); returning the sorted unique value set of a column (cross_col_uniq); returning the most frequent value in a row with tie-breaking to a background value using the findall+max_list+unique-winner-or-Bg pattern (cross_row_mode); and doing the same for a column (cross_col_mode).

Chapter 104 covers the Boolean Mask Operations Pack (Layer 122, mk_* prefix): creating a 1/0 mask from a grid by marking positions that equal a target value using nested maplist with a two-clause cut-on-match helper (mask_from_val); creating the complementary mask that marks non-background positions (mask_from_bg); inverting every bit in a mask (mask_invert); computing element-wise AND of two masks using maplist/4 with a two-clause cut-based helper (mask_and); computing element-wise OR (mask_or); computing element-wise XOR using (A+B) mod 2 arithmetic (mask_xor); producing a grid that keeps original values at mask=1 positions and replaces mask=0 positions with a background value (mask_apply); producing a grid that fills mask=1 positions with a constant and keeps mask=0 positions unchanged (mask_fill); blending two grids by taking values from the second grid at mask=1 positions and from the first grid at mask=0 positions using maplist/5 for four-way parallel iteration (mask_overlay); returning sorted R-C pairs of mask=1 positions using findall+nth0+sort (mask_where); counting 1-valued cells using findall+member+length (mask_count); testing whether any cell equals 1 using member/2 with cut-on-first-match (mask_any); testing whether all cells equal 1 using forall/2 (built-in) (mask_all); and extracting the sorted set of unique Grid values at mask=1 positions using findall+nth0+sort (mask_extract).

Chapter 105 covers the Grid-as-Table Operations Pack (Layer 123, tb_* prefix): transposing a grid so rows become columns by iterating column indices and collecting each column using member+nth0 inside findall (table_transpose); sorting rows ascending by the value of a specified column using findall+keysort+unwrap (table_sort_rows); filtering rows to keep only those where a specified column equals a target value using structural equality (table_filter_rows); grouping rows by their column value into V-Rows pairs ordered by sorted unique key using two-pass findall (table_group_by); counting rows per unique column value as V-N pairs (table_count_by); deduplicating rows in document order using a recursive accumulator with \+member/2 (table_unique_rows); extracting a subset of columns in a specified order using member+nth0 inside findall (table_select_cols); removing a column from every row using between+inequality (table_drop_col); appending a list as a new rightmost column using nth0 index pairing and append/3 (table_add_col); inserting a row at a specified position using two-clause recursion (table_insert_row); deleting a row at a specified index using findall with index inequality (table_delete_row); swapping two rows by pre-fetching both and rebuilding with if-then-else (table_swap_rows); finding the first row where a column has its maximum value using max_list/2 then nth0+cut (table_col_max_row); and finding the first row where a column has its minimum value using min_list/2 then nth0+cut (table_col_min_row).

Chapter 110 covers the Grid Edge Detection and Boundary Analysis Pack (Layer 128, ed_* prefix): collecting all horizontal edge R-C positions where adjacent rows differ using findall with between and nth0 (edge_h_edges); collecting all vertical edge R-C positions where adjacent columns differ (edge_v_edges); testing whether a single horizontal edge exists between a specific row pair at a column and returning Bool=1 or 0 (edge_h_edge); testing for a single vertical edge with the same Bool pattern (edge_v_edge); combining horizontal and vertical edges into a sorted list of h-R-C and v-R-C terms (edge_all_edges); finding all cells of a given value that have at least one 4-connected neighbor with a different value using disjunctive findall (edge_boundary_cells); finding all cells on the outer grid perimeter using row and column index comparison (edge_outer_boundary); finding all cells of a given value that are NOT boundary cells using negation-as-failure against the boundary set (edge_inner_cells); counting the total number of edges as the sum of horizontal and vertical edge list lengths (edge_edge_count); testing whether a grid has zero edges (edge_is_smooth); producing a same-size mask grid where each cell is 1 if it is a boundary cell and Bg otherwise using nested findall and a private four-direction helper (edge_label_edges); testing whether a single cell is a boundary cell and returning Bool=1 or 0 using the same private helper (edge_has_edge); finding all 2x2 top-left corner positions where the four cells are not all equal using findall with negation-as-failure on the four-cell uniformity condition (edge_corners); and counting corner positions (edge_corner_count).

Chapter 111 covers the Cell Distance and Proximity Computation Pack (Layer 129, dt_* prefix): computing Manhattan (L1) distance as the sum of absolute row and column differences (distance_manhattan); computing Chebyshev (L-infinity, king-move) distance as the maximum of absolute row and column differences (distance_chebyshev); computing squared Euclidean distance as DR*DR+DC*DC returning an integer to avoid floating-point square roots (distance_euclidean_sq); finding the nearest cell of a given value to a query position by collecting all D-R-C triples and selecting the minimum using a private min_list_key_ accumulator helper (distance_nearest); finding the farthest cell of a given value using a symmetric max_list_key_ helper (distance_farthest); finding all cells of a given value at exactly a specified Manhattan distance using findall with equality guard (distance_all_at); collecting all in-bounds positions within a Manhattan radius using findall with an =< guard (distance_within_manhattan); doing the same for Chebyshev radius (distance_within_chebyshev); building a same-size distance transform grid where each cell holds its Manhattan distance to the nearest occurrence of a target value using pre-collected target positions and min_list (distance_map_manhattan); building the Chebyshev distance transform grid (distance_map_chebyshev); collecting positions at exactly a specified Manhattan distance forming a ring shape (distance_ring_manhattan); computing the integer floor centroid of all cells with a given value by summing row and column indices separately with the private sumlist_ helper and dividing by count (distance_centroid); computing the maximum pairwise Manhattan distance (diameter) of a set of R-C pairs by enumerating all unordered pairs with A @< B and selecting the maximum with max_list_key_ (distance_diameter_manhattan); and computing the diameter under Chebyshev distance (distance_diameter_chebyshev).

Chapter 112 covers the Grid Topology and Connected Component Analysis Pack (Layer 130, tp_* prefix): finding all cells of a given value 4-connected to a seed using BFS with a memberchk-based visited set and a four-direction topology_val_nbr_ helper (topology_component); partitioning all cells of a value into connected components using topology_split_cells_ which iterates seed extraction and subtract/3 removal (topology_all_components); counting components (topology_component_count); measuring the size of the component at a seed (topology_component_size); finding the largest component by tagging each with its length and selecting the maximum (topology_largest_component); testing whether Dst is reachable from Src through a value's cells by flood filling from Src (topology_reachable); finding Bg cells that cannot reach the grid border via Bg neighbors by seeding a BFS from all border Bg cells and collecting unreached Bg cells (topology_enclosed); testing whether any enclosed Bg cells exist (topology_has_hole); counting distinct enclosed Bg components using topology_split_cells_plain_ which is a variant BFS over a restricted set of allowed cells (topology_hole_count); replacing enclosed Bg cells with Val in a new grid (topology_fill_holes); collecting components with at least one border cell using once/1 to avoid duplicate entries from multiple border-touching cells (topology_border_components); collecting components with no border cells using negation-as-failure (topology_interior_components); building a same-size grid where each Val cell shows its 1-based component index via a flat R-C-Label lookup constructed by topology_build_label_map_ (topology_label_components); and testing same-component membership by delegating to topology_reachable (topology_same_component).

Chapter 113 covers the Row and Column Projection and Profile Analysis Pack (Layer 131, pj_* prefix): computing the sum of each row by delegating to maplist(sum_list, Grid, Sums) (pj_row_sums); computing the sum of each column by extracting each column with the private pj_col_ helper and summing (pj_col_sums); counting how many times a value appears in each row using include(=(Val), Row, Occ) and length (pj_row_counts); counting per-column occurrences the same way (pj_col_counts); finding the maximum value per row via maplist(max_list, Grid, Maxes) (pj_row_maxes); finding the maximum per column via pj_col_ and max_list (pj_col_maxes); collecting sorted unique values per row via sort (pj_row_uniq); collecting sorted unique values per column (pj_col_uniq); finding all column indices where a value appears in any row, deduplicating with sort (pj_shadow_h); finding all row indices where a value appears in any column (pj_shadow_v); computing the longest contiguous run of a value in each row using the pj_run_acc_ tail-recursive accumulator with cur and max accumulators (pj_h_profile); computing the longest run per column (pj_v_profile); computing the modal value of each row using pj_mode_ which sorts for unique values, counts each via include, then selects the maximum-count pair with smallest-value tie-breaking via pj_max_pair_ (pj_row_modes); and computing modal values per column (pj_col_modes).

Chapter 114 covers the Row and Column Gradient and Progression Analysis Pack (Layer 132, gr_* prefix): computing adjacent differences per row using maplist over gradient_adj_diffs_ which recurses with three clauses for empty, singleton, and pair-plus-tail lists (gradient_h_diffs); computing column-wise differences between adjacent row pairs using gradient_pair_diffs_ for element-wise subtraction (gradient_v_diffs); classifying each row as 1 (weakly non-decreasing), -1 (weakly non-increasing), or 0 (mixed) using gradient_is_nondec_ and gradient_is_noninc_ guard predicates (gradient_h_mono); classifying each column (gradient_v_mono); computing max minus min per row using max_list and min_list from library(lists) (gradient_h_range); computing range per column (gradient_v_range); computing last minus first element per row as the net horizontal slope (gradient_h_slope); computing net slope per column (gradient_v_slope); testing whether all elements in each row are equal using gradient_all_equal_ with =:= guards (gradient_h_const); testing column constancy (gradient_v_const); computing the running cumulative sum of each row via gradient_cumsum_acc_ with an integer accumulator (gradient_h_cum); computing cumulative sums down each column using gradient_v_cum_acc_ which accumulates the row-wise sum via gradient_row_add_ after the first row (gradient_v_cum); computing second-order differences by applying gradient_adj_diffs_ twice via gradient_second_diffs_ (gradient_h_second); and computing second-order vertical differences by calling gradient_v_diffs twice (gradient_v_second).

Chapter 115 covers the 2D Grid Extrema, Local Peaks, and Threshold Filtering Pack (Layer 133, ex_* prefix): finding the maximum value in a grid by flattening all values via findall and calling max_list (extrema_max_val); finding the minimum with min_list (extrema_min_val); collecting all R-C positions whose value equals the grid maximum using findall with between and nth0 matching (extrema_max_cells); collecting minimum positions (extrema_min_cells); finding the 0-based row index with the maximum row sum using findall of sum-index pairs and extrema_argmax_acc_ which is a recursive accumulator that updates only on strict > preserving first-occurrence tie-breaking (extrema_row_argmax); finding the column index with maximum column sum using extrema_col_sum_ which extracts each column via findall + sum_list (extrema_col_argmax); symmetric argmin using extrema_argmin_acc_ with strict < (extrema_row_argmin, extrema_col_argmin); finding cells strictly greater than all in-bounds 4-neighbors by computing neighbor coordinates RU/RD/CL/CR and testing each via extrema_check_nbr_lt_ which uses if-then-else for bounds checking and value comparison (extrema_local_max4); symmetric local minima (extrema_local_min4); collecting cells above a threshold with V > Thresh in findall (extrema_above); cells below (extrema_below); computing max minus min via extrema_flat_vals_ (extrema_range); and collecting non-zero positions with V =\= 0 (extrema_nonzero).

Chapter 116 covers the Per-Cell Neighborhood Value Aggregation Pack (Layer 134, na_* prefix): computing per-cell sum of in-bounds 4-neighbor values via nested findall with sum_list (naggr_sum4); computing per-cell sum of 8-neighbor values (naggr_sum8); computing per-cell maximum of 4-neighbors returning 0 for empty neighborhoods via if-then-else on Vals=[] (naggr_max4); maximum of 8-neighbors (naggr_max8); minimum of 4-neighbors (naggr_min4); minimum of 8-neighbors (naggr_min8); integer floor mean of 4-neighbors using S // N (naggr_mean4); floor mean of 8-neighbors (naggr_mean8); range (max - min) of 4-neighbors returning 0 when fewer than 2 exist (naggr_range4); range of 8-neighbors (naggr_range8); count of distinct values among 4-neighbors using sort/2 to deduplicate then length (naggr_spread4); count of distinct 8-neighbor values (naggr_spread8); count of 4-neighbors differing from the cell itself using nested findall with =:= comparison then Total - SC arithmetic (naggr_diff4); count of differing 8-neighbors (naggr_diff8). All predicates use two private helpers naggr_nbr4_vals_ and naggr_nbr8_vals_ that enumerate offset lists via member and filter with explicit bounds guards. All 42 acceptance tests pass.

Chapter 117 covers the Integer Median Computation for Lists and 2D Grids Pack (Layer 135, md_* prefix): computing the lower floor median of a non-empty integer list by applying msort/2 to preserve duplicates, computing the 0-based index MI=(N-1)//2 into the sorted list, and extracting the element at that index (median_median); extracting the median of grid row R using nth0/3 to get the row then median_median (median_row); extracting the median of column C by collecting column values via findall+member+nth0 then median_median (median_col); per-row median list using findall+member+median_median (median_row_medians); per-column median list using findall+between+median_col (median_col_medians); global grid median by flattening all values via findall then median_median (median_grid); 4-connected median filter using triple-nested findall to collect self plus in-bounds 4-neighbors then median_median on that list (median_filter4); 8-connected median filter with 8-direction offsets (median_filter8); cells strictly above the global median using findall+between+nth0 with V>Median guard (median_above); cells strictly below (median_below); cells in row R above that row's median (median_row_above); cells in row R below (median_row_below); cells in column C above that column's median (median_col_above); cells in column C below (median_col_below). All 42 acceptance tests pass. Key insight: msort preserves duplicates unlike sort, making it essential for correct median computation.

Chapter 118 covers the Neighborhood Mode Filter for 2D Grids Pack (Layer 136, nm_* prefix): computing the mode (most frequent value) of a non-empty integer list by building Value-Count pairs via nmode_freq_pairs_ on an msort-sorted list, finding the maximum count via max_list, collecting all candidates with that count, and sorting ascending to pick the smallest as tie-breaker (nmode_mode); collecting all tied-for-maximum values sorted ascending (nmode_mode_all); returning both the mode and its count (nmode_mode_count); computing the mode of grid row R via nth0 + nmode_mode (nmode_row); computing column mode by collecting column values with findall+member+nth0 then nmode_mode (nmode_col); per-row mode list (nmode_row_modes); per-column mode list (nmode_col_modes); global grid mode from all cell values (nmode_grid); 4-connected mode filter using triple-nested findall collecting cell+neighbor values then nmode_mode (nmode_filter4); 8-connected mode filter with 8-direction offsets (nmode_filter8); cells whose in-bounds 4-neighbors all share exactly one distinct value using sort(NbrVals,[_]) as a one-step uniformity test (nmode_uniform4); same for 8-neighbors (nmode_uniform8); cells whose own value differs from the mode of their 4-neighbors alone using nmode_mode on neighbors only then V=\=NbrMode (nmode_outlier4); same for 8-neighbors (nmode_outlier8). Critical implementation detail: nmode_count_prefix_/4 clause 1 (empty list) has a cut to prevent the catch-all clause from also matching an empty list, which would otherwise cause nmode_mode to generate duplicate solutions inside findall (manifesting as doubled mode values in nmode_row_modes output). All 42 acceptance tests pass.

Chapter 119 covers the Dense Ranking of Integer Values in Lists and 2D Grids Pack (Layer 137, rk_* prefix): computing the 1-based dense rank of a value in a list by sorting unique values and finding the 1-based position of V using rk_pos1_ which walks the sorted unique list from position 1 with a recursive accumulator and cut-on-match (rk_rank_of); replacing each list element with its dense rank using findall+member+rk_pos1_ (rk_dense); computing 0-based indices of list elements in ascending value order by zipping values with their indices then calling msort for stable sort then extracting indices (rk_argsort_asc); same for descending order by negating values before msort (rk_argsort_desc); applying rk_dense to each row independently via findall+member (rk_row_dense); applying per-column ranking via nested findall that re-computes column values for each (R,C) cell (rk_col_dense); grid-wide ranking by flattening all values to get unique sorted values then mapping each cell via rk_pos1_ (rk_grid_dense); cell-level row rank (rk_row_rank_of), column rank (rk_col_rank_of), and grid rank (rk_grid_rank_of) all using the same extract-unique-find-rank pattern; top-N selection by taking the last N sorted unique values then collecting matching cells (rk_top_n); bottom-N selection using first N (rk_bottom_n); rank-threshold filtering keeping cells with dense rank > K (rk_above_rank) or < K (rk_below_rank). All 42 acceptance tests pass.

Chapter 120 covers the Mean, Sum, and Deviation Statistics Pack (Layer 138, vt_* prefix): summing all list values via sum_list/2 (varstat_sum); floor arithmetic mean via sum//count integer division (varstat_mean_floor); rounded arithmetic mean via float division and banker's rounding (varstat_mean_round); per-element signed deviation from floor mean via findall+member+subtraction (varstat_deviation); per-element absolute deviation (varstat_abs_deviation); list of per-row integer sums via findall+member+sum_list (varstat_row_sums); list of per-column sums via findall+between+nested_findall+sum_list (varstat_col_sums); list of per-row floor means (varstat_row_means); list of per-column floor means (varstat_col_means); global floor mean by flattening all cells (varstat_global_mean); R-C positions of cells strictly above global mean in row-major order (varstat_above_mean); R-C positions of cells strictly below global mean (varstat_below_mean); R-C positions of cells above their own row's floor mean (varstat_row_above_mean); R-C positions of cells above their own column's floor mean enumerated in row-major order with per-cell column mean recomputation (varstat_col_above_mean). All 42 acceptance tests pass.

Chapter 121 covers the Value Co-Occurrence and Adjacency Analysis Pack (Layer 139, co_* prefix): collecting ordered horizontal adjacent pairs via a recursive cooccur_row_adj_ helper (cooccur_h_pairs); collecting ordered vertical adjacent pairs in column-major order via a recursive cooccur_col_adj_ helper (cooccur_v_pairs); down-right diagonal pairs A(R,C)-B(R+1,C+1) (cooccur_d_pairs_dr); down-left diagonal pairs A(R,C)-B(R+1,C-1) (cooccur_d_pairs_dl); directed horizontal count using include/3 on h_pairs (cooccur_count_h); directed vertical count (cooccur_count_v); undirected 4-adjacent count by summing directed V1-V2 and V2-V1 from h+v pairs, treating V1==V2 specially (cooccur_count_adj4); testing that every V1 cell has a V2 4-neighbor via \+cooccur_has_nonadj4_ (cooccur_always_adj4); testing no V1-V2 adjacency via \+cooccur_shared_border (cooccur_never_adj4); finding V cells with no same-V 4-neighbor via \+ (cooccur_isolated4); collecting distinct non-V values bordering V cells via sort (cooccur_border_vals); deterministic shared-edge test with cut (cooccur_shared_border); horizontal transition frequency triples sorted by count descending via msort+reverse (cooccur_row_transitions); and finding the most frequently adjacent value via msort+last on count pairs (cooccur_most_common_adj4). All 42 acceptance tests pass.

Chapter 122 covers the Row and Column Signature Analysis Pack (Layer 140, rs_* prefix): extracting a single column as a top-to-bottom list via findall+member+nth0 (rowsig_col_at); extracting all columns in column order via findall+between+rowsig_col_at (rowsig_all_cols); computing a row frequency table as Row-Count pairs sorted by count descending via sort+include+msort+reverse (rowsig_row_freq); computing a column frequency table (rowsig_col_freq); finding the most frequently occurring row via msort+last on Count-Row pairs, with largest row breaking ties (rowsig_modal_row); finding the modal column (rowsig_modal_col); finding all row indices whose row appears exactly once via include count=1 (rowsig_uniq_rows); finding unique column indices (rowsig_uniq_cols); finding all R1-R2 index pairs where row R1 equals row R2 and R1 < R2 via double between (rowsig_dup_row_pairs); finding duplicate column index pairs (rowsig_dup_col_pairs); testing a row for palindrome structure via reverse(Row)=Row (rowsig_row_palindrome); testing a column for palindrome structure (rowsig_col_palindrome); testing two rows for anagram (same value multiset) via msort(Row1)=msort(Row2) (rowsig_rows_anagram); and testing two columns for anagram (rowsig_cols_anagram). All 47 acceptance tests pass.

Chapter 123 covers the Grid Collection Operations Pack (Layer 141, go_* prefix): finding positions where every grid has a given value via forall (gridops_always); finding positions where no grid has a given value via \+ exists (gridops_never); finding positions where some but not all grids have a given value via sort-deduplicated findall (gridops_sometimes); counting per-cell how many grids have a given value via nested findall+length (gridops_count_v); computing the most frequent value per cell with smallest-value tie-breaking via sort+include+msort+last (gridops_modal); finding cells where all grids agree on the same value via forall+first-grid anchor (gridops_stable); finding disagreement positions via \+member on stable set (gridops_unstable); testing two grids for structural equality via unification (gridops_eq); elementwise integer addition via maplist/4 (gridops_add); elementwise subtraction (gridops_sub); elementwise maximum (gridops_emax); elementwise minimum (gridops_emin); overlaying multiple grids by taking the first non-background value per cell via if-then-else+member (gridops_overlay); and intersecting multiple grids to keep only unanimously non-background values via if-then-else+forall (gridops_intersect). All 42 acceptance tests pass.

Chapter 124 covers the Coordinate-Valued Grid Generation Pack (Layer 142, ix_* prefix): generating a grid where every cell in row R has value R via nested findall+between (index_row_grid); generating a grid where cell(R,C)=C (index_col_grid); diagonal-band index grids where cell(R,C)=R+C (index_sum_grid); anti-diagonal index grids where cell(R,C)=R-C with negative values possible (index_diff_grid); product index grids where cell(R,C)=R*C (index_prod_grid); Manhattan distance fields from a reference point (index_manhattan_grid); Chebyshev distance fields that produce concentric square shells (index_chebyshev_grid); diagonal modular stripe grids via (R+C) mod N (index_mod_grid); horizontal stripe grids via R mod N (index_row_mod_grid); vertical stripe grids via C mod N (index_col_mod_grid); masking rows not in an index list to background using findall(Bg,member(_,OrigRow),Row) for same-length fill (index_mask_rows); masking columns not in an index list to background (index_mask_cols); elementwise binary operations between an index grid and a value grid for Op in {add,sub,mul,max_op,min_op} (index_apply); and signed linear row-major offsets from a reference point using stride W (index_from). All 42 acceptance tests pass.

Chapter 125 covers the Grid Folding, Unfolding, and Fold-Symmetry Detection Pack (Layer 143, fd_* prefix): splitting a grid into top/bottom halves at a horizontal crease row (fold_split_h) or left/right halves at a vertical crease column (fold_split_v); overlaying two same-size grids with background-priority semantics where non-background cells of grid A cover grid B (fold_overlay); folding the bottom half upward with the crease after row R so that output row I overlays Top[I] with Bottom[R-I] (fold_fold_h); folding the right half leftward with the crease after column C so that output col I overlays Left[row][I] with Right[row][C-I] (fold_fold_v); unfolding a half-grid into a symmetric full grid by stacking with its row-reversed copy (fold_unfold_h) or by joining each row with its column-reversed copy (fold_unfold_v); testing whether a grid is symmetric under a fold crease using the between-rows mirror formula row I maps to row 2R+1-I (fold_sym_h) and column J maps to col 2C+1-J (fold_sym_v) which is consistent with the folding predicates; finding the first valid crease row (fold_find_fold_h) or crease column (fold_find_fold_v); finding the first row where all cells equal a marker value V (fold_mark_row) or the first column where all cells equal V (fold_mark_col); and composing a horizontal fold then vertical fold in one call that reduces a grid to its top-left quadrant (fold_fold_both). All 42 acceptance tests pass.

Chapter 126 covers the Grid Rotation and Rotational Symmetry Detection Pack (Layer 144, ro_* prefix): rotating a grid 90 degrees clockwise so each new row C equals column C of the original read bottom-to-top giving a W-by-H output (rotation_rot90); rotating 180 degrees by reversing each row then reversing row order (rotation_rot180); rotating 270 degrees clockwise by reading column W-1-C top-to-bottom for each new row (rotation_rot270); dispatching to any of these by index N in 0..3 with cuts to prevent choicepoints (rotation_rot_n); producing all four rotations as a list of length 4 (rotation_all); finding the lexicographically smallest rotation as a canonical form via msort (rotation_canonical); testing 2-fold rotational symmetry meaning the grid equals its own 180-degree rotation (rotation_is_rot2); testing 4-fold rotational symmetry meaning the grid equals its own 90-degree rotation requiring a square grid (rotation_is_rot4); determining the smallest symmetry order in {1,2,4} by cascading cuts (rotation_sym_order); rotating a list of R-C cell coordinate pairs by N*90 degrees within an H-by-W bounding box using formulas (rotation_rotate_cells): N=1: (R,C)->(C,H-1-R), N=2: (R,C)->(H-1-R,W-1-C), N=3: (R,C)->(W-1-C,R); overlaying a grid with its 180-degree rotation using background-priority semantics (rotation_spin2); overlaying all four rotations in sequence to complete 4-fold-symmetric patterns (rotation_spin4); finding the smallest N in 0..3 such that rotation_rot_n(GridA,N) equals GridB (rotation_match_rotation); and testing whether any rotation of GridA equals GridB (rotation_equiv_rotation). Round-trip properties verified: four 90-degree rotations and two 180-degree rotations each return the original. All 46 acceptance tests pass.

Chapter 127 covers the Warp Pack (Layer 145, wr_* prefix): shifting a single row right by N cells with Bg fill for vacated positions using the formula Out[C]=Row[C-N] (warp_shift_row); shifting a single column down by N cells (warp_shift_col); applying a horizontal shear where row I shifts right by I*Step with Bg fill (warp_shear_h); applying a vertical shear where column J shifts down by J*Step (warp_shear_v); undoing a horizontal shear by negating the step so row I shifts left by I*Step (warp_unshear_h); undoing a vertical shear (warp_unshear_v); cyclically shifting all rows right by N with toroidal wrap-around using (C-N) mod W (warp_cyclic_h); cyclically shifting all columns down by N using (R-N) mod H (warp_cyclic_v); applying a cyclic horizontal shear where row I wraps right by I*Step (warp_cyclic_shear_h); applying a cyclic vertical shear where column J wraps down by J*Step (warp_cyclic_shear_v); applying per-row arbitrary offsets where row I shifts right by Offsets[I] generalizing the shear (warp_skew_offsets); computing the anti-diagonal transpose where Out[W-1-C][H-1-R]=Grid[R][C] giving a W-by-H output and being an involution on square grids (warp_transpose_anti); finding the step in [-(W-1),W-1] such that warp_shear_h maps GridA to GridB (warp_find_shear_h); and finding the step in [-(H-1),H-1] such that warp_shear_v maps GridA to GridB (warp_find_shear_v). SWI-Prolog mod handles negative values correctly so cyclic predicates need no special-case for negative shift amounts. All 42 acceptance tests pass.

Chapter 128 covers the Border Pack (Layer 146, br_* prefix): the central ring depth formula min(R, H-1-R, C, W-1-C) assigns each cell to a concentric ring where ring 0 is the outermost border and ring N is N steps inward; the maximum ring index for an H by W grid is (min(H,W)-1)//2; enumerating R-C pairs in ring N in row-major order using evaluated bounds H1 is H-1 and W1 is W-1 before between/3 calls (border_ring_cells); collecting the grid values at those positions (border_ring_vals); determining the uniform color of ring N with sort/2 for a singleton check failing if non-uniform (border_ring_color); testing that ring N has a single uniform color (border_is_uniform_ring); adding one layer of V cells around a grid to produce an (H+2) by (W+2) output using findall with append to prepend and append V to each row (border_add_border); removing the outermost ring to produce a (H-2) by (W-2) inner grid returning the empty list for grids too small to have an interior (border_strip_border); stripping N rings by recursive application of border_strip_border with N=0 as the identity base case (border_inner_n); querying the uniform color of ring 0 only (border_outer_color); testing that ring 0 is uniform (border_is_uniform_outer); collecting the list of uniform ring colors from outside in stopping at the first non-uniform ring (border_ring_colors); returning the maximum ring index (min(H,W)-1)//2 (border_max_ring); testing that every ring from 0 to border_max_ring is uniform forming a concentric bullseye pattern using forall/2 (border_is_nested); building a depth-map grid where each cell contains its ring index using nested findall with evaluated bounds (border_depth_map); and replacing all cells in ring N with a new value V while keeping non-ring cells unchanged via a private helper that checks ring depth equality (border_fill_ring). All 42 acceptance tests pass.

Chapter 129 covers the Splice Pack (Layer 147, sp_* prefix): structural editing of 2D grid rows and columns that reshapes the grid by changing the number of rows or columns rather than modifying cell values; the central split-and-reassemble pattern uses length(Prefix, K) and append(Prefix, Suffix, List) to split a list at position K then reassembles with the modified element; inserting a new row before row index R where R=0 prepends and R=H appends (sp_insert_row); deleting the row at index R (sp_delete_row); inserting a column of constant value V before column index C by applying a private helper to every row via maplist (sp_insert_col); deleting column C from every row (sp_delete_col); swapping two rows by index using findall with between enumeration and conditional assignment leaving the grid unchanged when R1=R2 (sp_swap_rows); swapping two columns by applying a per-row private helper that enumerates column indices with between (sp_swap_cols); reversing the order of rows with reverse/2 (sp_reverse_rows); reversing cells within each row with maplist(reverse) (sp_reverse_cols); cyclically shifting rows by K positions where K is taken modulo H so K=H returns the original grid (sp_rotate_rows); cyclically shifting columns by K positions via a per-row helper (sp_rotate_cols); replacing row R with N copies where N=1 is identity and N=0 deletes the row by splitting with length/append building N copies with maplist(=(Row)) and reassembling (sp_replicate_row); replacing column C with N copies via a per-row helper using the same split-copy-reassemble pattern (sp_replicate_col); keeping only rows at given indices in the specified order using findall with member/2 allowing repetition and omission (sp_select_rows); and keeping only columns at given indices via a per-row helper (sp_select_cols). All 42 acceptance tests pass.

Chapter 130 covers the Objop Pack (Layer 148, oo_* prefix): object-level manipulation within the full 2D grid representation where objects are identified by their color value and all operations accept and return complete grids rather than sparse cell lists; collecting all R-C positions with a given color as a sorted list using findall with between and nth0 (objop_cells_of); computing the bounding box (R0,C0,R1,C1) of a cell list by extracting all row indices with findall and taking min_list and max_list (objop_bbox); counting cells of a given color by collecting them and taking the list length (objop_count); computing bounding box height H and width W from the bbox corners (objop_size); computing the bounding box center row and column via integer division (objop_center); the central private helper objop_set_cells_ doing a full H-by-W grid scan using nested findall with between and nth0, replacing listed positions with a given value using memberchk and an if-then-else (objop_set_cells_); replacing all V cells with a background color (objop_erase); recoloring all V cells to a new value (objop_repaint); exchanging two non-overlapping colors by first setting all V1 positions to V2 then setting the original V2 positions to V1 via two sequential objop_set_cells_ calls (objop_swap); translating all V cells by (DR,DC) with in-bounds clipping by computing new positions and filtering with between on the grid dimensions (objop_move); copying V cells to an offset position without erasing the originals by painting only the offset positions (objop_copy); rotating V cells 90 degrees CW around the bbox top-left using normalized formula (r,c) to (c,H-1-r) where r=R-R0 and c=C-C0 (objop_rotate90); rotating 180 degrees with normalized formula (r,c) to (H-1-r,W-1-c) (objop_rotate180); flipping V cells left-right within the bbox using normalized column c to W-1-c (objop_mirror_h); and flipping top-bottom using normalized row r to H-1-r (objop_mirror_v). All 42 acceptance tests pass.

Chapter 131 covers the Pair Pack (Layer 149, pr_* prefix): object pairing and scene correspondence operating on obj(Color, Cells) terms where Cells is a sorted list of r(R,C) terms as produced by the scene pack; the central innovation is the D4-canonical shape computed by normalizing the cell list to the origin then generating all 8 D4 transforms (identity, 90 CW, 180, 270 CW, horizontal flip, vertical flip, main diagonal transpose, anti-diagonal transpose) and taking the lexicographically smallest result under standard Prolog term order so that a horizontal pair and a vertical pair have the same canonical and all rotations and reflections of a shape are identified as equivalent; extracting the color from an obj term (pair_obj_color); counting the cells in an obj term (pair_obj_size); testing D4-equivalent shapes (pair_shape_eq); testing same color (pair_color_eq); testing same cell count (pair_size_eq); grouping a list of obj terms by color as sorted Color-[Obj] pairs using two-phase findall with sort for distinct keys and then findall per key (pair_group_color); grouping by cell count (pair_group_size); grouping by D4-canonical shape where objects with rotationally or reflectively equivalent shapes land in the same group (pair_group_shape); finding the unique obj whose color appears exactly once using group_color and filtering for singleton groups returning the single element or failing if not exactly one (pair_unique_color); finding the unique obj whose size appears exactly once (pair_unique_size); finding Color-Obj1-Obj2 triples across two scene lists where both objects share the same color via cross-product findall (pair_match_color); finding N-Obj1-Obj2 triples for same-size pairs (pair_match_size); and finding Shape-Obj1-Obj2 triples for D4-equivalent shape pairs where a horizontal pair in scene 1 matches a vertical pair in scene 2 (pair_match_shape). All 42 acceptance tests pass.

Chapter 132 covers the Arrange Pack (Layer 150, ag_* prefix): arrangement-level reasoning over obj(Color, Cells) terms where all positional predicates use the integer floor-average centroid (sum of row indices // cell count, sum of col indices // cell count) as the representative point for each object; computing the centroid of an obj term (ag_centroid); computing the displacement vector from one centroid to another (ag_offset); testing that one object's centroid row is at most another's (ag_row_order); testing centroid column order (ag_col_order); testing that two objects share the same centroid row (ag_row_aligned); testing that two objects share the same centroid column (ag_col_aligned); sorting an obj list by centroid row using keysort for stability so equal-row objects preserve their original relative order (ag_sort_by_row); sorting by centroid column (ag_sort_by_col); computing consecutive gaps between sorted distinct centroid rows by first deduplicating with sort then applying ag_consec_gaps_ (ag_row_gaps); computing consecutive centroid column gaps (ag_col_gaps); testing that all consecutive centroid row gaps are equal requiring at least two objects with distinct rows (ag_equal_row_gaps); testing equal column gaps (ag_equal_col_gaps); computing the bounding box of all cells of all objs in a list where each obj may be multi-cell (ag_group_bbox); and finding the obj in a list whose centroid is closest to a reference obj under Manhattan distance by building distance-keyed pairs and using keysort to find the minimum (ag_nearest). Private helper ag_consec_gaps_ has a cut on its singleton clause to prevent choicepoint warnings. All 42 acceptance tests pass.

Chapter 133 covers the Xform Pack (Layer 151, xf_* prefix): object-level transformation application and transformation inference operating on obj(Color, Cells) terms; applied transformations: xform_recolor (replace color keeping same cells; implemented as single head-unification clause), xform_translate (shift all cells by r(DR,DC); findall+sort), xform_normalize (translate obj to origin by subtracting min row and min col; findall+sort), xform_d4 (apply named D4 op id/r90/r180/r270/fh/fv/fd1/fd2 to obj; normalize then apply xform_cell_d4_ with H1=max row and W1=max col of normalized cells then sort then translate back to original top-left corner so transformation preserves position anchor); identity and offset: xform_same_cells (same sorted cell set by shared-variable unification; zero-overhead), xform_cell_offset (normalize both objs, require normalized cell sets to unify meaning same shape, return DR and DC as differences of bbox top-left corners), xform_is_recolor (same cells by shared variable, C1 \= C2 for different colors); cell-set arithmetic: xform_cells_added (subtract(C2,C1) from library(lists)), xform_cells_removed (subtract(C1,C2)), xform_cells_kept (findall+memberchk for intersection), xform_overlap_count (length of kept cells); inference: xform_any_d4 (try all 8 D4 ops via member, apply to normalized Obj1 cells, sort, unify with normalized Obj2 cells, cut after first match to return the first matching op deterministically; id is checked first so identity is found immediately for equal shapes), xform_scale_factor (integer divisibility check: Len2 mod Len1 =:= 0 then N=Len2//Len1 >= 1), xform_merge (append two cell lists then sort/2 which deduplicates; colors must unify). Private helper xform_cell_d4_ is defined locally with same 8-case D4 formula as in the pair pack. All 42 acceptance tests pass.

Chapter 134 covers the Query Pack (Layer 152, qu_* prefix): aggregate statistics and queries over lists of obj(Color, Cells) terms produced by the scene pack where all 14 predicates are deterministic and operate on collections of objects rather than individual objects; counting objs by color attribute as sorted Color-N pairs using two-phase findall+sort pattern where all color values are collected then deduplicated with sort then a count is computed for each distinct key with a nested findall (query_count_by_color); counting by cell count as sorted Size-N pairs with the same two-phase pattern (query_count_by_size); counting by origin-normalized form where "form" means the cell list translated so min row=0 and min col=0 then sorted which is an exact structural match not D4-invariant so a horizontal bar and vertical bar have different forms even though they are D4-equivalent shapes (query_count_by_form); finding the color with the highest occurrence count with ties broken by smallest color value because the Color-N list is sorted by color so member+cut finds the smallest color at the target count (query_most_frequent_color); finding the color with the lowest occurrence count with same tie-breaking (query_least_frequent_color); finding the obj with the most cells with ties broken by first in input order because findall preserves argument order so member+cut finds the first occurrence at the maximum (query_largest_obj); finding the obj with the fewest cells with same tie-breaking (query_smallest_obj); summing the cell counts of all objs in the list using findall to collect lengths then sum_list (query_total_cells); floor-average cell count per obj using query_total_cells divided by length(Objs) with integer division // (query_avg_size); testing that all objs share a single color value using findall all colors then sort/2 deduplication then require singleton [_] (query_all_same_color); testing that all objs share a single cell count with the same singleton pattern (query_all_same_size); testing that all objs share the same origin-normalized form using the same singleton pattern applied to normalized forms (query_all_same_form); returning the sorted list of distinct color values present in the obj list using findall+sort/2 (query_colors); and returning the sorted list of distinct cell counts using findall+sort/2 (query_sizes). All 42 acceptance tests pass.

Chapter 135 covers the Sift Pack (Layer 153, si_* prefix): object list filtering by attribute predicates where all 14 predicates accept an input Objs list and return Out as the subset of objects satisfying the given condition and an empty result list is always valid; color exact match by head-unification (si_by_color) keeping only objs where the Color head argument equals the given value; color exclusion using C \= Color (si_not_color); exact cell count match using length/2 (si_by_size); origin-normalized form match where form is the cell list translated to min-row=0 min-col=0 and sorted which is an exact structural match not D4-invariant using a locally defined si_norm_ helper (si_by_form); strict cell count greater-than filter (si_larger_than) and less-than filter (si_smaller_than); membership in a provided color list using memberchk/2 (si_color_in) and its complement (si_color_not_in); extreme size filters that return ALL tied objects unlike the query pack which returns only the first: all objs tied for maximum cell count via two-phase collect-max-then-filter (si_max_size), all objs tied for minimum cell count (si_min_size); color frequency filters using the two-phase count pattern from the query pack then filtering for colors with count=1 (si_unique_color) or count>1 (si_shared_color); and grid border filters requiring H and W grid dimensions where the border is row=0, row=H-1, col=0, col=W-1 detected by a four-clause helper si_on_border_ with cuts: keeping objs with any border cell using member and si_on_border_ (si_on_border), and keeping objs with no border cell using negation-as-failure \+ (member + si_on_border_) (si_off_border). All 42 acceptance tests pass.

Chapter 136 covers the Pigment Pack (Layer 154, pg_* prefix): bulk color operations on object scenes where the entire list of obj(Color, Cells) terms is treated as the unit of manipulation; uniform recoloring of all objs to a fixed color using findall with the color argument in the output head (pigment_recolor_all); single-color replacement using if-then-else (C=:=From -> obj(To,Cells) ; obj(C,Cells)) inside findall (pigment_recolor_one); bilateral color swap using nested if-then-else to handle two target colors (pigment_swap); lenient color table application where objs not in the table keep their original color using memberchk with if-then-else (pigment_apply_table); strict color table application that excludes objs not in the table treating it as a combined filter-and-transform (pigment_apply_table_strict); color table inference from two scenes with identical cell structures where each pair of obj terms with the same cell set records an Old-New color pair then sort/2 deduplicates (pigment_infer_table); zip-based recoloring that pairs each obj with the corresponding color from a list stopping at the shorter list using a three-clause recursive predicate with cuts on both base cases (pigment_zip_recolor); majority color targeting where the private helper pigment_count_by_color_ computes color counts then max_list finds the max count then member+cut finds the smallest majority color then if-then-else recolors (pigment_majority_to); minority color targeting using min_list (pigment_minority_to); unique color targeting where colors with count=1 are found and objs with those colors change (pigment_unique_to); shared color targeting where colors with count>1 are found (pigment_shared_to); table inversion by swapping From-To pairs (pigment_invert_table); extracting sorted distinct From colors from a table (pigment_table_from); and consistency verification using negation-as-failure to detect contradictory mappings (pigment_consistent). All 42 acceptance tests pass.

Chapter 137 covers the Delta Pack (Layer 155, dl_* prefix): scene-level delta analysis that compares two object scenes (lists of obj(Color, Cells) terms) and answers what changed; object matching uses exact cell-set equality via Prolog unification so two objects match iff they have identical Cells lists regardless of color; delta_added/3 collects objects in S2 whose cell set appears nowhere in S1 using findall+negation-as-failure (\+ member(obj(_, Cells), S1)); delta_removed/3 symmetrically collects objects in S1 absent from S2; delta_matched/3 collects O1-O2 pairs sharing identical cell sets by binding Cells from O1 then unifying it into O2 inside findall then deduplicating with sort; delta_recolored/3 collects C1-C2 color change pairs for matched objects where C1 \= C2 then sort deduplicates; delta_unchanged/3 collects objects verbatim in both scenes using member(O, S1), member(O, S2) which requires full term equality (same color and same cells); delta_color_gain/3 and delta_color_loss/3 compute set differences on sorted color palettes using findall+sort then findall with \+ member; delta_count_diff/3 computes length(S2) - length(S1) directly; delta_size_diff/3 sums cell counts in each scene using findall+length then sum_list and subtracts; delta_is_added_only/2, delta_is_removed_only/2, delta_is_recolor_only/2, and delta_is_stable/2 are deterministic tests that compose the other predicates by checking that the appropriate subsets are empty lists; delta_scene_diff/3 is the composite predicate that returns all four components as the compound term delta(Added, Removed, Recolored, Unchanged). All 42 acceptance tests pass.

Chapter 138 covers the Group Pack (Layer 156, gp_* prefix): object grouping and partition where a list of obj(Color, Cells) terms is split into Key-[Objs] groups sharing a common property and the groups are represented as sorted Key-[Objs] pairs using the -/2 functor; partition predicates: gp_by_color/2 (colors as keys via two-phase findall+sort collecting colors then grouping), gp_by_size/2 (cell counts as keys via length/2 in two-phase pattern), gp_by_row/2 (minimum row of cells as key via nested findall+min_list), gp_by_col/2 (minimum column via nested findall+min_list), gp_by_form/2 (origin-normalized cell list as key via private gp_norm_ helper using the same min-row/min-col translation and sort as query and sift packs); inspection predicates: gp_size_of/2 (length of groups list), gp_flatten/2 (findall over member(_-Grp,Groups)+member(O,Grp)), gp_group_sizes/2 (findall+sort of group lengths), gp_all_same_size/1 (base case [] with cut + general case: collect lengths, sort to singleton [_]), gp_keys/2 (findall+sort of all keys); extrema predicates: gp_largest_group/2 (N-K pairs then max_list + member+cut then memberchk for final lookup to avoid choicepoint) and gp_smallest_group/2 (min_list version); filter predicates: gp_singleton_groups/2 (findall with length=1 check) and gp_shared_groups/2 (findall with N>1 check). All 42 acceptance tests pass.

Chapter 139 covers the Proximity Pack (Layer 157, px_* prefix): object-level proximity and distance computation for lists of obj(Color, Cells) terms where all distance measures use Manhattan distance (sum of absolute row and column differences); proximity_centroid/3 computes the integer-truncated centroid (R, C) of an obj by summing row values (sum_list/2), summing column values, dividing each by the cell count with integer truncation (//); proximity_centroid_dist/3 computes the Manhattan distance between two obj centroids; proximity_min_cell_dist/3 finds the minimum Manhattan distance across all (cell1, cell2) pairs from two objects' cell lists using findall over member on each list then min_list; proximity_touching/2 succeeds if any cell of O1 is 4-adjacent (Manhattan distance = 1) to any cell of O2 using member on both cell lists then =:= 1 check with a cut; proximity_nearest/3 and proximity_farthest/3 find the object with minimum or maximum centroid distance from a reference using findall of D-O pairs then min_list/max_list then member+cut for tie-breaking by list order; proximity_sort_by_dist/3 uses findall of D-O pairs then msort (stable sort) then findall to extract objects in order; proximity_within_dist/4 and proximity_beyond_dist/4 filter by centroid distance using =< and > comparisons inside findall; proximity_closest_pair/2 and proximity_farthest_pair/2 generate all index-ordered pairs (i < j) using between(1,N,I) then I1 is I+1 then between(I1,N,J) with nth1/3 for access, compute all distances, find min/max, take the first matching triple then retrieve objects by index; proximity_touching_objs/3 collects objects passing proximity_touching/2 and proximity_non_touching_objs/3 collects objects failing it via \+ proximity_touching; proximity_dist_rank/3 produces D-Obj pairs via msort for stable ascending order. All 51 acceptance tests pass.

Chapter 140 covers the Link Pack (Layer 158, lk_* prefix): object-to-object correspondence linking where a link is an O1-O2 pair and a links list is a list of such pairs; lk_by_position/3 zips two object lists element-wise using a three-clause recursive predicate (base cases for empty first and empty second lists with cuts, recursive case pairs heads and recurses on tails); lk_by_nearest/3 links each O1 to its centroid-nearest O2 using a private lk_nearest_/3 helper that findalls D-O pairs from centroid distances then min_list + member+cut to pick the nearest; lk_by_color/3 uses findall over the Cartesian product checking color atom equality (O1=obj(C,_), O2=obj(C,_) with the same C) then sort to deduplicate; lk_by_size/3 uses findall over Cartesian product with length/2 equality then sort; lk_by_form/3 uses the private lk_norm_/2 helper (same min-row/min-col translation + sort as in group and proximity packs) to normalize each object's cells then checks normalized-form equality in findall then sort; lk_source/2 and lk_target/2 use findall member(O1-_, Links) and member(_-O2, Links); lk_invert/2 uses findall member(O1-O2, Links) yielding O2-O1; lk_count/2 uses length/2; lk_apply_color/2 uses findall member(obj(_,Cells1)-obj(C2,_), Links) yielding obj(C2,Cells1); lk_apply_cells/2 uses findall member(obj(C1,_)-obj(_,Cells2), Links) yielding obj(C1,Cells2); lk_filter_same_color/2 uses findall with O1=obj(C,_), O2=obj(C,_) pattern; lk_filter_diff_color/2 uses C1 \= C2 check; lk_unlinked/3 uses findall with \+ member(O-_, Links) negation-as-failure. All 53 acceptance tests pass.

Chapter 141 covers the Layout Pack (Layer 159, lt_* prefix): multi-object layout analysis for a list of obj(Color, Cells) terms; layout_global_bbox/5 collects all r(R,_) row values and r(_,C) col values from all cells of all objects via nested findall over member, then min_list/max_list to get the four corners; layout_bbox_area/2 computes (R2-R1+1)*(C2-C1+1) from the global bbox; layout_row_range/3 and layout_col_range/3 compute min-row or min-col of each object via nested findall+min_list, then min_list/max_list on the resulting list; layout_all_same_row/1 and layout_all_same_col/1 have a base case [] with cut and a general case that collects topmost rows or leftmost cols then sort to singleton [_] for uniqueness; layout_centroid_of_all/3 computes each object's centroid via private layout_centroid_ helper (sum_list+integer division), collects all centroid rows and cols, then sum_list+// to average; layout_row_count/2 and layout_col_count/2 collect topmost rows or leftmost cols then sort for distinct values then length; layout_is_grid/3 collects R-C centroid pairs, sorts for distinct rows and cols, verifies every combination is present with \+ (member(R,...), member(C,...), \+ member(R-C,...)) then checks |Objs|=Rows*Cols; layout_is_diagonal_dr/1 collects R-C values for each centroid then sort to singleton; layout_is_diagonal_dl/1 collects R+C values; layout_gap_h/2 collects C-O pairs sorted by centroid col via msort, extracts cols, computes consecutive diffs via private layout_diffs_ recursive predicate, then verifies all diffs equal and > 0; layout_gap_v/2 is the row analog. All 56 acceptance tests pass.

Chapter 142 covers the Object Symmetry Analysis Pack (objsym, os_* prefix, Layer 170): testing D4 rotational and reflectional symmetry of a single obj term; os_is_symmetric_d4 checks all 8 dihedral symmetries; os_symmetry_axes identifies which of the four standard axes (horizontal, vertical, main diagonal, anti-diagonal) a shape is symmetric about; os_is_rotationally_symmetric checks 90-degree and 180-degree rotational symmetry separately; os_normalize_d4 computes the lexicographically-smallest orientation under all 8 D4 transforms for canonical form comparison. All 42 acceptance tests pass.

Chapter 143 covers the Object Shape Classification Pack (objbound, ob_* prefix, Layer 171): computing bounding boxes, shape aspect ratios, and shape class labels for obj(Color,Cells) terms; ob_bbox computes the four-corner bounding box; ob_aspect_ratio gives integer height-to-width ratio; ob_is_square tests that the bounding box is square; ob_shape_class returns a label (dot, line_h, line_v, square, tall, wide) based on bbox geometry. All 42 acceptance tests pass.

Chapter 144 covers the Object Pair Relation Pack (objrel, or_* prefix, Layer 172): spatial relation testing between pairs of obj(Color,Cells) terms using bounding box and centroid comparisons; or_above, or_below, or_left_of, or_right_of test strict bounding box order; or_touching tests 4-adjacency at the cell level; or_contained_in tests full cell containment; or_overlapping tests shared cells; or_same_shape tests D4-canonical form equality. All 42 acceptance tests pass.

Chapter 145 covers the Object List Filtering Pack (objfilter, of_* prefix, Layer 173): filtering and selecting subsets of obj(Color,Cells) lists by attribute predicates; objfilter_by_color, objfilter_not_color select by exact color; of_by_size, of_larger_than, of_smaller_than filter by cell count; objfilter_max_size and objfilter_min_size return all objects tied for the extremal size; of_on_border and of_off_border filter by grid edge contact; of_by_form filters by origin-normalized shape. All 42 acceptance tests pass.

Chapter 145 (canvas) covers the Grid Canvas and Object Rendering Pack (canvas, cv_* prefix, Layer 174): creating blank grids and rendering obj(Color,Cells) terms onto them; canvas_blank creates an H-by-W grid filled with a background color; cv_paint_obj renders one obj onto a grid; cv_paint_scene renders all objects in a list; canvas_render is the full pipeline from scene and dimensions to output grid. All 42 acceptance tests pass.

Chapter 146 covers the Object Sequence Analysis Pack (objseq, oq_* prefix, Layer 175): detecting arithmetic, geometric, and positional progressions in an ordered list of obj(Color,Cells) terms; oq_size_diffs computes consecutive size differences; oq_is_arithmetic_size detects constant-difference size sequences; oq_row_diffs and oq_col_diffs compute consecutive centroid displacements; oq_is_uniform_stride detects constant-displacement motion paths; oq_next_in_sequence extrapolates the next object under a detected progression. All 42 acceptance tests pass.

Chapter 147 covers the Object Delta Pack (objdelta, dp_* prefix, Layer 176): analyzing and applying change between two matched obj(Color,Cells) pairs; objdelta_color_delta extracts the color change; objdelta_size_delta extracts the cell count change; dp_cell_delta extracts added and removed cells; dp_apply applies a delta term (recolor, add_cells, remove_cells, composite) to an obj; dp_infer_delta infers the minimal delta term explaining the Before-After pair. All 42 acceptance tests pass.

Chapter 148 covers the Object Tiling Pack (objcopy, tc_* prefix, Layer 177): creating multiple shifted copies of an obj(Color,Cells) term to fill a row, column, or rectangular region; tc_tile_row produces N evenly-spaced horizontal copies with given column stride; tc_tile_col produces N vertical copies with given row stride; tc_tile_grid produces an M-by-N grid of copies; tc_fill_row packs copies to fill an H-by-W region without overlap. All 42 acceptance tests pass.

Chapter 149 covers the Object Correspondence Pack (objmatch, mx_* prefix, Layer 178): establishing correspondences between two lists of obj(Color,Cells) terms; objmatch_by_color matches pairs sharing the same color; objmatch_by_size matches pairs sharing the same cell count; objmatch_by_form matches pairs sharing the same D4-canonical shape; mx_nearest matches each object in the first list to its centroid-nearest counterpart in the second list; objmatch_zip pairs objects by list position. All 53 acceptance tests pass.

Chapter 150 covers the Object Merge Pack (objmerge, mg_* prefix, Layer 179): combining, splitting, and set operations on obj(Color,Cells) terms and lists; mg_union merges the cell lists of two same-color objs; mg_intersect computes shared cells; mg_subtract removes one obj's cells from another; mg_split_components decomposes a multi-cell obj into its 4-connected components; mg_merge_scene combines all objs of the same color into a single obj. All 42 acceptance tests pass.

Chapter 151 covers the Object Attribute Aggregate Pack (objattr, oa_* prefix, Layer 180): computing aggregate statistics (min, max, mean, sum, histogram) over an attribute of a list of obj(Color,Cells) terms; oa_sizes collects all cell counts; oa_min_size, oa_max_size, oa_sum_sizes, oa_mean_size derive summary statistics; oa_color_hist produces a sorted Color-Count histogram; oa_unique_colors returns the distinct sorted color list; oa_size_hist produces a Size-Count histogram. All 42 acceptance tests pass.

Chapter 152 covers the Object Grouping Pack (objgroup, og_* prefix, Layer 181): partitioning obj(Color,Cells) lists into Key-[Objs] groups sharing a common attribute; objgroup_by_color groups by color atom; objgroup_by_size groups by cell count; objgroup_by_form groups by D4-canonical shape; objgroup_by_row groups by topmost row; objgroup_by_col groups by leftmost column; og_singleton_groups returns groups with exactly one member; og_shared_groups returns groups with more than one member. All 42 acceptance tests pass.

Chapter 153 covers the Scene Comparison Pack (scenecmp, sm_* prefix, Layer 182): comparing two obj(Color,Cells) lists as scenes; sm_same_n tests equal object count; scenecmp_same_colors tests equal sorted color palettes; sm_same_sizes tests equal sorted cell-count lists; scenecmp_same_forms tests equal sorted D4-canonical shape lists; sm_added and sm_removed identify objects present in one scene but absent from the other by cell-set matching; sm_recolored identifies matched objects whose colors changed. All 42 acceptance tests pass.

Chapter 154 covers the Object Locate Pack (objlocate, lq_* prefix, Layer 183): querying a list of obj(Color,Cells) terms relative to a reference object; objlocate_above, objlocate_below, objlocate_left_of, objlocate_right_of filter objects by strict bounding box order relative to a reference; lq_touching filters to objects 4-adjacent to the reference; lq_within_dist filters by Manhattan centroid distance threshold; objlocate_nearest and objlocate_farthest return the single closest or farthest object by centroid distance. All 42 acceptance tests pass.

Chapter 155 covers the Scene Transform Pack (scenexf, sx_* prefix, Layer 184): applying a single named transformation uniformly to all objects in an obj(Color,Cells) list; scenexf_recolor_all recolors every object to a fixed color; sx_translate_all shifts every centroid by (DR,DC); sx_d4_all applies a D4 isometry to every object; sx_scale_all scales every object by integer factor N; sx_filter_apply applies a predicate to select objects and transforms only those. All 42 acceptance tests pass.

Chapter 156 covers the Rule Inference Pack (ruleinfer, ri_* prefix, Layer 185): inferring a named transformation rule term from a list of Before-After obj-list pairs; ruleinfer_infer_recolor detects a consistent single-color-to-single-color mapping across all pairs; ruleinfer_infer_shift detects a consistent translation offset; ri_infer_d4 detects a consistent D4 isometry; ri_infer_scale detects a consistent integer scale factor; ri_infer tries all strategies in order and returns the first consistent rule found. All 42 acceptance tests pass.

Chapter 157 covers the Scene Apply Pack (sceneapply, sa_* prefix, Layer 186): evaluating named rule terms against obj(Color,Cells) lists; sceneapply_apply dispatches on the rule functor (recolor, translate, d4, scale, identity, and composites) and returns the transformed scene; sceneapply_verify checks that sceneapply_apply on Before exactly yields After for a rule; sa_coverage counts the number of pairs for which sceneapply_verify holds; sa_accurate tests that a rule achieves perfect coverage across all training pairs. All 42 acceptance tests pass.

Chapter 158 covers the Conditional Transform Pack (condxf, xc_* prefix, Layer 187): applying transformations selectively to objects matching a predicate; xc_when applies a rule to every object satisfying a guard predicate and leaves others unchanged; xc_unless applies the rule to objects NOT satisfying the guard; xc_map applies different rules to different color groups; xc_partition splits a scene into matched and unmatched objects then applies different rules to each partition. All 42 acceptance tests pass.

Chapter 159 covers the scenepair Pack (ps_* prefix, Layer 188): holistic Before-After scene pair analysis computing scene-level observables for transformation rule hypothesis ranking; scenepair_same_n_objs tests equal object counts; scenepair_added_objs and scenepair_removed_objs detect added and removed objects; scenepair_color_set_before and scenepair_color_set_after extract sorted color palettes; scenepair_color_delta identifies recolored matched objects; scenepair_is_recolor, scenepair_is_shift, scenepair_is_to_origin, and scenepair_size_preserved test for common transformation patterns. All 41 acceptance tests pass.

Chapter 160 covers the scenerank Pack (rk_* prefix, Layer 189): ranking candidate rule terms by coverage across Before-After training pairs; rk_coverage counts how many pairs a rule correctly transforms; rk_consistent tests that a rule achieves perfect coverage; rk_rank_rules sorts candidates by descending coverage; rk_best_rule returns the highest-coverage candidate; rk_perfect_rules returns all candidates with coverage equal to the pair count; rk_top_n_rules returns the N highest-coverage candidates as Coverage-Rule pairs. All 41 acceptance tests pass.

Chapter 161 covers the colortable Pack (ct_* prefix, Layer 190): color substitution table learning and application for obj(Color,Cells) scenes; colortable_infer_from_pair learns a From-To color map from one Before-After scene pair; colortable_merge_maps combines evidence from multiple maps; colortable_learn_map infers a consistent table from a list of pairs; colortable_apply_map applies a learned table to a new scene; ct_consistent tests that a table has no contradictory mappings; ct_invert reverses all From-To entries. All 44 acceptance tests pass.

Chapter 162 covers the gridsolve Pack (gs_* prefix, Layer 191): end-to-end scene puzzle solver integrating rule inference, ranking, and application; gridsolve_solve tries all candidate rule types, ranks by coverage, and applies the best rule to a test scene; gridsolve_coverage counts training pairs explained by a rule; gridsolve_consistent tests perfect coverage; gridsolve_apply transforms a scene using a named rule term; gs_accuracy returns the fraction of pairs perfectly explained. All 41 acceptance tests pass.

Chapter 163 covers the transformgen Pack (tg_* prefix, Layer 192): systematic generation of scene transformation rule candidates; transformgen_scene_colors extracts the sorted distinct color set; transformgen_recolor_candidates generates all pairwise recolor(F,T) candidates; transformgen_recolor_all_candidates generates recolor_all(T) candidates; transformgen_shift_candidates generates shift(DR,DC) candidates within a search radius; tg_d4_candidates generates all 8 D4 isometry candidates; tg_candidates combines all candidate types for ranking. All 42 acceptance tests pass.

Chapter 164 covers the multicolor Pack (mc_* prefix, Layer 193): multi-color scene analysis with frequency counting, partitioning, and color-indexed queries; multicolor_color_counts computes a sorted Color-Count histogram; multicolor_count_of_color returns the count for one color; multicolor_color_present tests that at least one object has a given color; multicolor_majority_color returns the most frequent color; mc_minority_color returns the least frequent; mc_unique_colors returns colors with exactly one object; mc_bicolor tests that exactly two distinct colors are present. All 42 acceptance tests pass.

Chapter 165 covers the sceneinv Pack (si_* prefix, Layer 194): scene invariant detection across training pairs; si_n_before and si_n_after compute the object count present in all Before or all After scenes respectively; si_colors_before and si_colors_after find colors invariant across all scenes; si_size_invariant tests that total cell counts are constant; si_palette_invariant tests that the sorted color set is identical across all scenes; si_count_invariant tests that object counts are constant. All 46 acceptance tests pass.

Chapter 166 covers the seqinfer Pack (sq_* prefix, Layer 195): sequential rule inference searching for a multi-step transformation sequence; sq_apply applies an ordered list of rule terms to a scene in sequence; sq_verify tests that applying a sequence to every Before scene yields the corresponding After; sq_infer searches over short sequences of candidates to find one that passes sq_verify; sq_coverage counts pairs explained by a sequence; sq_score returns the fraction of pairs the sequence explains. All 44 acceptance tests pass.

Chapter 167 covers the gridquery Pack (gq_* prefix, Layer 196): query and manipulation operations on raw grids (list of rows, each a list of color atoms); gridquery_size returns height and width; gridquery_at reads a cell value by index; gq_majority returns the most frequent color; gq_count_color counts occurrences of a given color; gq_crop extracts a sub-grid by bounding box; gridquery_replace substitutes one color for another throughout; gq_hflip and gq_vflip mirror the grid; gridquery_transpose exchanges rows and columns; gq_rotate90 rotates clockwise. All 45 acceptance tests pass.

Chapter 168 covers the gridparse Pack (gp_* prefix, Layer 197): conversion between raw grid format and the obj(Color,Cells) scene representation; gp_bg_color identifies the background as the most frequent color; gp_grid_cells collects all r(R,C) positions of a given color; gp_color_components performs 4-connected BFS flood fill into connected components; gp_grid_to_scene converts a raw grid to an obj list; gp_scene_to_grid renders a scene back to a raw grid; gp_grid_equal tests structural grid equality; gp_n_objects, gp_largest_object, gp_smallest_object, and gp_objects_by_size provide object statistics. All 43 acceptance tests pass.

Chapter 169 covers the gridtask Pack (gt_* prefix, Layer 198): end-to-end raw-grid task solving without obj conversion; gridtask_diff_cells and gridtask_n_changed compute cell-level differences; gridtask_bg_color identifies the background; gridtask_infer_color_map learns a consistent From-To color map from one Before-After pair including identity pairs; gridtask_color_map_pairs infers a consistent map from multiple pairs; gridtask_is_identity, gridtask_is_scale, and gridtask_infer_shift detect the identity, integer-scale, and translation strategies; gridtask_pair_score computes pixel accuracy; gridtask_solve tries all four strategies and applies the best to the test grid. All 51 acceptance tests pass.

Chapter 170 covers the gridnbr Pack (gn_* prefix, Layer 199): cell adjacency, morphological operations, and neighbor counts on raw grids; gridnbr_nbr4 and gridnbr_nbr8 collect 4- and 8-connected neighbor values omitting out-of-bounds positions; gridnbr_count4 and gridnbr_count8 count a given color among neighbors; gridnbr_count4_grid and gridnbr_count8_grid produce a grid of neighbor counts; gridnbr_any4 tests for at least one Color neighbor using memberchk (deterministic); gridnbr_all4 tests that all 4-neighbors are Color (fails on cells with no neighbors); gridnbr_mark_border replaces each non-background cell touching a background 4-neighbor with the atom border; gridnbr_mark_isolated replaces each non-background cell with no same-color 4-neighbor with the atom isolated; gridnbr_expand_color implements morphological dilation adding one layer of Color to all background cells neighboring Color; gridnbr_shrink_color implements morphological erosion removing Color from cells that touch a non-Color 4-neighbor; gridnbr_majority_nbr4 finds the most frequent color among 4-neighbors via keyed-msort on negative counts; gridnbr_conway_step replaces each Color cell having exactly N Color 4-neighbors with the background color. All 51 acceptance tests pass.

Chapter 171 covers the griddist Pack (gd_* prefix, Layer 200): grid distance transform operations; gd_manhattan and gd_chebyshev compute point-to-point L1 and L-infinity distances; gd_dist_map and gd_dist_map8 produce per-cell grids of minimum Manhattan and Chebyshev distances to the nearest Color cell, returning 9999 for absent colors; gd_nearest_coord finds the [R2,C2] coordinates of the nearest Color cell from a given position; gd_zone marks each non-Color cell within Manhattan distance N of any Color cell with the atom zone; gd_ring returns all in-bounds cell positions at exactly Manhattan distance D from a given cell; gd_voronoi assigns each background cell the color of its nearest foreground cell (Manhattan distance); gd_bfs_flood computes BFS distances from a start cell treating Wall-color cells as impassable walls; gd_recolor_by_dist recolors each Color cell based on its distance from the nearest background cell using a user-supplied palette; gd_expand_n implements N-step morphological dilation and gd_shrink_n implements N-step morphological erosion both using the distance map; gd_equidistant marks cells where the distance to ColorA equals the distance to ColorB with the atom equidist; gd_max_dist finds the cell farthest from any Color cell, failing if Color is absent. All 53 acceptance tests pass.

Chapter 172 covers the gridperiod Pack (gper_* prefix, Layer 201): periodic pattern detection and extension on raw grids; gper_is_row_periodic and gper_is_col_periodic test whether a grid has row or column period P using negation-as-failure across all valid row or cell pairs; gper_row_period and gper_col_period find the smallest such period using between/3 with cut; gper_tile_dims combines both to return the smallest tile dimensions; gper_extract_tile returns the top-left TH x TW sub-grid; gper_tile_grid constructs an H x W grid by repeating a tile via wrap-around indexing Grid[r][c] = Tile[r mod TH][c mod TW]; gper_extend_rows and gper_extend_cols extend a grid to a target height or width by repeating in one dimension; gper_extend tiles in both dimensions in a single pass; gper_autocorr_row and gper_autocorr_col measure the fraction of row or column pairs at lag P that are identical, returning 1.0 when no pairs exist; gper_shift_wrap_rows shifts rows down by N with wrap-around and gper_shift_wrap_cols shifts columns right by N with wrap-around. All 51 acceptance tests pass.

Chapter 173 covers the gridscale Pack (gsc_* prefix, Layer 202): integer block-pixel scaling; gridscale_upsample scales a grid up by factor N making each cell an NxN block; gridscale_downsample collapses each NxN block to a single cell failing if any block is not uniform; gridscale_block_majority downsizes using majority-vote per block and never fails; gridscale_scale_factor finds N such that GridB = upsample(GridA, N) and fails if none exists; gridscale_is_scale_of verifies the relationship using negation-as-failure; gridscale_block_at extracts the NxN sub-grid at block position (BR, BC); gridscale_block_uniform succeeds if a block is all one color; gridscale_all_uniform succeeds if every NxN block is uniform (the necessary condition for being an exact upsampling); gridscale_infer_tile finds the smallest tile by searching from the largest valid divisor down; gridscale_factors returns all positive integers dividing both H and W; gridscale_resize performs nearest-neighbor resize to any target dimensions; gridscale_subsample takes every N-th row and column; gridscale_pad adds an N-cell wide border of a given color; gridscale_crop_to_factor crops to the largest dimensions divisible by N. All 60 acceptance tests pass.

Chapter 174 covers the gridrun Pack (grl_* prefix, Layer 203): run-length encoding and stripe analysis; grl_row_runs encodes row R as a list of Color-Len pairs (maximal same-color runs); grl_col_runs encodes column C similarly; grl_all_row_runs and grl_all_col_runs collect run lists for every row or column; grl_decode recovers a flat color list from a run-length encoding; grl_run_count returns the number of distinct runs in row R; grl_uniform_row and grl_uniform_col succeed if a row or column has exactly one run returning its color; grl_is_striped_h succeeds if every row is uniform using negation-as-failure; grl_is_striped_v succeeds if every column is uniform; grl_stripe_colors_h and grl_stripe_colors_v return the color list for horizontally or vertically striped grids failing if any row or column is not uniform; grl_max_run returns the Color and Len of the longest run in row R breaking ties by term order; grl_alternating succeeds if row R has all run lengths 1, exactly 2 distinct colors, and at least 2 cells. All 55 acceptance tests pass.

Chapter 175 covers the gridflood Pack (gf_* prefix, Layer 204): BFS flood-fill, region analysis, hole filling, and connected-component enumeration; gridflood_fill replaces the 4-connected same-color region at seed (R,C) with NewColor and is a no-op if already that color; gridflood_fill8 does the same with 8-connectivity including diagonal neighbors; gridflood_recolor globally swaps OldColor to NewColor regardless of connectivity; gridflood_isolate keeps one 4-connected region and sets everything else to BgColor; gridflood_region_cells returns the list of R-C coordinates of the 4-connected region at (R,C) in BFS order; gridflood_region_size returns the cell count; gridflood_region_bbox returns the bounding box (MinR, MinC, MaxR, MaxC); gridflood_enclosed_cells finds BgColor cells unreachable from any border cell via 4-connected BgColor paths (holes); gridflood_fill_enclosed replaces those holes with FillColor; gridflood_components enumerates all 4-connected Color regions as a list of cell-lists; gridflood_n_components counts those regions; gridflood_largest returns the cell-list of the largest region breaking ties by first occurrence; gridflood_is_connected succeeds if all Color cells form at most one region supporting connectivity 4 or 8; gridflood_boundary_fill flood-fills from (R,C) stopping at BoundaryColor walls. All 54 acceptance tests pass.

Chapter 176 covers the gridpath Pack (gpa_* prefix, Layer 205): BFS pathfinding and distance analysis; gridpath_shortest_path finds the shortest 4-connected path from (R1,C1) to (R2,C2) avoiding ObsColor cells and returns the ordered R-C sequence including both endpoints; gridpath_path_length returns the step count (path length minus 1); gridpath_reachable succeeds if a path exists; gridpath_all_reachable returns all cells reachable from a seed via non-ObsColor 4-paths; gridpath_distance_map returns a list of R-C-Dist triples giving BFS distances from the seed to every reachable cell; gridpath_nearest finds the nearest reachable cell of a given color; gridpath_flood_n returns all cells reachable within N steps; gridpath_wavefront returns cells at exactly N BFS steps returning [] if none; gridpath_straight_h and gridpath_straight_v generate ordered cell lists for horizontal and vertical segments inclusive of endpoints; gridpath_line_of_sight succeeds if two cells share a row or column with no ObsColor between them and fails for diagonal pairs; gridpath_between_h and gridpath_between_v return strictly interior cells of segments in ascending index order; gridpath_region_path finds the shortest path between any cell of Color1 and any cell of Color2. All 57 acceptance tests pass.

Chapter 177 covers the gridmask Pack (gm_* prefix, Layer 206): boolean mask operations on raw grids; gm_overlay places non-BgColor cells of GridB transparently over GridA; gm_union combines two regions keeping GridA color where both are non-BgColor or either grid's color when only one is; gm_intersect returns FgColor where both grids are non-BgColor and BgColor everywhere else; gm_difference keeps GridA cells where GridB is BgColor and replaces others with BgOut; gm_invert swaps foreground and background colors; gm_mask_apply passes Grid cells through where Mask is non-BgColor and substitutes FillColor where Mask is BgColor; gm_extract keeps only the listed R-C cells setting others to BgColor; gm_stamp overwrites Grid cells at (OriginR,OriginC) with Stamp cells ignoring out-of-bounds Stamp cells; gm_compare returns the differing R-C positions between two grids; gm_equal succeeds by unification; gm_sub extracts a rectangular sub-grid failing if out of bounds; gm_paste transparently pastes a Patch skipping BgColor Patch cells; gm_border_mask generates a border vs interior binary mask for N-layer borders; gm_color_mask generates a binary mask selecting cells of a given color. All 44 acceptance tests pass.

Chapter 178 covers the gridxform Pack (gx_* prefix, Layer 207): geometric transformations of raw grids; gridxform_rotate90 rotates 90 degrees clockwise mapping each result[R][C] to original[H-1-C][R] and producing a W x H output from an H x W input; gridxform_rotate180 rotates 180 degrees with same dimensions; gridxform_rotate270 rotates 270 degrees clockwise producing a W x H output; gridxform_flip_h mirrors columns left-to-right with same dimensions; gridxform_flip_v mirrors rows top-to-bottom with same dimensions; gridxform_transpose maps (r,c) to (c,r) producing a W x H output; gridxform_flip_d2 reflects along the anti-diagonal producing a W x H output; gridxform_crop extracts rows [R0..R1] x cols [C0..C1] (inclusive); gridxform_crop_content auto-crops to the tightest bounding box of non-BgColor cells returning the grid unchanged when all cells are background; gridxform_pad surrounds Grid with BgColor padding on all four sides with independent Top/Bot/Left/Right counts; gridxform_scale upscales each cell to an N x N block; gridxform_tile fills an H x W target using modular indexing into the tile; gridxform_d4_group enumerates all 8 D4 isometries in fixed order [identity, r90, r180, r270, flip_h, flip_v, transpose, flip_d2]; gridxform_normalize returns the lexicographically smallest of the 8 D4 isometries as the canonical form. All 44 acceptance tests pass.

Chapter 179 covers the gridsymm Pack (gsm_* prefix, Layer 208): symmetry detection, completion, violation detection, and score for raw grids; gridsymm_sym_h succeeds if Grid[R][C] = Grid[H-1-R][C] for all cells, testing top-bottom symmetry; gridsymm_sym_v tests left-right symmetry; gridsymm_sym_d1 and gridsymm_sym_d2 test main-diagonal and anti-diagonal symmetry (square grids only) and fail for non-square grids; gridsymm_sym_rot90 tests 90-degree rotational invariance (square grids only); gridsymm_sym_rot180 tests 180-degree rotational invariance for any rectangular grid; gridsymm_symmetries returns the list of all symmetry types the grid possesses; gridsymm_complete_h with Dir=top maps result[R][C] to Grid[min(R,H-1-R)][C] so the top half is reflected to the bottom; gridsymm_complete_h with Dir=bottom reflects the bottom half upward; gridsymm_complete_v mirrors left or right halves; gridsymm_complete_rot180 fills BgColor cells from their 180-rotational mirrors while preserving non-BgColor cells; gridsymm_violations_h, gridsymm_violations_v, and gridsymm_violations_rot180 return the list of first-half R-C positions where the symmetry fails; gridsymm_score computes the fraction of symmetric pairs that match as a float in [0.0..1.0] for types h, v, or rot180. All 44 acceptance tests pass.

Chapter 180 covers the gridcolor Pack (gc_* prefix, Layer 209): color analysis and transformation of symbolic grids; gridcolor_count counts cells matching a given color using findall; gridcolor_histogram produces sorted Color-Count pairs for all distinct colors; gridcolor_most_frequent and gridcolor_least_frequent find the color with maximum and minimum count respectively using histogram plus msort with last/2 for max and first for min; gridcolor_unique_colors returns the alphabetically sorted list of distinct colors using sort/2; gridcolor_color_cells returns R-C positions of all cells matching a given color; gridcolor_contains uses memberchk for efficient presence testing; gridcolor_count_colors gives the number of distinct colors; gridcolor_recolor replaces all occurrences of OldColor with NewColor; gridcolor_color_map applies a list of Old-New pair mappings leaving unmapped colors unchanged; gridcolor_threshold produces a binary grid where cells in a given Colors list become FgColor and all other cells become BgColor; gridcolor_dominant finds the most frequent non-BgColor; gridcolor_fraction computes the floating-point fraction of cells matching Color in [0.0..1.0]; gridcolor_sorted_by_freq returns colors in descending frequency order by negating counts before sorting. All 44 acceptance tests pass.

Chapter 181 covers the gridstitch Pack (gst_* prefix, Layer 210): grid assembly and disassembly operations; gst_concat_h places GridA and GridB side by side by zipping row lists with append, requiring matching row counts; gst_concat_v stacks GridA on top of GridB by appending row lists, requiring matching column counts; gst_hstack and gst_vstack reduce a list of grids left-to-right using the corresponding two-grid concat, with deterministic handling of empty and singleton lists (a cut on the singleton clause prevents choice points); gst_split_h splits a grid at row R by constraining length(Top, R) and using append, and gst_split_v splits each row at column C using the same length-plus-append pattern applied with maplist; gst_halves_h and gst_halves_v compute H//2 and W//2 and delegate to the general split predicates; gst_quadrants composes the half-splits to extract all four quadrants; gst_tile_grid arranges NR*NC sub-grids into a tiled layout using a two-level findall (outer over row bands, inner over columns) followed by per-band hstack and global vstack; gst_repeat_h and gst_repeat_v create N copies with findall and delegate to the stacking predicates; gst_add_border wraps Grid in an N-cell Color border by building a (H+2N) x (W+2N) grid where interior cells copy from the original and border cells emit Color; gst_strip_border is the inverse, extracting the (H-2N) x (W-2N) interior offset by N. All 44 acceptance tests pass.

Chapter 182 covers the gridedge Pack (ge_* prefix, Layer 211): edge and boundary detection for symbolic grids; ge_edge_cells collects FgColor cells with at least one differently-colored 4-connected in-bounds neighbor using once/1 inside findall to ensure each qualifying cell appears exactly once regardless of how many differing neighbors it has; ge_edge_cells8 uses the 8-connected (Moore) neighborhood; ge_is_edge tests a single cell with an if-then-else; ge_boundary collects Color1 cells adjacent to at least one Color2 cell, also using once for deduplication; ge_inner_border collects BgColor cells adjacent to non-BgColor cells; ge_outer_border collects non-FgColor cells adjacent to FgColor cells; ge_edge_grid builds a grid where FgColor edge cells are replaced by EdgeColor and other cells retain their values; ge_edge_count delegates to ge_edge_cells and measures the length; ge_neighbors_diff returns the list of 4-connected neighbor positions with different color from the reference cell; ge_corners collects FgColor cells whose inner findall of differently-colored 4-neighbors has length 2 or more; ge_endpoints collects FgColor cells with exactly 1 same-color 4-neighbor; ge_smooth_cells collects FgColor cells with no differently-colored in-bounds 4-neighbors using \+; ge_transition_map builds a new grid where each cell value is the count of differently-colored 4-neighbors (0 for interior uniform cells, up to 4 for cells surrounded by all different colors); ge_edge_color_count counts FgColor cells adjacent to a specified BgColor by delegating to ge_boundary. All 44 acceptance tests pass.
