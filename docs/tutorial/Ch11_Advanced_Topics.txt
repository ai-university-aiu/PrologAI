PROLOGAI TUTORIAL

Chapter 11: Advanced Topics - Actors, SONA, Workspace, and Protocols

Author: D. R. Dison, Founder of AIU (Artificial Intelligence University). Creator of PrologAI and Mentova. ORCID: 0009-0001-9246-5758. linkedin.com/in/d-r-dison/


INTRODUCTION

This chapter covers the more advanced parts of PrologAI. You do not need to master these topics before using Mentova for everyday reasoning. But if you want to build your own minds, extend Mentova, or integrate PrologAI with other AI systems, this chapter gives you the foundation.


ACTORS (PR 6 THROUGH PR 9)

An actor is an individual reasoning unit that runs concurrently with other actors.

Think of actors as workers in a company. The company (the mind) has many workers, each with a specific job. They communicate through messages and shared resources (the Lattice).

PrologAI actors are implemented in the actors pack (PR 6 through PR 9).

The main predicates:

    create_actor(+Name, +Module, +Init) - create a new actor.
    send_message(+Actor, +Message) - send a message to an actor.
    subscribe(+Channel, +Handler) - subscribe to a broadcast channel.
    publish(+Channel, +Content) - broadcast content on a channel.

Sentinels (PR 9) are a special kind of actor: they watch the Lattice for specific node_fact relation types and fire automatically when the pattern appears. You do not need to call them manually - they trigger on their own.

Example: when anchor_node(percept_signal, ...) is called, any sentinel registered to watch percept_signal will fire automatically. This is how the Level 1 Piagetian milestone (reflex coordination) works: the sentinel represents the automatic reflex arc.


SONA - EPISODIC MEMORY (PR 11)

SONA (Sparse Online Nexus Archive) is the mind's episodic memory.

While the Lattice holds structured knowledge (facts, assessments, plans), SONA holds experiences - sequences of events the mind has witnessed.

Think of SONA as the mind's ability to remember: "I saw X happen, and then Y happened, and then Z."

Key predicates:

    sona_absorb(+Trajectory) - store an experience in episodic memory.
    sona_retrieve(+Pattern, +Limit, -Results) - retrieve matching experiences.

A trajectory is any term representing a sequence of events:

    traj(actions([push_block(left), pull_block(right), stack]))

SONA uses sparse encoding (it does not store every detail, only the significant ones) and online learning (it updates continuously as the mind has experiences).

Example:

    ?- use_module(library(sona), [sona_absorb/1, sona_retrieve/3]).
    ?- sona_absorb(traj(events([saw(tweety, 10_30am), heard(robin, 10_35am)]))).
    ?- sona_retrieve(traj(_), 5, Results).


THE GLOBAL WORKSPACE (PR 18 AND PR 32)

The Global Workspace Theory (GWT) is a model of consciousness from cognitive science. It proposes that a "global workspace" broadcasts information to many specialized processors simultaneously.

PrologAI implements this theory as the workspace cycle (PR 18) and the attention economy (PR 32).

The workspace cycle:
1. Collects content from the Lattice and the actor network.
2. Selects the most salient (important) content.
3. Broadcasts it to all subscribers.
4. Subscribers process the broadcast and update the Lattice.
5. SONA absorbs the broadcast as an episodic memory.

Salience is computed as: novelty (0.4 weight) + goal_relevance (0.3) + affect (0.2) - habituation.

Content that is novel, goal-relevant, and emotionally significant rises to the top.

The attention economy (PR 32) adds a budget constraint: only a certain amount of content can be broadcast per cycle, so content competes for workspace access.

Running the workspace:

    ?- mentova_query(workspace, run_cycle(5), R).

This runs 5 workspace cycles. Each cycle broadcasts the highest-salience content and updates the Lattice.


THE ATTENTION SCHEMA (PR 42)

The attention schema is a model that Mentova maintains about its own attention.

Research on consciousness (particularly the work of Michael Graziano) proposes that consciousness is the brain's model of its own attention process. The attention schema theory suggests that what we call "awareness" is the brain's simplified description of how it is allocating attention.

PrologAI implements an attention schema (PR 42) that:
• Tracks what content is in the workspace.
• Predicts what will be attended to next.
• Maintains a record of habituated (over-repeated) content.
• Reports on attentional dynamics.

    ?- mentova_query(attention_schema, report, R).

Returns the current attention schema state.


THE AGENT COMMUNICATION PROTOCOLS

PrologAI supports four major agent communication protocols that allow Mentova to communicate with other AI systems.

MODEL CONTEXT PROTOCOL (MCP) - PR 14

MCP is a protocol for AI tool use, developed by Anthropic. It allows a language model (or any AI client) to call tools provided by a server.

PrologAI implements an MCP gateway that exposes 14 Mentova predicates as MCP tools. An external client can call these tools using standard MCP HTTP requests.

The MCP gateway runs on port 7474. Authenticated with an API key.

Available MCP tools include: lattice_inscribe, lattice_query, actor_list, assess_all, and more.

To start the MCP gateway:

    ?- use_module(library(mcp_gateway), [pai_mcp_start/1]).
    ?- pai_mcp_start(7474).


AGENT-TO-AGENT PROTOCOL (A2A) - PR 43

A2A is a protocol for agents to delegate tasks to each other.

It uses a task lifecycle: submitted -> working -> completed (or failed).

Each agent has an agent card that describes its capabilities and endpoint.

Mentova implements the full A2A protocol including durable peer mail (messages that persist until the recipient is available).

To start the A2A server:

    ?- use_module(library(a2a), [pai_a2a_start/1]).
    ?- pai_a2a_start(7475).


AGENT COMMUNICATION PROTOCOL (ACP) - PR 47

ACP is IBM's BeeAI REST protocol for agent runs.

POST /runs creates a new run. GET /runs/{id} checks status. DELETE /runs/{id} cancels.

The agent description is served at GET /.well-known/agent.json.

ACP runs are asynchronous: you submit a task and poll for the result.

    ?- use_module(library(acp), [pai_acp_start/1]).
    ?- pai_acp_start(7476).


AGENT NETWORK PROTOCOL (ANP) - PR 48

ANP provides decentralized agent identity using W3C Decentralized Identifiers (DIDs).

Each mind gets a DID (a globally unique identifier that does not depend on any central registry). Mentova's DID has the form did:web:hostname.

All ANP messages are signed with HMAC-SHA256. Incoming messages are verified before any content is processed.

ANP supports meta-protocol negotiation: when two agents meet, they announce which protocols they support, so they can choose the best one.

    ?- use_module(library(anp), [pai_anp_did/1, pai_anp_negotiate/2]).
    ?- pai_anp_did(DID).       % Get Mentova's DID.
    ?- pai_anp_negotiate(peer_endpoint, Protocols).  % Negotiate protocols with a peer.


THE ROBOT BODY (PR 46 - ROS BRIDGE)

PrologAI can inhabit a robot body through the Robot Operating System version 2 (ROS 2) bridge.

The bridge connects the Lattice to ROS 2 topics, services, and actions.

Perception from the robot (camera, LIDAR, touch) flows into the Lattice as percept_signal node_facts.

Actions decided by the mind are sent to ROS 2 action servers, which execute them on the physical or simulated robot.

The constitutional gate screens every physical action before it is sent. High-risk, irreversible actions require human authorization.

The robot body is one of several bodies Mentova can inhabit:
• text_io: the text console (the ?- prompt).
• game bodies: ARC-AGI, Raven's Progressive Matrices, Baba Is You, Pokemon.
• robot_body: the ROS 2 bridge.

Bodies are enrolled at boot time. Additional bodies can be enrolled during a session.


SUMMARY OF ALL 48 WORK PACKAGES

PR 1: kernel - foundational types and patterns.
PR 2: types - the type system.
PR 3: lattice - the knowledge store.
PR 4: node_facts - anchor_node/4, live_node_facts/2, traverse_nexus/4.
PR 5: scopes - the nine-zone compartmentalization.
PR 6: actors - cyclic actor framework.
PR 7: pubsub - publish-subscribe messaging.
PR 8: perception - sensory input processing.
PR 9: sentinels - automatic Lattice watchers.
PR 10: kernel extensions.
PR 11: sona - episodic memory.
PR 12: assessment - Piagetian, Bayley, CHC, consciousness indicators.
PR 13: refinement - testing harness.
PR 14: mcp_gateway - Model Context Protocol server.
PR 15: defeasible - default reasoning with exceptions.
PR 16: probabilistic - probabilistic reasoning.
PR 17: continual refinement - self-improvement harness.
PR 18: workspace - Global Workspace Theory cycle.
PR 19: prediction / active inference.
PR 20: bayesian - Bayesian updating.
PR 21: evidential reasoning and budgets.
PR 22: model composition and planning.
PR 23: curiosity and self-directed learning.
PR 24: somatic markers (emotion-like influence).
PR 25: daydream - steered imagination.
PR 26: appraisal - emotional appraisal and coping.
PR 27: motivation - motivational drive system.
PR 28: embedding - vector embedding integration.
PR 29: vector_backend - vector search.
PR 30: frames - frame-based knowledge.
PR 31: vsa - Vector Symbolic Architecture.
PR 32: attention economy - salience and competition.
PR 33: induction - rule learning.
PR 34: chainer - meta-reasoning chain.
PR 35: abduction - best-explanation reasoning.
PR 36: hyperon bridge (OpenCog Hyperon integration).
PR 37: clause synthesis and specialization.
PR 38: reflection and synthesis.
PR 39: reference frames (spatial and conceptual).
PR 40: spinoff - sub-agent creation.
PR 41: distributional semantics and probabilistic logic.
PR 42: attention schema.
PR 43: a2a - Agent-to-Agent protocol.
PR 44: tooling - tool-use faculty.
PR 45: language understanding.
PR 46: ros_bridge - robot body (ROS 2).
PR 47: acp - Agent Communication Protocol.
PR 48: anp - Agent Network Protocol (decentralized identity).
PR 49: lattice_crypto - Cryptographic Privacy Layer (RSA, ECDH, and PQC hybrid encryption for sensitive Lattice data).
PR 50: imagination - Imaginative memory: mindscapes, tableaux, and rendered reveries.
PR 51: awareness - Situational awareness: the ladder of regards, theory of mind, and self-reconciliation.


IMAGINATIVE MEMORY (PR 50)

Imaginative memory is the mind's inner stage. A mindscape is a compartmentalized canvas held in a non-observed reality, so nothing imagined can ever be mistaken for something perceived. On a mindscape you place a tableau: an arrangement of elements, each a vantage (a camera angle), a figure (an object, possibly grounded to a percept), or a motif (an animation resource). Rendering a mindscape produces a reverie: a deterministic, frame-by-frame animation in which each figure moves by its own velocity, letting the mind rehearse how a scene would unfold before acting.

    ?- use_module(library(imagination)).
    ?- pai_mindscape_new(imagined, M).
    ?- pai_tableau_add(M, [element(vantage, eye_level, []),
                           element(figure, ball, [pos(0,0), vel(2,1)])], T).
    ?- pai_reverie_render(M, 3, R), pai_reverie_frames(R, Frames).

The ball starts at (0,0) and is found at (4,2) by the third frame. Asking for a mindscape in the observed reality simply fails: imagination is sandboxed away from perceived fact.


SITUATIONAL AWARENESS (PR 51)

Awareness is engineered as a ladder of regards (viewpoints): ambient_regard (general awareness, the default), selfward_regard (self-interest), otherward_regard(Agent) (the view of another mind, for theory of mind), avowed_regard (what the mind owns), and disavowed_regard (what the mind disowns). A proposition is held under a regard, so the mind can believe something from one standpoint and deny it from another.

    ?- use_module(library(awareness)).
    ?- pai_regard_hold(selfward_regard, location(ball, box)).
    ?- pai_tom_attribute(alice, not(location(ball, box))).
    ?- pai_tom_divergence(alice, Divs).

The divergence list reports that alice holds a false belief relative to the mind's own view - the computational core of theory of mind. pai_regard_reconcile/1 then integrates the owned and disowned regards into a single coherent self-account.


DREAMING ENGINE (PR 52)

Humans have dreamed for as long as there have been humans. Now dreaming is becoming an activity for machines.

The dreaming pack gives PrologAI an idle-period dream cycle with three phases, each modeled on a stage of biological sleep.

Phase 1 - Slow-Wave (NREM analog). During this phase, the mind replays its most important SONA trajectories with a small stochastic perturbation called replay_noise. This is the symbolic equivalent of generative replay, in which an AI system re-simulates past experiences during offline periods to prevent catastrophic forgetting. After replay, the attention-economy banker cycle runs, promoting important STI (Short-Term Importance) nodes to LTI (Long-Term Importance) storage.

Phase 2 - REM (Rapid Eye Movement analog). During this phase, the mind queries all Lattice nodes with a positive LTI value and randomly pairs them to generate hypothetical new relations, tagged imagined. Nothing from this phase is asserted into the observed Lattice without a separate explicit validation step. This mirrors the way DreamerV3's RSSM imagines novel trajectories inside a learned world model.

Phase 3 - Hypnagogic and Hypnopompic transitions. Every dream event is recorded in the dream journal with a monotonic DreamId and a phase label.

Example:

    ?- use_module(library(dreaming)).
    ?- pai_dream_cycle(my_mind, Report).
    ?- pai_dream_journal(my_mind, Journal).
    ?- pai_dream_counterfactual(my_mind, some_node_id, 3, Alternatives).

The dreaming pack has soft dependencies on SONA, attention, and lattice. If any of these are not loaded, the corresponding phase degrades gracefully.


RUVECTOR HTTP BACKEND (vector_backend PR 2 update)

The vector_backend pack now supports the RuVector backend alongside the built-in pure-Prolog backend.

RuVector (https://github.com/ruvnet/ruvector) is a Rust-language vector database using HNSW (Hierarchical Navigable Small World) indexing with SIMD (Single Instruction, Multiple Data) acceleration and a self-learning GNN (Graph Neural Network) reranking layer.

Start the server (default port 6333):
    bash packs/vector_backend/scripts/ruvector_server.sh

Switch the backend and run the bakeoff:
    ?- vb_set_backend(ruvector).
    ?- run_bakeoff([prolog, ruvector], [100, 1000]).

If the server is not running, all six backend predicates degrade gracefully: insert/delete return silently, search returns an empty list, and create still returns a valid Ref term.

Confirmed bakeoff results (2026-06-24):

    ruvector: 0.2655  (100,000 vectors)
    prolog:   0.0650  (capped at 200 for speed)

RuVector is approximately 4 times faster than the pure-Prolog backend.

The score changes by less than 2% from 100 to 100,000 vectors, confirming that the HNSW index scales sub-linearly.


SHADOW STORE AND REBUILD PREDICATE (vector_backend, shadow store update)

The ruvector-server stores all vector collections in memory only.

If the server process stops for any reason, all vectors in the HNSW index are lost.

The Lattice facts (the actual knowledge) are stored in SWI-Prolog and are not affected.

Only the similarity index is at risk.

To mitigate this, the backend_ruvector module maintains an in-process shadow store.

Every call to vbr_insert/4 records a vbr_shadow/5 dynamic fact:

    vbr_shadow(CollectionName, BaseUrl, IdAtom, FloatVec, MetaAtom)

The shadow facts live in the SWI-Prolog process and survive a RuVector server restart as long as the Prolog session stays alive.

When the server is restarted, call the rebuild predicate:

    ?- vb_rebuild(Ref).

or, if using the backend_ruvector module directly:

    ?- vbr_rebuild(Ref).

The rebuild predicate executes four steps automatically.

Step 1 - It drops the collection from the server (the server may have already forgotten it, so errors are ignored).

Step 2 - It reads the vector dimension from the first shadow entry.

Step 3 - It recreates the collection on the server with the original name and dimension.

Step 4 - It re-inserts every shadow vector by calling the raw HTTP insert predicate directly, without modifying the shadow store.

When rebuild finishes, the HNSW index is fully restored and search results are accurate again.

To check how many vectors are in the shadow store:

    ?- vbr_shadow_count(Ref, Count).

A count of zero means either no vectors have been inserted or vbr_close/1 was called.

After a rebuild, the count remains unchanged because rebuild reads the shadow store but does not add or remove any facts.

Note on vbr_delete/2: The ruvector-server does not expose a per-point DELETE endpoint in the current version.

Calling vbr_delete(Ref, Id) removes the shadow entry for that Id so the vector will not be re-inserted on the next rebuild.

The ghost result (the deleted vector appearing in search results) will persist until the next rebuild cycle.

Note on session restarts: If the SWI-Prolog session itself restarts, the shadow facts are also cleared.

In that case, PrologAI's normal startup procedures (loading the Lattice, generating embeddings, inserting vectors) will rebuild both the HNSW index and the shadow store from scratch.

The vb_rebuild/1 interface is backend-agnostic.

For the pure-Prolog backend, vb_dispatch_rebuild(prolog, _Ref) is a no-op: the pure-Prolog backend stores vectors as dynamic facts that are always in-process and intact.


WHAT COMES NEXT

Chapter 12 covers writing your own PrologAI programs: creating .pl files, loading them into Mentova, and building new reasoning capabilities.

Chapter 13 covers the Cryptographic Privacy Layer: how to encrypt passwords, identification numbers, and other sensitive values stored in the Lattice, using RSA, ECDH, and Post-Quantum Cryptography.

Chapter 14 covers the Ephemera Pack: how PrologAI can compose and execute short-lived programs in Prolog, Python, and Bash to solve sub-problems that are most naturally expressed as code.

Chapter 17 covers the Grid Pack: native ARC-AGI grid perception and manipulation, including object extraction, spatial transformations, symmetry detection, and flood fill.

Chapter 18 covers the Analogy Pack: inferring ARC-AGI transformation rules from training examples using the full D4 dihedral group and color substitution maps.

Chapter 19 covers the Scene Pack: building a structured object inventory from a grid, with object properties and spatial relations.

Chapter 20 covers the Quant Pack: counting, grouping, frequency analysis, and uniformity tests over object sets.

Chapter 21 covers the Pattern Pack: period detection in lists, rows, and columns; tile-based grid construction; integer scaling; horizontal and vertical repetition; mirror expansion; and constructed patterns including checkerboard and stripes.

Chapter 22 covers the Compose Pack: sequential rule pipelines (cp_pipe), conditional branching (cp_branch), repetition combinators (cp_repeat, cp_until, cp_fixed_point), row and column mapping (cp_map_rows, cp_map_cols), pairwise grid combination (cp_zip), and left-fold over lists of grids (cp_fold).

Chapter 23 covers the Motion Pack: gravity predicates (mv_gravity_down, mv_gravity_up, mv_gravity_left, mv_gravity_right), grid translation (mv_shift_grid), directional sliding (mv_slide_col, mv_slide_row), scene-level object translation (mv_obj_translate, mv_scene_translate), scene-to-grid rendering (mv_scene_to_grid), scene gravity via grid round-trip (mv_scene_gravity), and proximity computation (mv_distance, mv_closest_cell).

Chapter 24 covers the Frame Pack: rectangular border detection (fr_border_color, fr_has_border), interior extraction (fr_inner, fr_interior_uniform, fr_interior_color), frame generation (fr_make_framed, fr_add_border), sub-region border testing (fr_region_has_border, fr_region_border_color), bounding box search (fr_bounding_box), and concentric nesting analysis (fr_is_nested, fr_ring_count).

Chapter 25 covers the Path Pack: 4-connected neighbor enumeration (pf_neighbors), flood fill (pf_flood_fill), connectivity testing (pf_connected, pf_is_connected), connected component analysis (pf_components, pf_component_count, pf_component_size, pf_largest_component), BFS shortest path (pf_shortest_path, pf_path_length, pf_path_exists), reachability (pf_reachable), and region bounding box (pf_fill_bbox).

Chapter 26 covers the Symmetry Pack: individual D4 symmetry tests (sy_is_hsymmetric, sy_is_vsymmetric, sy_is_rot180, sy_is_rot90, sy_is_diagonal, sy_is_antidiagonal), symmetry group computation (sy_group), orbit generation (sy_rotations, sy_orbit), canonical form (sy_canonical), orbit equivalence (sy_equivalent), and symmetry order (sy_order).

Chapter 27 covers the Color Pack: palette extraction (cl_palette, cl_color_count, cl_is_mono, cl_has_color), color counting and histograms (cl_count, cl_histogram), palette comparison (cl_same_palette), color replacement and remapping (cl_replace, cl_remap, cl_swap), dominant and rarest color detection (cl_dominant, cl_rarest), and color filtering (cl_isolate, cl_remove).

Chapter 28 covers the Shape Pack: normalized shape creation from cell lists (sh_from_cells) and grids (sh_from_grid), shape properties (sh_area, sh_bounding_size, sh_contains_cell, sh_equal), spatial transformations (sh_translate, sh_rotate90, sh_reflect_h, sh_reflect_v), D4 orbit and canonical form (sh_orbit, sh_canonical, sh_equivalent), and grid placement (sh_to_grid).

Chapter 29 covers the Relation Pack: positional ordering (rl_above, rl_below, rl_left_of, rl_right_of), 4-connected adjacency (rl_adjacent), minimum Manhattan distance (rl_distance), bounding box containment (rl_contained_bbox), set relations (rl_overlap, rl_disjoint), row and column alignment (rl_same_row, rl_same_col), integer centroid (rl_centroid), centroid offset (rl_offset), and cardinal direction (rl_direction).

Chapter 30 covers the Sequence Pack: integer range generation (sq_range), first differences (sq_delta), arithmetic sequence detection and extension (sq_is_arithmetic, sq_common_diff, sq_extend_arith), list chunking (sq_chunk), zipping and unzipping pairs (sq_zip, sq_unzip), cumulative prefix sums (sq_cumsum), 0-indexed slicing (sq_slice), one-level flatten (sq_flatten1), matrix transposition (sq_transpose), and period detection (sq_period, sq_is_periodic).

Chapter 31 covers the Crop Pack: bounding box detection (cr_bbox), rectangular subgrid extraction (cr_crop_bbox), content-aware cropping (cr_crop_content), uniform padding (cr_pad), border removal (cr_strip_border), horizontal and vertical splitting (cr_split_h, cr_split_v), row and column band extraction (cr_rows, cr_cols), horizontal and vertical stitching (cr_stitch_h, cr_stitch_v), subgrid embedding (cr_embed), center extraction (cr_center), and quadrant splitting (cr_quadrants).

Chapter 32 covers the Overlay Pack: transparent overlay (ov_over, ov_blend), bitwise grid operations (ov_or, ov_and, ov_xor), difference and intersection (ov_diff, ov_intersect), masking and inverse masking (ov_mask, ov_mask_inv), priority merge across a list of grids (ov_priority), color replacement (ov_replace, ov_fill_bg), and pointwise extrema (ov_max, ov_min).

Chapter 33 covers the Measure Pack: area (ms_area), bounding box (ms_bbox, ms_bbox_size), perimeter (ms_perimeter), diameter (ms_diameter), extent fraction (ms_extent), aspect ratio (ms_aspect), row and column spans (ms_row_span, ms_col_span), integer centroid (ms_centroid), Chebyshev radius (ms_radius), interior and border cell counts (ms_interior_count, ms_border_count), and distinct color count (ms_color_count).

Chapter 34 covers the Transform Pack: spatial scaling (tr_scale_up, tr_scale_down), horizontal and vertical tiling (tr_tile_h, tr_tile_v, tr_tile), transposition (tr_transpose), axis reflections (tr_flip_h, tr_flip_v), rotations (tr_rot90, tr_rot180), content shifting with fill (tr_shift), color-map application (tr_apply_map), single-color replacement (tr_replace_color), and mask-based cell selection (tr_mask_grid).

Chapter 35 covers the Select Pack: finding the largest and smallest regions (sl_largest, sl_smallest), sorting regions by cell count (sl_sort_by_area), filtering by exact, minimum, or maximum area (sl_filter_area, sl_filter_area_min, sl_filter_area_max), border-membership testing (sl_touches_border), border and interior filtering (sl_filter_border, sl_filter_interior), directional filtering by row and column bounds (sl_above_row, sl_below_row, sl_left_of_col, sl_right_of_col), and unique-area selection (sl_unique_area).

Chapter 36 covers the Count Pack: cell count by color (cn_color_count), value count in a flat list (cn_by_value), sorted color histogram (cn_histogram), most and least frequent colors (cn_max_color, cn_min_color), rows and columns containing a color (cn_color_rows, cn_color_cols), distinct value counts per row and column (cn_row_distinct, cn_col_distinct), total cell count (cn_grid_total), equal and differing cell counts between two grids (cn_equal_cells, cn_diff_cells), region color lookup (cn_region_color), and per-color region tally (cn_regions_per_color).

Chapter 37 covers the Fill Pack: filling a region list with a color (fl_fill_region), filling a bounding box rectangle (fl_fill_bbox), filling entire rows or columns (fl_fill_row, fl_fill_col), filling an explicit cell list (fl_fill_cells), filling the outermost grid ring (fl_fill_border), filling only the boundary cells of a region (fl_outline_region), filling only the interior cells (fl_fill_interior), creating a new uniform-color grid (fl_solid_rect), creating a new checkerboard grid (fl_checkerboard), drawing horizontal and vertical line segments (fl_draw_hline, fl_draw_vline), filling the main diagonal (fl_fill_main_diag), and overlaying a subgrid with transparency (fl_stamp).

Chapter 38 covers the Pattern Pack: detecting the tiling period of a row (pt_row_period) or column (pt_col_period), finding the horizontal and vertical tiling period of an entire grid (pt_grid_period_h, pt_grid_period_v), extracting the minimal horizontal tile (pt_tile_unit_h), vertical tile (pt_tile_unit_v), and 2D tile (pt_tile_unit), testing whether a grid is an exact tiling of a given tile (pt_is_tiling), finding all top-left positions where a tile matches (pt_find_tile), counting those positions (pt_count_tile), extracting an H x W subgrid at a specified position (pt_extract_tile), counting distinct rows and columns (pt_unique_rows, pt_unique_cols), and testing whether all rows are identical (pt_has_uniform_rows).

Chapter 39 covers the Compare Pack: finding cells where two grids differ (cp_diff_cells), cells where they agree (cp_same_cells), cells where a color was gained (cp_added_color) or lost (cp_removed_color), cells that changed to (cp_changed_to) or from (cp_changed_from) a specific color, a 0/1 difference map (cp_diff_map), an integer-exact similarity score (cp_similarity), set difference of two cell regions (cp_region_diff), set intersection (cp_region_intersect), set union (cp_region_union), order-independent region equality (cp_region_equal), structural grid equality (cp_grids_equal), and a list of Old-New color pairs for all changed cells (cp_color_shift).

Chapter 40 covers the Spatial Pack: cardinal direction between two cells (sp_direction), Manhattan distance (sp_distance_manhattan), Chebyshev distance (sp_distance_chebyshev), 4-connected in-bounds neighbor enumeration (sp_neighbors4), 8-connected in-bounds neighbor enumeration (sp_neighbors8), 4-connected adjacency test (sp_adjacent4), 8-connected adjacency test (sp_adjacent8), bounding box containment (sp_bbox_contains), cell membership in a region (sp_in_region), row-band region filtering (sp_row_between), column-band region filtering (sp_col_between), nearest region cell by Manhattan distance (sp_closest), farthest region cell (sp_farthest), and integer centroid of a region (sp_centroid).

Chapter 41 covers the Induction Pack: grid dimensions (id_grid_dims), sorted distinct colors in input and output grids (id_input_colors, id_output_colors), colors gained or lost between grid pairs (id_new_colors, id_lost_colors), per-cell change and no-change lists (id_changed_cells, id_unchanged_cells), inferred Old-New color substitution map (id_color_map), consistent color-substitution test (id_is_recolor), uniform single-color output detection (id_uniform_output, id_output_color), dimension ratio of an output relative to its input (id_size_ratio), integer uniform-scale test (id_is_scale), and integer scale factor extraction (id_scale_factor).

Chapter 42 covers the Gravity Pack: column value extraction (gv_col_values) and replacement (gv_set_col), compacting all non-background cells to the bottom of each column (gv_compact_col, gv_fall_down), to the top (gv_fall_up), to the left of each row (gv_compact_row, gv_fall_left), to the right (gv_fall_right), settling a specific color to the bottom of each column past background while other non-background cells act as obstacles (gv_settle_color, gv_stack_down), floating a specific color to the top (gv_float_color, gv_stack_up), and applying a custom column or row transform (gv_apply_col, gv_apply_row).

Chapter 43 covers the Noise Pack: binary mask application with a fill color (ns_mask_apply), mask inversion (ns_mask_invert), pointwise AND and OR of two masks (ns_mask_and, ns_mask_or), building a binary mask from a color criterion (ns_mask_from_color), converting a mask to a region list (ns_mask_to_region) and back (ns_region_to_mask), finding cells that differ from the dominant color (ns_noise_cells), denoising a grid by replacing outliers with the majority color (ns_denoise), identifying the most frequent color (ns_majority_color), counting occurrences of a color (ns_color_count), finding cells belonging to rare colors (ns_sparse_cells), finding cells belonging to frequent colors (ns_dense_cells), and isolating one color by zeroing out all others (ns_isolate_color).

Chapter 44 covers the Generate Pack: uniform color fill (ge_uniform), horizontal and vertical gradients (ge_gradient_h, ge_gradient_v), checkerboard alternating two colors (ge_checkerboard), horizontal and vertical color stripes (ge_stripes_h, ge_stripes_v), bordered rectangle and frame patterns (ge_border_rect, ge_frame), main diagonal and anti-diagonal patterns (ge_diagonal, ge_antidiagonal), identity matrix pattern (ge_identity_grid), cross through the center (ge_cross), building a grid from a list of cell-color pairs (ge_from_map), and tiling a smaller pattern to fill a larger grid (ge_repeat_pattern).

Chapter 45 covers the Lookup Pack: association list key lookup (lk_get), adding or replacing a key-value pair (lk_put), extracting all keys (lk_keys) or values (lk_values), key membership test (lk_has_key), removing a key-value pair (lk_delete), transforming all values with a predicate (lk_map_values), building a map from pairs (lk_from_pairs), grid row access (lk_grid_row), grid column access (lk_grid_col), grid cell access (lk_grid_cell), building a Color-to-positions index map (lk_color_positions), building a position-to-Color index map (lk_position_color), and inverting an association list (lk_invert).

Chapter 46 covers the Connect Pack: 4-connected flood fill from a seed cell (cc_flood4), 8-connected flood fill (cc_flood8), all 4-connected components of a color (cc_components4), all 8-connected components (cc_components8), component count in 4-connected (cc_count4) and 8-connected (cc_count8) mode, sorted component size lists (cc_sizes4, cc_sizes8), largest component by size (cc_largest4, cc_largest8), smallest component by size (cc_smallest4), cells on the boundary of a region (cc_border_cells), cells in the interior of a region (cc_interior_cells), and background cells enclosed inside a closed shape (cc_enclosed).

Chapter 47 covers the Morph Pack: 4-connected and 8-connected dilation (mo_dilate4, mo_dilate8), 4-connected and 8-connected erosion (mo_erode4, mo_erode8), repeated dilation and erosion (mo_dilate4_n, mo_erode4_n), morphological open to remove small protrusions (mo_open4), morphological close to fill small gaps (mo_close4), inner boundary cells (mo_boundary4, mo_boundary8), outer one-cell ring of background cells (mo_ring4), filling enclosed background holes with color (mo_fill_holes4), adding a one-cell border around the grid (mo_pad), and removing the outermost ring (mo_unpad).


Chapter 48 covers the Rewrite Pack: applying a color substitution map to all grid cells (rw_map_color), replacing one color with another (rw_replace_color), swapping two colors simultaneously (rw_swap_colors), painting a region of r(R,C) cells to a target color (rw_set_region), applying a binary mask to select which cells receive a fill color (rw_mask_apply), compositing two grids by letting non-background cells of Grid2 overwrite Grid1 (rw_overlay), stamping a smaller patch into a larger grid at an offset (rw_stamp), applying a list of r(R,C)-Color cell edits (rw_diff_apply), renumbering distinct non-background colors to 1,2,... in row-major first-appearance order (rw_normalize), complementing each color value with respect to a maximum (rw_invert_colors), replacing one background color with another (rw_remap_bg), painting the outermost ring of cells to a color (rw_set_border), filling an axis-aligned rectangle with a color (rw_fill_rect), and conditionally recoloring each cell based on a 3-argument goal predicate (rw_conditional).


Chapter 49 covers the Run Pack: run-length encoding a flat list to Value-Count pairs (rn_encode), decoding Value-Count pairs back to a flat list (rn_decode), encoding a single grid row (rn_row_encode) or column (rn_col_encode), encoding all rows (rn_grid_rows) or all columns (rn_grid_cols) of a grid, computing the total element count of a run list (rn_length), looking up the value at a 0-indexed position (rn_at), finding the longest run of a given value (rn_max_run), counting distinct runs of a given value (rn_count_runs), testing whether all runs share the same value (rn_uniform), removing leading and trailing occurrences of a background value (rn_trim), repeating an encoded sequence N times with boundary run merging (rn_repeat), and enumerating the 0-indexed positions of all cells with a given value (rn_positions).


Chapter 50 covers the Arith Pack: cell-wise addition of two grids (ar_cell_add), cell-wise subtraction (ar_cell_sub), cell-wise multiplication (ar_cell_mul), cell-wise modulo by a scalar (ar_cell_mod), adding a scalar to every cell (ar_scalar_add), multiplying every cell by a scalar (ar_scalar_mul), sum of all cells in one row (ar_row_sum), sum of all cells in one column (ar_col_sum), list of row sums (ar_row_sums), list of column sums (ar_col_sums), grid-wide maximum cell value (ar_cell_max), grid-wide minimum cell value (ar_cell_min), clamping every cell to a range (ar_cell_clamp), and cell-wise absolute difference of two grids (ar_cell_abs_diff).


Chapter 51 covers the Obj Pack: constructing an object term from a color and cell list (obj_from_cells), extracting the color (obj_color), extracting the sorted cell list (obj_cells), measuring the number of cells (obj_size), computing the axis-aligned bounding box (obj_bbox), computing the integer centroid (obj_center), computing the translation-independent normalized shape (obj_shape), extracting all 4-connected objects of one color (obj_inventory), extracting all objects of all non-zero colors (obj_all), counting objects of a color (obj_count), finding the largest object (obj_largest), finding the smallest object (obj_smallest), looking up which object contains a given cell (obj_at_cell), and sorting objects by size (obj_sort_size).


Chapter 52 covers the Pipeline Pack: registering a named step handler (pl_register), querying a registered handler (pl_registered), removing a handler (pl_unregister), applying one step with an optional local registry (pl_step), threading an input through a sequence of named steps (pl_run), mapping a 2-argument goal over a list (pl_map), filtering a list to elements that satisfy a 1-argument goal (pl_filter), folding a 3-argument goal over a list with an accumulator (pl_fold), zipping two lists into a list of pairs (pl_zip), unzipping a list of pairs into two lists (pl_unzip), taking the first N elements (pl_take), dropping the first N elements (pl_drop), and partitioning a list into satisfied and rejected elements (pl_partition).


Chapter 53 covers the Context Pack: adding or replacing a key-value binding (ctx_put), retrieving a value by key (ctx_get), testing key presence (ctx_has), removing a key (ctx_delete), extracting all keys (ctx_keys) or values (ctx_values), building a map from a pair list (ctx_from_pairs), converting to a pair list (ctx_to_pairs), merging two maps with Map2 overriding Map1 (ctx_merge), dispatching a goal by key with a default fallback (ctx_dispatch), selecting the value for the first present key in a priority list (ctx_select), transforming all values via a goal (ctx_map_values), filtering entries by a key predicate (ctx_filter_keys), and counting entries (ctx_size).


Chapter 54 covers the Score Pack: testing structural equality of two grids (sc_exact), counting cells where two grids agree (sc_cell_match), counting the total number of cells in a grid (sc_cell_total), computing pixel accuracy as a float in [0.0, 1.0] (sc_accuracy), computing per-color recall (sc_color_recall), per-color precision (sc_color_precision), and per-color F1 score (sc_color_f1), applying a rule to a single training pair and measuring accuracy (sc_pair_score), computing mean accuracy over a list of training pairs (sc_pairs_score), testing whether a rule solves one training pair exactly (sc_perfect), testing whether a rule solves all training pairs exactly (sc_pairs_perfect), ranking candidate grids by accuracy descending (sc_rank), picking the best candidate (sc_best), and filtering candidates by a minimum accuracy threshold (sc_threshold).


Chapter 55 covers the Induct Pack: computing the color-delta between two grids as a list of changed-cell triples (in_delta), testing identity (in_constant), inferring a color substitution map from one training pair (in_color_map), intersecting color maps across all training pairs for consistency (in_color_map_pairs), computing the row and column size change for one pair (in_size_change), verifying consistent size change across all pairs (in_size_change_pairs), building the union color palette for one pair (in_color_palette), separating input and output color sets across all pairs (in_palette_pairs), listing invariant cells (in_invariant_cells), listing changed cells (in_changed_cells), verifying a consistent delta across all pairs (in_consistent_delta), finding the background color by frequency (in_bg_color), verifying a consistent background across all pairs (in_bg_color_pairs), and intersecting two color maps (in_common_keys).


Chapter 56 covers the Hyp Pack: applying a color substitution map to every cell with identity fallback for unmapped colors (hy_color_sub), the identity no-op hypothesis (hy_identity), a partial-application alias for color substitution (hy_from_map), testing a hypothesis on one training pair and returning pixel accuracy (hy_test), testing on all pairs and returning mean accuracy (hy_test_all), exact-match test for one pair (hy_verify), exact-match test for all pairs (hy_verify_all), selecting the best hypothesis from a list (hy_select), ranking hypotheses by mean accuracy descending (hy_rank), an alias for color substitution (hy_apply_map), sequential two-map color substitution (hy_compose), inverting a color substitution map (hy_invert_map), color lookup with identity fallback (hy_map_lookup), and describing a hypothesis as a human-readable atom (hy_describe).


Chapter 57 covers the Sym Pack: reflecting a grid left-right (sy_reflect_h), top-bottom (sy_reflect_v), and across the main diagonal (sy_transpose), rotating 90 degrees clockwise (sy_rotate90), 180 degrees (sy_rotate180), and 270 degrees clockwise (sy_rotate270), testing for horizontal symmetry (sy_has_h_symm), vertical symmetry (sy_has_v_symm), 2-fold rotational symmetry (sy_has_rot2_symm), and 4-fold rotational symmetry (sy_has_rot4_symm), listing all symmetry names present in a grid (sy_symmetries), making a grid horizontally symmetric by mirroring the left half (sy_make_h_symm), making it vertically symmetric by mirroring the top half (sy_make_v_symm), and computing the full D4 orbit of all distinct spatial transforms (sy_d4_orbit).


Chapter 58 covers the Seek Pack: finding all (row,col) positions of a value in a grid (sk_positions), finding row indices containing a value (sk_rows_with), finding column indices containing a value (sk_cols_with), listing all border cell positions (sk_border_cells), listing all interior cell positions (sk_interior_cells), testing whether a sub-grid matches exactly at a position (sk_fits), nondeterministically enumerating positions where a sub-grid fits (sk_find_sub), collecting all fitting positions as a list (sk_all_subs), counting the number of fitting positions (sk_count_sub), counting how many cells match when a sub-grid is placed at a position (sk_match_count), finding the position that maximises the match count (sk_best_fit), discovering the D4 transform name that maps one grid to another (sk_find_d4), upscaling a grid so each cell becomes a Factor-by-Factor block (sk_upscale), and finding the integer scale factor relating two grids (sk_find_scale).

Chapter 59 covers the Remap Pack: replacing every occurrence of one value with another (rm_replace), swapping two values throughout a grid (rm_swap), applying a color substitution map to all cells with identity fallback for unmapped values (rm_apply_map), applying a map only to cells matching a specific value (rm_apply_map_to), inverting a map by swapping keys and values (rm_invert_map), composing two maps by chaining their lookups (rm_compose_maps), normalizing all distinct values to consecutive integers starting from 1 (rm_normalize), shifting all cell values by an integer offset (rm_shift), clamping all cell values to a range (rm_clamp), recoloring cells that satisfy a predicate goal (rm_conditional), binarizing a grid to foreground/background (rm_binarize), remapping the background color to a new value (rm_remap_bg), extracting the sorted palette of distinct values (rm_palette), and reindexing a grid using a supplied palette (rm_reindex).

Chapter 60 covers the Logic Pack: boolean intersection of two grids keeping values only where both are non-background (lg_and), boolean union returning first grid's value on ties (lg_or), exclusive-or returning a value only where exactly one grid is non-background (lg_xor), inverting foreground and background (lg_not), set-difference keeping only cells present in the first grid but absent in the second (lg_diff), overlaying one grid onto another with background treated as transparent (lg_overlay), applying a mask to keep grid values only where the mask is non-background (lg_mask_apply), creating a binary presence mask from a grid (lg_mask_from), computing per-row presence flags indicating whether any cell is non-background (lg_any_row), computing per-column presence flags (lg_any_col), computing per-row fullness flags indicating whether all cells are non-background (lg_all_row), per-column fullness flags (lg_all_col), cell-wise equality producing 1 where grids agree and 0 where they differ (lg_eq), and cell-wise inequality (lg_neq).

Chapter 61 covers the Window Pack: listing R2-C2-Val triples for all in-bounds 4-connected neighbors of a cell (wn_neighbors4), listing 8-connected neighbor triples (wn_neighbors8), counting 4-connected neighbors equal to a target value (wn_count4), counting 8-connected neighbors equal to a value (wn_count8), extracting an H x W sub-grid starting at a given position (wn_extract), enumerating all valid H x W sliding windows as R0-C0-Sub triples (wn_slide), padding all sides of a grid with N layers of a padding value (wn_pad), testing whether a cell is a local maximum among its 4-connected neighbors (wn_local_max4), testing whether a cell is a local minimum (wn_local_min4), finding cells adjacent to a target value but not equal to it (wn_halo4), computing integer convolution of a grid with a kernel (wn_convolve), finding the floor-center coordinates of a grid (wn_center), computing the Manhattan distance between two grid positions (wn_manhattan), and listing in-bounds cells at exactly Manhattan distance D from a given cell (wn_cells_at_dist).

Chapter 62 covers the Sort Pack: computing the integer sum of each row (so_row_sums), computing the integer sum of each column (so_col_sums), counting a specific value per row (so_row_count), counting a specific value per column (so_col_count), sorting rows ascending by count of a value (so_sort_rows_asc), sorting rows descending by count of a value (so_sort_rows_desc), sorting columns ascending by count of a value (so_sort_cols_asc), sorting columns descending by count of a value (so_sort_cols_desc), finding the row index with the highest count of a value (so_max_row), finding the row index with the lowest count (so_min_row), finding the column index with the highest count (so_max_col), finding the column index with the lowest count (so_min_col), listing all cell values sorted ascending with duplicates preserved (so_sorted_vals), and finding the 1-based rank of a cell's value among distinct grid values (so_cell_rank).

Chapter 63 covers the Tile Pack: repeating a tile N times horizontally (ti_tile_h), repeating a tile N times vertically (ti_tile_v), tiling a motif into an NR-by-NC arrangement of copies (ti_tile), splitting a grid into horizontal bands of TH rows each (ti_split_rows), splitting a grid into vertical stripes of TW columns each (ti_split_cols), splitting a grid into a list-of-tile-rows of TH x TW tiles (ti_split), reassembling split tiles back into one grid (ti_flatten_tiles), overlaying a motif at position (R, C) in a base grid (ti_stamp), stamping a motif at every position in a list (ti_stamp_all), extracting the tile at tile-position (TR, TC) (ti_extract_tile), checking if a grid is an exact tiling of one motif (ti_is_tiling), finding the smallest horizontal period in columns (ti_find_period_h), finding the smallest vertical period in rows (ti_find_period_v), and generating an H x W checkerboard with two values (ti_checkerboard).

Chapter 64 covers the Trace Pack: finding maximal contiguous non-background runs in a row (tr_runs_row), per-row run lists for a grid (tr_spans_h), per-column run lists (tr_spans_v), casting a horizontal ray to find the first non-background cell in a direction (tr_ray_h), casting a vertical ray (tr_ray_v), listing all R-C pairs in a horizontal line inclusive of endpoints (tr_line_h), listing all R-C pairs in a vertical line (tr_line_v), extracting grid values along a list of positions (tr_path_vals), painting a value at each position in a list (tr_draw_path), listing the border cells of a bounding rectangle (tr_bbox_border), non-background cells touching background or on the grid boundary (tr_perimeter), background cells touching non-background cells (tr_outline), all cells on the grid boundary (tr_edge_cells), and computing the floor midpoint of two positions (tr_midpoint).

Chapter 65 covers the Label Pack: assigning unique integer labels to each maximal 4-connected component (lb_label), returning component cell lists (lb_components), counting components (lb_count), returning the cell count of a specific label (lb_size_of), returning all Label-Size pairs (lb_sizes_all), listing cells of a specific label (lb_cells_of), returning the bounding box corners of a label (lb_bbox_of), listing foreground labels adjacent to a given label (lb_neighbors_of), replacing all cells of a label with a value (lb_fill_label), keeping only the largest component (lb_keep_largest), removing components below a size threshold (lb_remove_small), coloring each label from a cycling palette (lb_color_labels), merging two labels into one (lb_merge_two), and extracting one component from the original grid (lb_select_label).

Chapter 66 covers the Morph Pack: expanding all non-background regions by one 4-connected step copying the neighbor's color (mo_dilate), shrinking all non-background regions by one step (mo_erode), applying dilation N times (mo_dilate_n), applying erosion N times (mo_erode_n), morphological opening as erode then dilate (mo_open), morphological closing as dilate then erode (mo_close), morphological smoothing as open then close (mo_smooth), extracting only perimeter non-background cells (mo_boundary), extracting only interior non-background cells (mo_interior), dilating with a fixed fill value instead of the neighbor's color (mo_dilate_val), BFS flooding from seed cells into adjacent background territory (mo_grow_from), computing the L1 Manhattan distance from each non-background cell to the nearest background cell (mo_dist_to_bg), extracting the cells added at exactly the Nth dilation step as a ring (mo_ring), and filling all enclosed background regions not reachable from the grid border (mo_fill_holes).

Chapter 67 covers the Walk Pack: returning all R-C positions in row-major order (wk_row_scan), column-major order (wk_col_scan), zigzag boustrophedon order alternating left-right on even rows and right-left on odd rows (wk_zigzag_scan), grouped by main diagonal D = C minus R (wk_diag_scan), grouped by anti-diagonal D = R + C (wk_antidiag_scan), in clockwise inward spiral order peeling rings from outside to center (wk_spiral_in), and in clockwise border order for the outermost ring only (wk_border_walk); extracting values on a specific main diagonal (wk_diag_extract) or anti-diagonal (wk_antidiag_extract); computing the main diagonal index D = C minus R for a cell (wk_diag_of) or the anti-diagonal index D = R + C (wk_antidiag_of); extracting grid values at a list of R-C positions (wk_cells_to_vals); painting values at a list of R-C positions (wk_vals_to_cells); and returning all non-border R-C positions in row-major order (wk_inner_cells).

Chapter 68 covers the Step Pack: taking one unbounded step in a direction (st_step); taking one bounded step that fails if out of grid (st_step_in); collecting all in-bounds cells in a direction excluding the start (st_ray); collecting cells in a direction stopping before a given value (st_ray_to); collecting all in-bounds cells including the start (st_walk); returning the four cardinal directions (st_dirs4); returning all eight principal directions (st_dirs8); rotating a direction 90 degrees clockwise (st_rotate_cw); rotating 90 degrees counter-clockwise (st_rotate_ccw); reversing a direction (st_opposite); computing the unit step direction between two cells (st_normalize); following a list of direction steps from a start cell (st_path); finding the first cell in a direction with a given value (st_first); and counting the number of steps until the grid boundary (st_to_edge).

Chapter 69 covers the Pivot Pack: computing the integer floor centroid of a cell list (pv_centroid); converting an absolute cell to a pivot-relative offset (pv_to_rel); converting a pivot-relative offset back to an absolute cell (pv_from_rel); rotating one cell 90 degrees clockwise around a pivot (pv_rotate_cell_cw); rotating a list of cells 90 degrees clockwise (pv_rotate_cells_cw); rotating 180 degrees (pv_rotate_cells_180); rotating 90 degrees counter-clockwise (pv_rotate_cells_ccw); reflecting a list of cells horizontally around the pivot column (pv_reflect_cells_h); reflecting vertically around the pivot row (pv_reflect_cells_v); reflecting across the main diagonal through the pivot (pv_reflect_cells_diag); reflecting across the anti-diagonal through the pivot (pv_reflect_cells_antidiag); computing the sorted D4 orbit of a single cell around the pivot (pv_orbit); computing the sorted D4 symmetry closure of a cell list (pv_sym_closure); and stamping a list of DR-DC offset cells at a pivot position in a grid (pv_stamp_at).


Chapter 70 covers the Project Pack: propagating each non-BG cell's value downward through BG cells in the same column until hitting another non-BG cell or the boundary (pj_shadow_down); propagating upward (pj_shadow_up); propagating leftward (pj_shadow_left); propagating rightward (pj_shadow_right); dispatching shadow casting by direction atom down, up, left, or right (pj_shadow_dir); returning sorted row indices that contain at least one non-BG cell (pj_nonbg_rows); returning sorted column indices that contain at least one non-BG cell (pj_nonbg_cols); returning a list of non-BG cell counts per row (pj_row_counts); returning a list of non-BG cell counts per column (pj_col_counts); collapsing all rows to one row by taking the first non-BG value per column (pj_collapse_rows); collapsing all columns to one column by taking the first non-BG value per row (pj_collapse_cols); finding the row index of the topmost non-BG cell in a column (pj_col_first); finding the row index of the bottommost non-BG cell in a column (pj_col_last); and finding the column index of the leftmost non-BG cell in a row (pj_row_first).


Chapter 72 covers the Order Pack: computing the integer floor centroid of an obj(Color, Cells) term (od_centroid); sorting objects by centroid row ascending (od_sort_row); sorting by centroid column ascending (od_sort_col); sorting by row then column in reading order (od_reading_order); sorting by color value ascending (od_sort_color); returning the object with the smallest centroid row (od_topmost); returning the object with the largest centroid row (od_bottommost); returning the object with the smallest centroid column (od_leftmost); returning the object with the largest centroid column (od_rightmost); retrieving the Nth object in row-ascending order (od_nth_row); retrieving the Nth object in column-ascending order (od_nth_col); finding the object whose centroid has minimum Manhattan distance to a reference position (od_nearest); finding the object whose centroid has maximum Manhattan distance to a reference position (od_farthest); and finding the 1-based rank of an object in row-ascending order (od_rank_row).


Chapter 71 covers the Diff Pack: finding all changed cells between two grids as diff(R,C,OldValue,NewValue) terms (df_cell_diff); finding cells that transitioned from background to non-background (df_added); finding cells that transitioned from non-background to background (df_removed); finding cells that stayed non-background but changed color (df_recolored); finding cells whose value did not change (df_stable); reporting which colors were added to or lost from the palette (df_palette_change); finding cells that changed in every In-Out pair in a list (df_common_diffs); finding cells that were stable in every pair (df_common_stable); finding cells that were added in every pair (df_always_added); finding cells that were removed in every pair (df_always_removed); counting the total number of changed cells (df_total_changes); applying a list of diff terms to a grid (df_apply_diffs); inverting a list of diffs to reverse the direction of each change (df_invert_diffs); and filtering a list of diffs by a goal predicate (df_filter_diffs).


Chapter 73 covers the Assemble Pack: horizontally concatenating a list of same-height grids into one wide grid (as_hcat); vertically stacking a list of same-width grids into one tall grid (as_vcat); assembling a 2D matrix of grids into one combined grid by joining each row then stacking the strips (as_grid_of); reducing each K x K non-overlapping block to a single cell by majority vote (as_downscale); surrounding a grid with a W-cell wide frame of a chosen color (as_border); embedding a grid at the integer floor center of a larger canvas filled with a background color (as_center_in); extracting one of four named quadrants tl, tr, bl, or br using floor division at the midpoint (as_quarter); concatenating a grid with its left-right mirror side by side (as_flip_h_cat); stacking a grid with its top-bottom mirror (as_flip_v_cat); interleaving columns from two same-size grids so columns alternate between them (as_zip_h); interleaving rows from two same-size grids (as_zip_v); unconditionally pasting a sub-grid into a base grid at a given offset, overwriting regardless of cell value (as_paste); replacing cells with a fill value wherever the corresponding mask cell is non-zero (as_mask_fill); and cropping or padding a grid to exactly a target number of rows and columns using a background fill (as_crop_to).


Chapter 74 covers the Region Pack: checking whether a row or column is entirely equal to a separator value (rg_is_sep_row, rg_is_sep_col); collecting all separator row or column indices into a sorted list (rg_sep_rows, rg_sep_cols); computing inclusive R0-R1 or C0-C1 span pairs for the non-separator sections along each axis (rg_spans_h, rg_spans_v); splitting a grid at its separator rows into a list of sub-grids with separators excluded (rg_cut_h); splitting a grid at its separator columns into a list of sub-grids (rg_cut_v); assembling a 2D matrix of all sections by cutting in both directions (rg_sections); retrieving the N-th horizontal or vertical section by 1-indexed number (rg_section_h, rg_section_v); counting how many horizontal or vertical sections a grid has (rg_count_h, rg_count_v); and extracting the sub-grid of the section that contains a given cell coordinate, failing if the coordinate is itself on a separator (rg_region).


Chapter 75 covers the Neighbor Pack: returning a list of nb(Row,Col,Val) terms for each valid 4-connected neighbor of a cell, with out-of-bounds directions omitted (nb_4neighbors); the same for 8-connected neighbors including diagonals (nb_8neighbors); testing whether a non-Bg cell has at least one Bg or out-of-bounds 4-neighbor, making it a boundary cell (nb_is_boundary); testing whether a non-Bg cell has all 4-neighbors in-bounds and non-Bg, making it an interior cell (nb_is_interior); collecting all boundary cells as sorted R-C pairs (nb_boundary_cells); collecting all interior cells as sorted R-C pairs (nb_interior_cells); counting 4-neighbors with the same value as the cell (nb_count_same); counting 4-neighbors with a different value (nb_count_diff); returning the sorted set of distinct values found among 4-neighbors (nb_adjacent_colors); finding all cells of a specific Color that are adjacent to a cell of a different color or the grid boundary, forming the outline of the Color region (nb_contour); testing whether any cell of ColorA is 4-adjacent to any cell of ColorB anywhere in the grid (nb_color_touches); enumerating all such 4-adjacency pairs as (R1-C1)-(R2-C2) terms (nb_touching_pairs); replacing the 4-connected region from a seed cell with a fill value to produce a modified grid (nb_flood_fill); and expanding a Color region by one cell in all 4-connected directions by setting adjacent Bg cells to Color (nb_dilate).


Chapter 76 covers the Permute Pack: reordering all rows of a grid according to an explicit index list so that result row i comes from grid row Perm[i] (pm_permute_rows); reordering all columns by an index list (pm_permute_cols); exchanging exactly two rows (pm_swap_rows); exchanging exactly two columns (pm_swap_cols); cyclically shifting rows so the last N rows move to the front (pm_cycle_rows); cyclically shifting columns so the last N columns move to the left (pm_cycle_cols); discovering the index permutation that maps the rows of one grid to the rows of another (pm_find_row_perm); discovering the index permutation that maps the columns of one grid to the columns of another (pm_find_col_perm); sorting all rows in ascending lexicographic order while preserving duplicates (pm_sort_rows); sorting all columns in ascending lexicographic order while preserving duplicates (pm_sort_cols); inserting a complete row before a given position (pm_insert_row); removing the row at a given position (pm_delete_row); inserting a column of values before a given position in every row (pm_insert_col); and removing the column at a given position from every row (pm_delete_col).


Chapter 77 covers the Gravity Pack: compacting all non-Bg values to the left end of a row while padding right with Bg (gv_pack_row_left); compacting to the right end (gv_pack_row_right); compacting to the top of a column list (gv_pack_col_up); compacting to the bottom of a column list (gv_pack_col_down); sliding all non-Bg cells left within their row (gv_fall_left); sliding all non-Bg cells right within their row (gv_fall_right); making all non-Bg cells fall downward within their column (gv_fall_down); making all non-Bg cells rise upward within their column (gv_fall_up); dispatching gravity by direction atom down, up, left, or right (gv_fall_dir); sliding only a specific Color left while other non-Bg cells act as immovable walls bounding each gravity segment (gv_fall_color_left); sliding only Color right within each segment (gv_fall_color_right); dropping only Color cells downward within each column segment (gv_fall_color_down); raising only Color cells upward within each column segment (gv_fall_color_up); and dispatching color-specific gravity by direction atom (gv_fall_color_dir).


Chapter 78 covers the Count Pack (Layer 96): counting occurrences of a value in a flat list (cn_by_value); counting a value across all cells of a 2D grid (cn_color_count); building a frequency histogram of all cell values as parallel sorted Colors and Counts lists (cn_histogram); returning the most frequently occurring value (cn_max_color); returning the least frequently occurring value (cn_min_color); counting the number of rows containing a given value (cn_color_rows); counting the number of columns containing a given value (cn_color_cols); counting distinct values in a single row (cn_row_distinct); counting distinct values in a single column (cn_col_distinct); returning the total number of cells as rows times columns (cn_grid_total); counting cells where two grids of identical dimensions agree (cn_equal_cells); counting cells where two grids differ (cn_diff_cells); looking up the grid value at the first cell of a region expressed as a list of r(R,C) terms (cn_region_color); and building a Color-Count tally for a list of regions (cn_regions_per_color).


Chapter 79 covers the Stripe Pack (Layer 97): testing whether row R has all cells equal to Color (sr_uniform_row); testing whether column C has all cells equal to Color (sr_uniform_col); returning sorted row indices uniformly equal to a specified Color (sr_uniform_rows); returning sorted column indices uniformly equal to a specified Color (sr_uniform_cols); returning R-Color pairs for all uniform rows in row-index order (sr_all_stripe_rows); returning C-Color pairs for all uniform columns in column-index order (sr_all_stripe_cols); returning sorted row indices that are NOT uniform (sr_mixed_rows); returning sorted column indices that are NOT uniform (sr_mixed_cols); filling row R with Color to produce a new grid (sr_fill_row); filling column C with Color to produce a new grid (sr_fill_col); filling all row indices in a list with Color (sr_fill_rows); filling all column indices in a list with Color (sr_fill_cols); returning r(R,C) terms for the Cartesian product of two index lists (sr_cross_cells); and filling all cells at a set of row-column intersections with Color (sr_cross_fill).


Chapter 80 covers the Diagonal Pack (Layer 98): extracting the values on the main diagonal where C equals R (dg_main_diag); extracting values on the main anti-diagonal where R plus C equals NC minus 1 through the top-right corner (dg_anti_diag); extracting values on the N-th diagonal where C minus R equals N (dg_nth_diag); extracting values on the N-th anti-diagonal where R plus C equals N (dg_nth_anti_diag); returning all diagonals as a list of value lists sorted by offset from -(NR-1) to NC-1 (dg_all_diags); returning all anti-diagonals as a list sorted by sum from 0 to NR+NC-2 (dg_all_anti_diags); filling the main diagonal with Color (dg_fill_main); filling the main anti-diagonal with Color (dg_fill_anti); filling the N-th diagonal with Color (dg_fill_nth_diag); filling the N-th anti-diagonal with Color (dg_fill_nth_anti_diag); computing the diagonal index N equals C minus R for cell (R,C) (dg_cell_diag); computing the anti-diagonal index N equals R plus C for cell (R,C) (dg_cell_anti_diag); testing whether the N-th diagonal has all cells equal to Color (dg_uniform_diag); and testing whether the N-th anti-diagonal has all cells equal to Color (dg_uniform_anti_diag).


Chapter 81 covers the Recur Pack (Layer 99): generating an N-term arithmetic sequence from a start value and step (rc_arith); extracting the common difference from an existing arithmetic sequence (rc_arith_step); testing whether a sequence is arithmetic and returning its step (rc_is_arith); generating a sequence by repeating a unit N times end-to-end (rc_repeat); finding the minimal repeating unit of a sequence whose length must evenly divide the sequence length (rc_period); testing whether a sequence is an exact integer-multiple repetition of some unit (rc_is_periodic); computing the next N terms of an arithmetic sequence by continuing from its last element (rc_next_arith); computing the next N terms of a periodic sequence by cycling from the current offset (rc_next_repeat); extending an arithmetic sequence by N terms and returning the extended list and detected step (rc_extend_arith); extending a periodic sequence by N terms and returning the extended list and minimal period unit (rc_extend_repeat); returning the N-th element one-based of the infinite cyclic repetition of a unit using modular arithmetic (rc_cycle_nth); applying a three-argument goal pair-wise over two same-length lists to produce a result list (rc_zip_with); computing consecutive element-wise differences of a numeric list where the result has one fewer element (rc_diff_list); and testing whether all consecutive differences of a numeric list are equal and returning that common value (rc_const_diffs).


Chapter 84 covers the Cluster Pack (Layer 102): computing the Chebyshev L-infinity distance max of absolute row and column differences (cl_chebyshev); computing the Manhattan L-1 distance sum of absolute differences (cl_manhattan); computing the squared Euclidean distance sum of squared differences for integer comparison without square roots (cl_euclidean_sq); filtering a cell list to those within a Chebyshev radius of a query point (cl_within); finding the cell in a set with the minimum Chebyshev distance to a point with tie-breaking by first occurrence (cl_nearest); finding the cell with the maximum Chebyshev distance (cl_farthest); computing the integer centroid of a cell set by rounding the mean row and column indices (cl_center); computing the maximum pairwise Chebyshev distance within a set (cl_diameter); computing the bounding box row and column spans (cl_spread); grouping cells by their grid color into parallel sorted Colors and Groups lists (cl_group_by_color); sorting cells by ascending Chebyshev distance to a point using stable keysort (cl_sort_by_dist); finding the pair of cells with minimum pairwise Chebyshev distance (cl_nearest_pair); finding the pair with maximum pairwise distance (cl_farthest_pair); and filtering cells whose row and column indices both fall within given inclusive bounds (cl_cells_in_band).


Chapter 83 covers the Rect Pack (Layer 101): enumerating all cells of a solid axis-aligned rectangle in row-major order (rt_cells); listing only the frame cells of a rectangle in sorted row-major order (rt_border); listing interior cells strictly inside the frame or returning empty for thin rectangles (rt_interior); filling the entire solid rectangle with a color (rt_draw); drawing only the frame with a color (rt_draw_border); filling only the interior with a color (rt_draw_interior); computing the total number of cells as row count times column count (rt_area); testing whether a cell lies within the rectangle on all four inclusive sides (rt_contains); returning the four corner cells in top-left top-right bottom-left bottom-right order (rt_corners); computing the bounding box of any non-empty list of R-C cells as minimum and maximum row and column values (rt_bbox); testing whether every cell in the rectangle has the same color and returning that color (rt_is_solid); testing whether every border cell has the same color (rt_is_frame); testing whether two axis-aligned rectangles share at least one cell using row and column interval arithmetic (rt_overlap); and scaling a rectangle by an integer factor from its top-left corner by multiplying the row and column spans (rt_scale).


Chapter 82 covers the Line Pack (Layer 100): listing all R-C cells on a horizontal segment at a given row between two column bounds in ascending column order (li_hline); listing all R-C cells on a vertical segment at a given column between two row bounds in ascending row order (li_vline); listing cells on a 45-degree diagonal segment where the row span must equal the column span in walk order from start to end (li_diag_seg); drawing a horizontal line onto a grid by setting every cell along the segment to a color (li_draw_h); drawing a vertical line onto a grid (li_draw_v); drawing a 45-degree diagonal line onto a grid (li_draw_diag); testing whether two cells share a row where R1 equals R2 (li_same_row); testing whether two cells share a column where C1 equals C2 (li_same_col); testing whether two cells lie on the same diagonal where C minus R is constant (li_same_diag); testing whether two cells lie on the same anti-diagonal where R plus C is constant (li_same_anti); testing whether three cells are collinear meaning they all share a row, column, diagonal, or anti-diagonal (li_collinear); extracting the first and last R-C pairs from a non-empty cell list (li_endpoints); returning the cells strictly between two aligned points on the same row, column, diagonal, or anti-diagonal excluding both endpoints (li_gap); and classifying a segment as point, horizontal, vertical, diagonal, anti_diagonal, or other (li_line_type).


Chapter 85 covers the Ray Pack (Layer 103): testing whether a cell coordinate pair is within the 0-based grid bounds (ry_in_bounds); enumerating all in-bounds cells starting one step beyond an origin in a direction (DR,DC) until leaving the grid (ry_cells_in_dir); finding the first cell in a direction whose value differs from a background color (ry_first_hit); collecting the maximal background-colored prefix of a ray before the first obstacle (ry_cast_clear); computing the step count from an origin to the first non-background cell (ry_distance_to_hit); computing the step count from an origin to the grid boundary in a direction (ry_distance_to_edge); testing that every cell in a direction is the background color (ry_is_clear); computing the coordinates exactly N steps from an origin in a direction and failing if out of bounds (ry_project); mapping named direction atoms up down left right ul ur dl dr to unit row-column step pairs (ry_dir_to_delta); reflecting a direction across a horizontal axis by negating the row component (ry_reflect_h); reflecting a direction across a vertical axis by negating the column component (ry_reflect_v); testing that all interior cells on a same-row same-column or same-diagonal segment between two cells are background (ry_los); enumerating the interior cells strictly between two aligned cells on the same row column main diagonal or anti-diagonal (ry_los_cells); and casting rays in all 4 compass directions simultaneously returning a list of Dir-Result pairs where each result is the first obstacle cell or the atom none if the direction is clear to the boundary (ry_cast_all_4).


Chapter 86 covers the Vec2 Pack (Layer 104): computing the displacement vector from one cell to another as DR=R2-R1 and DC=C2-C1 (vv_offset); applying a displacement vector to translate a point (vv_add); scaling a vector by an integer factor with negative factors reversing direction (vv_scale); negating a vector to point in the opposite direction (vv_neg); computing the squared Euclidean length without floating-point arithmetic (vv_len_sq); computing the L1 Manhattan magnitude as the sum of absolute components (vv_manhattan); computing the L-infinity Chebyshev magnitude as the maximum absolute component (vv_chebyshev); rotating a vector 90 degrees clockwise in row-column coordinates where RDR=DC and RDC=-DR (vv_rot90_cw); rotating 90 degrees counter-clockwise where RDR=-DC and RDC=DR (vv_rot90_ccw); rotating 180 degrees equivalent to negation (vv_rot180); computing the dot product DR1*DR2+DC1*DC2 which is positive for same-direction vectors and zero for orthogonal ones (vv_dot); computing the z-component of the 2D cross product DR1*DC2-DC1*DR2 which is positive when V2 is counter-clockwise from V1 (vv_cross); testing whether two vectors are parallel by asserting that their cross product is zero (vv_parallel); and translating a list of R-C cell coordinates by a displacement vector preserving order (vv_translate_region).


Chapter 87 covers the Spread Pack (Layer 105): computing a BFS distance map from a single source through background cells returning Cell-D pairs for every reachable cell (sd_dist_map); computing a multi-source BFS distance map returning minimum distances from any seed cell (sd_dist_multi); listing all background cells reachable from a source in sorted order (sd_reachable); finding the BFS shortest path distance between two specific cells failing when no path exists (sd_path_dist); testing reachability between two cells deterministically (sd_path_exists); collecting all cells at exactly BFS distance D in a sorted list (sd_ring); collecting all cells within BFS distance MaxD in a sorted list (sd_zone); finding the minimum BFS distance from a source to any cell in a target set (sd_dist_to_set); identifying the farthest reachable cell by BFS distance (sd_farthest); performing one BFS level expansion returning the sorted deduplicated set of unvisited Bg neighbors of a frontier (sd_expand_once); spreading a color outward through background cells up to MaxD BFS steps from all seed cells of that color (sd_spread_color); computing the maximum BFS distance from a source to any reachable cell (sd_max_dist); overlaying BFS distances onto the grid leaving non-Bg and unreachable cells unchanged (sd_fill_dist); and computing a Voronoi assignment of Bg cells to the nearest seed by index with ties broken by lower seed index (sd_voronoi).


Chapter 88 covers the Enclosure Pack (Layer 106): collecting all cells on the four grid edges in sorted order (en_border_cells); finding all background cells reachable from the grid border by 4-connected background steps returning the exterior region (en_outer_cells); finding all background cells not reachable from the border returning the enclosed interior region (en_inner_cells); testing if a specific cell is an enclosed background cell (en_is_inner); testing if a specific cell is an exterior background cell (en_is_outer); painting all enclosed background cells with a given color (en_fill_inner); counting the number of enclosed background cells (en_inner_count); testing whether at least one enclosed background cell exists (en_has_inner); partitioning enclosed background cells into their 4-connected components (en_inner_components); partitioning exterior background cells into their 4-connected components (en_outer_components); filling the single enclosed background component containing a given cell with a color failing if the cell is not enclosed (en_fill_hole); finding all cells of a given color that lie on the boundary of the color region defined as cells on the grid edge or with at least one non-color neighbor (en_boundary_cells); finding all cells of a given color strictly interior to the color region defined as cells with exactly four in-bounds neighbors all of the same color (en_interior_cells); and testing whether all four orthogonal neighbors of a cell are either out of bounds or equal to a given wall color (en_is_surrounded).


Chapter 89 covers the Hull Pack (Layer 107): computing the convex hull of a cell set as a list of vertices in clockwise screen order using Andrew's monotone chain algorithm with collinear points excluded (hu_convex_hull); testing whether a discrete cell set is convex meaning it equals the complete set of grid cells inside its convex hull within the bounding box (hu_is_convex); counting the number of hull vertices excluding collinear intermediate points (hu_hull_size); computing twice the hull polygon area as an integer using the shoelace formula with zero returned for degenerate hulls of fewer than 3 vertices (hu_hull_area2); testing whether a given row-column point lies inside or on the boundary of a convex hull with separate handling for empty, single-vertex, and two-vertex degenerate cases (hu_in_hull); collecting all grid cells that lie inside or on the convex hull as a sorted list (hu_cells_in_hull); painting all cells inside the convex hull with a given color (hu_fill_hull); finding all background cells inside the convex hull but absent from the input shape, representing the concavities or dents in the shape (hu_concavities); finding the pair of cells with the greatest squared Euclidean distance and returning that squared distance (hu_diameter); computing the row-span and column-span of the hull bounding box (hu_aspect); testing whether the convex hull has exactly four vertices and all four interior angles are exactly 90 degrees working for both axis-aligned and tilted rectangles (hu_is_rect); computing the sum of squared edge lengths around the closed hull as an integer proxy for perimeter avoiding floating-point arithmetic (hu_hull_perim2); computing the integer floor of the average row and column of the hull vertices as a centroid estimate (hu_centroid); and testing whether every cell in a given set lies inside or on the convex hull of another set (hu_hull_contains).


Chapter 90 covers the Patch Pack (Layer 108): extracting an H x W rectangular sub-patch from a grid starting at a given row and column position (pa_extract); getting the height and width dimensions of a patch (pa_dims); non-deterministically enumerating all positions where a small pattern occurs exactly in a larger grid by Prolog structural unification of the extracted sub-grid (pa_match); collecting all matching positions as a list (pa_match_all); counting matching positions (pa_match_count); matching with a background wildcard where background-colored cells in the pattern match any grid cell and non-background cells must match exactly (pa_match_bg); enumerating all overlapping H x W sub-patches in row-major order with their top-left coordinates (pa_slides); finding the sorted list of all distinct patches of a given size (pa_unique_patches); finding the patch that occurs most frequently and its count using msort-then-grouping (pa_most_common_patch); returning the sorted list of R-C cell positions where two same-size patches differ (pa_diff); testing whether all cells in a patch have the same value and returning that value (pa_is_uniform); reversing each row independently for a horizontal left-right flip (pa_flip_h); reversing the sequence of rows for a vertical top-bottom flip (pa_flip_v); and rotating a patch 90 degrees clockwise using the formula Rotated(r-prime, c-prime) equals Patch(H-1-c-prime, r-prime) to produce a W x H output from an H x W input (pa_rot90).


Chapter 91 covers the Signal Pack (Layer 109): finding the sorted list of 0-based indices of strict interior local maxima in a list where index I is a peak if List[I] exceeds both immediate neighbors and I is neither the first nor the last element (sg_peaks); finding strict interior local minima by the symmetric condition (sg_valleys); finding the 0-based index of the maximum value with first-occurrence tie-breaking using max_list then nth0 plus cut (sg_argmax); finding the index of the minimum value (sg_argmin); computing consecutive element-wise differences where the result has one fewer element than the input (sg_diffs); mapping each consecutive difference to its sign as 1 for positive, -1 for negative, or 0 for zero (sg_slope_signs); testing that each consecutive pair satisfies strict less-than vacuously true for lists of length 0 or 1 (sg_is_increasing); testing strict greater-than (sg_is_decreasing); testing non-strict less-than-or-equal (sg_is_nondecreasing); testing non-strict greater-than-or-equal (sg_is_nonincreasing); partitioning 0-based indices into those with values strictly above a threshold and those at or below it (sg_threshold); extracting a complete row of a grid by 0-based index (sg_row_profile); extracting a complete column by capturing the column index in a YALL lambda inside maplist (sg_col_profile); and extracting both the row and column profiles through a single cross-shaped query at a given grid position (sg_cross_profile).


Chapter 92 covers the Histogram Pack (Layer 110): counting occurrences of a specific value in a list using include/3 with ==(V) and counting the result (hi_count); computing sorted V-Count pairs for every distinct value in ascending value order (hi_freq); finding the most frequent value and its count with ties broken by smallest value (hi_mode); finding the least frequent value with ties broken by largest value (hi_rare); returning the sorted list of distinct values via sort/2 (hi_unique_vals); counting distinct values (hi_unique_count); sorting all V-Count pairs by count descending with ties broken by value ascending using negated-count keysort for stability (hi_sorted_by_freq); extracting the N most frequent V-Count pairs as a prefix of the sorted list using length/2 and append/3 (hi_top_n); testing that a value appears at least once with memberchk for determinism (hi_has_value); testing that every element of a list equals the first element using forall and =:= (hi_all_equal); flattening a 2D grid with append/2 and computing its frequency table (hi_grid_freq); finding the most frequent cell color in a grid (hi_grid_mode); finding the least frequent cell color in a grid (hi_grid_rare); and finding the 1-based rank of a value by frequency where rank 1 is the most common using nth1/3 with a cut for determinism (hi_rank).


Chapter 93 covers the Zoom Pack (Layer 111): upscaling a grid by integer factor F where each cell becomes an FxF block of the same color using a recursive per-row structure to preserve correct row ordering (zm_scale_up); repeating each element F times in a row using findall + between + append (zm_scale_up_row); downsampling a grid by sampling every Fth row and column starting at position 0 (zm_scale_down); sampling every Fth element from a single row using I mod F =:= 0 (zm_scale_down_row); inferring the integer scale factor from the ratio of column widths of two grids (zm_scale_factor); testing that a grid is exactly the F-upscaling of a smaller grid (zm_is_scaled_up); computing the upscaled dimensions BH = H*F and BW = W*F (zm_dims_up); computing the downsampled dimensions SH = H//F and SW = W//F (zm_dims_down); extracting the FxF block at tile position (BR, BC) using row and column arithmetic with a zm_take_cols_ helper (zm_extract_block); splitting a grid into all FxF blocks as a flat row-major list (zm_split_blocks); computing the number of complete tile blocks along each grid dimension (zm_blocks_dims); returning the single color of a uniform FxF block and failing for non-uniform blocks (zm_block_color); downsampling using the mode (most common value) of each FxF block with largest-value tie-breaking via msort + last on N-V count pairs (zm_scale_down_mode); and tiling a small grid NR times vertically and NC times horizontally by first extending each row NC times then repeating the full block NR times (zm_tile_grid).


Chapter 94 covers the Offset Pack (Layer 112): shifting a row rightward by N cells where the left N positions become background and the rightmost N elements are dropped using length/2 + append/3 for prefix extraction and findall + between for background cell generation (od_shift_row); shifting a row leftward (od_shift_row_left); circular rightward row shift where the last N mod W elements wrap to the front by splitting at W-N and reassembling (od_roll_row); shifting all grid rows rightward with maplist and a YALL lambda (od_shift_right); shifting leftward (od_shift_left); shifting content downward by inserting N background rows at the top and dropping N bottom rows (od_shift_down); shifting upward (od_shift_up); dispatching shift by direction atom using if-then-else for determinism (od_shift_dir); circular rightward shift of every row (od_roll_right); circular leftward shift of every row (od_roll_left); circular downward roll where the last N rows wrap to the top by splitting at H-N (od_roll_down); circular upward roll where the first N rows wrap to the bottom (od_roll_up); shifting a specific color by (DR, DC) where each output cell checks its source position for Color with an in-bounds test and if-then-else logic without an explicit mutable structure (od_shift_color); and finding the integer (DR, DC) displacement between two grids by brute-force search over all displacements up to grid dimensions with a cut after the first match (od_infer_shift).


Chapter 95 covers the Interleave Pack (Layer 113): merging two same-height grids by alternating rows so that row 0 comes from GridA, row 1 from GridB, row 2 from GridA, and so on (il_rows); merging by alternating columns within each row so that elements interleave A[0], B[0], A[1], B[1] (il_cols); splitting a grid into even-indexed rows and odd-indexed rows using a two-clause index-accumulating helper with a cut (il_derows); splitting each row into even-indexed and odd-indexed elements (il_decols); inserting a background row of the same width between every pair of adjacent rows to create a woven grid of 2*H-1 rows (il_weave_rows); inserting a background value between adjacent column values in each row to create rows of width 2*W-1 (il_weave_cols); removing odd-indexed rows to recover the even-indexed core by delegating to il_derows (il_unweave_rows); removing odd-indexed columns (il_unweave_cols); extracting every Step-th row starting at a 0-based index using findall + between + modular arithmetic (il_stride_rows); extracting every Step-th column using findall for column indices and maplist to project each row (il_stride_cols); extracting rows at a given 0-based index list using maplist over nth0 (il_select_rows); extracting columns at a given index list using maplist over per-row index projection (il_select_cols); round-robin interleaving of rows from a list of equal-height grids so that row I of each grid appears in turn before row I+1 of any grid, using findall with between and member (il_rows_n); and partitioning rows into consecutive groups of N using recursive length + append with an if-then-else for partial last groups (il_block_rows).


Chapter 96 covers the Panel Pack (Layer 114): testing whether a row is entirely background using forall/2 over all row elements (pn_is_divider_row); testing whether a column is entirely background using forall over nth0-accessed column values (pn_is_divider_col); listing all all-background row indices (pn_divider_rows); listing all all-background column indices (pn_divider_cols); splitting at given row divider indices where divider rows are excluded and bands between dividers become separate parts using a two-clause recursive helper with cut (pn_split_rows); splitting at given column divider indices using pn_slice_row_ via maplist (pn_split_cols); detecting and splitting at all-background rows automatically (pn_auto_split_rows); detecting and splitting at all-background columns automatically (pn_auto_split_cols); removing all all-background rows using findall and not-memberchk (pn_strip_rows); removing all all-background columns from each row (pn_strip_cols); splitting at floor midpoints to produce four quadrant sub-grids (pn_quadrants); partitioning into N equal-width column strips using findall with arithmetic for strip boundaries (pn_n_col_parts); partitioning into custom-width column slices given a list of widths using cumulative sums and a recursive slicer (pn_col_slices); and testing for any all-background row or column using if-then-else to short-circuit on the first found divider (pn_has_dividers).


Chapter 97 covers the Scan Pack (Layer 115): enumerating all R-C-V triples in row-major order with row as the outer loop (sn_row_major); enumerating in column-major order with column as the outer loop (sn_col_major); listing all R-C positions of a specific value using findall over both dimensions (sn_cells_of); returning a new grid with a single cell replaced using nested findall with if-then-else on the row and column indices (sn_set_cell); applying a user-supplied 4-argument goal Goal(R,C,OldV,NewV) to every cell via nested findall with call/5 (sn_map_cells); collecting R-C-V triples satisfying a 3-argument predicate via findall with call/3 (sn_filter_cells); applying a list of R-C-V batch updates using memberchk in an if-then-else lookup (sn_update_cells); traversing in boustrophedon order where even rows go left-to-right and odd rows go right-to-left by computing the column as W1-Offset for odd rows (sn_zigzag); traversing in clockwise spiral order from the outermost ring inward using a recursive sn_spiral_/6 helper with nested if-then-else termination and reverse/2 for bottom and left sides (sn_spiral_in); visiting the outer border cells clockwise using if-then-else guards on H > 1 and W > 1 and reverse/2 for right-to-left sides (sn_border_traversal); visiting NE-going diagonals where D = R+C is enumerated from 0 to H+W-2 with within-diagonal row iteration (sn_diag_traversal_ne); visiting SE-going diagonals where D = C-R is enumerated from -(H-1) to W-1 with within-diagonal row iteration (sn_diag_traversal_se); building an H-by-W grid from a sparse R-C-V cell list using memberchk in an if-then-else inside nested findall (sn_grid_from_cells); and finding the first occurrence of a value in row-major order using between/3 enumeration followed by a cut (sn_index_of).


Chapter 98 covers the Bound Pack (Layer 116): computing the tight inclusive bounding box of all cells of a given color using findall + min_list + max_list and failing when the color is absent (bd_bbox); computing bounding box height as R1-R0+1 (bd_bbox_h); computing bounding box width as C1-C0+1 (bd_bbox_w); cropping a grid to an inclusive row and column rectangle using findall over the row range and bd_slice_row_ with length/2 + append/3 for inclusive column slices (bd_crop_bbox); cropping to the tight bounding box of a color as a two-step pipeline (bd_crop_color); removing all all-background border rows and columns by finding the bounding box of all non-background cells with if-then-else for the all-background case (bd_trim); adding N background cells on all four sides using findall for background rows and maplist with bd_pad_row_ for row-level extension (bd_pad); placing a sub-grid into a canvas at a given top-left position with nested findall and if-then-else on patch row/column bounds (bd_place); computing the integer center of a color's bounding box using floor division (bd_center); testing whether a cell lies within an inclusive bounding box with four arithmetic comparisons (bd_bbox_contains); testing whether two inclusive bounding boxes overlap by the four-condition overlap formula (bd_bbox_overlap); computing the smallest bounding box enclosing two boxes using min and max (bd_bbox_union); expanding a bounding box outward by N cells on all sides with arithmetic (bd_expand); and filling every cell within an inclusive bounding box with a color using nested findall with if-then-else on the row and column bounds (bd_fill_bbox).


Chapter 99 covers the Cellset Pack (Layer 117): converting a grid to a sorted list of R-C positions for a given color using findall over all row-column pairs and sort/2 for canonical order (cs_from_grid); painting a sorted cell set onto an H by W canvas by testing membership with memberchk inside nested findall (cs_to_grid); computing the sorted set union of two cell sets by appending and sorting (cs_union); computing the sorted set intersection by collecting members of A that appear in B (cs_intersect); computing the set difference of A minus B by delegating to subtract/3 from library(lists) (cs_subtract); shifting all cells by a row-column offset using maplist with the private cs_add_ helper preserving sorted order because translation is monotone (cs_translate); computing the bounding box of a non-empty cell set using findall for row and column index lists and min_list plus max_list for extremes (cs_bbox); translating a cell set so its minimum row and column become zero and returning the negative offsets used with an empty-list base case (cs_normalize); counting cells using length/2 (cs_size); testing deterministic membership with memberchk (cs_contains); finding all 4-connected background neighbors not in the set using four fixed direction offsets in a findall and sorting to remove shared-neighbor duplicates (cs_adjacent_bg); testing shape equality after normalizing both sets to the origin and comparing with structural equality (cs_same_shape); rotating cell positions 90 degrees clockwise within a grid of H rows using the formula new_R = old_C and new_C = H-1-old_R with maplist and a final sort (cs_rotate_90); and mirroring cell positions horizontally within a grid of W columns using the formula new_C = W-1-old_C with maplist and sort (cs_mirror_h).


Chapter 100 covers the Locate Pack (Layer 118): testing whether a sub-grid appears at a specific top-left position in a larger grid using forall over all sub-grid positions (lc_subgrid_at); finding the first row-major position where a template matches using between enumeration plus lc_subgrid_at with a cut (lc_find_sub); collecting all matching positions with findall (lc_all_sub); counting occurrences by measuring the lc_all_sub result (lc_subgrid_count); finding the first row index where the grid row equals a pattern via nth0 unification with a cut (lc_row_pattern); collecting all matching row indices (lc_all_row_pattern); finding the first column index whose extracted value list equals a pattern via an inner findall with a cut (lc_col_pattern); collecting all such column indices using an outer findall whose inner findall unifies with the pattern (lc_all_col_pattern); decomposing a grid row as a prefix followed by a rest using append/3 (lc_row_prefix); testing whether a grid row ends with a given suffix using append and a cut for a deterministic test (lc_row_suffix); finding the unique cell position where a value appears exactly once by collecting all positions with findall and matching a single-element list pattern (lc_anchor); counting occurrences of a value per row using maplist with a findall-plus-member helper (lc_row_count); finding sorted rows that contain a value using findall with member and sort (lc_row_contains); and finding sorted columns that contain a value using findall with nth0 and sort (lc_col_contains).


Chapter 101 covers the Extended Cell Selection Pack (Layer 119, xs_* prefix): selecting cells with value strictly less than a threshold (xs_cells_lt); selecting cells with value strictly greater than a threshold (xs_cells_gt); selecting cells with value less than or equal to a threshold (xs_cells_le); selecting cells with value greater than or equal to a threshold (xs_cells_ge); selecting cells in a closed value interval (xs_cells_between); selecting cells not equal to a given value (xs_cells_ne); locating all cells that contain the maximum value using a two-pass collect-then-filter approach (xs_max_cells); locating all cells that contain the minimum value (xs_min_cells); applying a binary threshold to produce a new grid where each cell becomes one of two values depending on whether it meets the threshold (xs_threshold); replacing cells with value below a threshold with a new value (xs_replace_lt); replacing cells with value above a threshold with a new value (xs_replace_gt); computing the sorted list of unique values as an ordinal rank ladder (xs_rank_vals); finding the 0-based ordinal rank of a specific value (xs_val_rank); and replacing every cell with its ordinal rank to produce a rank grid (xs_rank_grid).


END OF CHAPTER 11

Chapter 102 covers the Cellular Automaton Neighborhood Aggregation Pack (Layer 120, at_* prefix): producing a count grid where each cell holds the number of in-bounds 4-connected neighbors equal to a target value using triple-nested findall (at_count_nbrs_4); doing the same for 8-connected neighborhoods (at_count_nbrs_8); counting 4-connected neighbors that share the same value as the cell itself (at_count_same_4); counting same-valued 8-connected neighbors (at_count_same_8); producing a boolean 1/0 grid indicating whether any in-bounds 4-neighbor equals a target value using a cut-on-first-match helper (at_any_nbr_4); producing a boolean grid indicating whether every in-bounds 4-neighbor equals a target value using forall/2 (at_all_nbrs_4); doing the any-test for 8-neighbors (at_any_nbr_8); doing the all-test for 8-neighbors (at_all_nbrs_8); finding isolated target-valued cells that have no same-valued 4-neighbor using negation-as-failure (at_isolated_4); finding 8-connected isolated cells (at_isolated_8); applying a birth rule where dead cells with exactly N live 4-neighbors become live (at_birth_4); doing the same for 8-neighbors (at_birth_8); computing the majority value among a cell and its in-bounds 4-neighbors with tie-breaking to a background value (at_majority_4); and applying one step of Conway's Game of Life with the B3/S23 rule using a three-clause at_gol_next_ helper (at_step_gol).

Chapter 103 covers the 1D Cross-Section Extraction Pack (Layer 121, cx_* prefix): extracting a single row by index using nth0/3 (cx_row); extracting a single column as a list by collecting the C-th element from every row using member+nth0 inside findall (cx_col); extracting the main diagonal where each element is Grid[R][R] (cx_diag); extracting the anti-diagonal where each element is Grid[R][W-1-R] with a guard C>=0 for non-square grids (cx_antidiag); computing the sum of a row using sum_list/2 (cx_row_sum); computing the sum of a column (cx_col_sum); counting occurrences of a value in a row using findall+member+length (cx_row_count); counting occurrences in a column (cx_col_count); returning sorted row indices that contain a given value using between+nth0+member inside findall then sort/2 (cx_rows_with); returning sorted column indices that contain a given value (cx_cols_with); returning the sorted unique value set of a row using sort/2 (cx_row_uniq); returning the sorted unique value set of a column (cx_col_uniq); returning the most frequent value in a row with tie-breaking to a background value using the findall+max_list+unique-winner-or-Bg pattern (cx_row_mode); and doing the same for a column (cx_col_mode).

Chapter 104 covers the Boolean Mask Operations Pack (Layer 122, mk_* prefix): creating a 1/0 mask from a grid by marking positions that equal a target value using nested maplist with a two-clause cut-on-match helper (mk_from_val); creating the complementary mask that marks non-background positions (mk_from_bg); inverting every bit in a mask (mk_invert); computing element-wise AND of two masks using maplist/4 with a two-clause cut-based helper (mk_and); computing element-wise OR (mk_or); computing element-wise XOR using (A+B) mod 2 arithmetic (mk_xor); producing a grid that keeps original values at mask=1 positions and replaces mask=0 positions with a background value (mk_apply); producing a grid that fills mask=1 positions with a constant and keeps mask=0 positions unchanged (mk_fill); blending two grids by taking values from the second grid at mask=1 positions and from the first grid at mask=0 positions using maplist/5 for four-way parallel iteration (mk_overlay); returning sorted R-C pairs of mask=1 positions using findall+nth0+sort (mk_where); counting 1-valued cells using findall+member+length (mk_count); testing whether any cell equals 1 using member/2 with cut-on-first-match (mk_any); testing whether all cells equal 1 using forall/2 (built-in) (mk_all); and extracting the sorted set of unique Grid values at mask=1 positions using findall+nth0+sort (mk_extract).

Chapter 105 covers the Grid-as-Table Operations Pack (Layer 123, tb_* prefix): transposing a grid so rows become columns by iterating column indices and collecting each column using member+nth0 inside findall (tb_transpose); sorting rows ascending by the value of a specified column using findall+keysort+unwrap (tb_sort_rows); filtering rows to keep only those where a specified column equals a target value using structural equality (tb_filter_rows); grouping rows by their column value into V-Rows pairs ordered by sorted unique key using two-pass findall (tb_group_by); counting rows per unique column value as V-N pairs (tb_count_by); deduplicating rows in document order using a recursive accumulator with \+member/2 (tb_unique_rows); extracting a subset of columns in a specified order using member+nth0 inside findall (tb_select_cols); removing a column from every row using between+inequality (tb_drop_col); appending a list as a new rightmost column using nth0 index pairing and append/3 (tb_add_col); inserting a row at a specified position using two-clause recursion (tb_insert_row); deleting a row at a specified index using findall with index inequality (tb_delete_row); swapping two rows by pre-fetching both and rebuilding with if-then-else (tb_swap_rows); finding the first row where a column has its maximum value using max_list/2 then nth0+cut (tb_col_max_row); and finding the first row where a column has its minimum value using min_list/2 then nth0+cut (tb_col_min_row).
